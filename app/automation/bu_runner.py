"""bu_runner — נוהג הזמנת מסעדה (Ontopo/Tabit) דרך browser-use agent (ניווט אוטונומי).

רץ ב-.venv-bu בלבד (browser-use מצמיד google-genai==1.65, מתנגש עם ה-app שעל 2.8),
ולכן הקובץ הזה מייבא *רק* browser_use + stdlib — לעולם לא app.*. מופעל כ-subprocess:
    .venv-bu/bin/python app/automation/bu_runner.py   < job.json

קלט: JSON job ב-stdin. פלט: כותב JSON תוצאה ל-job["result_path"] (נקי, בלי ערבוב עם
ה-logs הרועשים של browser-use ב-stdout/stderr).

שני מצבים לפי job["dry_run"]:
  dry_run=True  → recon: עוצרים ב*מסך הסיכום*, לפני האישור הסופי (לא סוגרים, גם אם
                  אין כרטיס). מדווח SUMMARY_REACHED (+ CARD_REQUIRED אם נדרש כרטיס).
  dry_run=False → commit: סוגרים באמת — *אבל* אם נדרש כרטיס אשראי עוצרים שם תמיד
                  (PCI: לא מזינים כרטיס). מקום בלי-כרטיס נסגר; מדווח BOOKED <אישור>.
"""

import asyncio
import datetime
import json
import os
import re
import sys


def _notes_line(job: dict) -> str:
    """העדפות שהלקוח נתן (אזור ישיבה, אירוע) — סוגרות בחירות שאחרת היו עוצרות ב-MISSING."""
    n = (job.get("notes") or "").strip()
    return (
        f'\nהעדפות מהלקוח: "{n}" — כבד אותן בבחירות שהאתר מציע. העדפה עם צורך מאחוריה '
        "(מעשנים, נגישות, עגלה) גוברת על הניסוח: אם יש אופציה שעונה על הצורך במדויק "
        '(למשל "אזור מעשנים") — בחר אותה גם כשהלקוח ניסח כללי ("בחוץ").'
        if n
        else ""
    )


# --- בלוקים משותפים ל-builders — נוסח אחד, מילה במילה ---

# עקרונות ניווט גנריים: באנרים, אלמנטים תקועים, חוקי הברזל של האתר-בלבד,
# אימות חזותי, ואבחון כישלון מקריאת הדף. כרגע בשימוש ה-builder הקולנועי בלבד:
# פרומפט המסעדות ב-_build_task מחזיק עותק inline זהה-כמעט (בתוספת "המסעדה")
# שנשמר מילה-במילה מ-main — איחוד מלא רק אחרי ריצה חיה של מסעדות על הנוסח המשותף.
_NAV_GENERIC = """- באנר עוגיות/פרסומת — סגור והמשך. אלמנט שלא מגיב ללחיצה — נסה מקלדת (Tab/Enter)
  או גלול אליו; חזרת על אותה פעולה פעמיים בלי שינוי במסך — שנה גישה (חזור אחורה
  ונסה נתיב אחר) במקום לנסות שוב. אותו מחסום או אותה שגיאה חוזרים גם אחרי שתי
  גישות שונות — אל תמשיך לנסות: עצור מיד ודווח FAILED:<הסיבה המתאימה>.
- עבוד רק מול האתר שקיבלת. אסור לפתוח מנוע חיפוש (גוגל/DDG וכדומה) או לנחש
  כתובות אחרות כדי "למצוא" את היעד — כתובת היעד היא זו שבמשימה, במלואה, גם
  אם היא מוצגת על המסך מקוצרת (עם ...). דף שגיאת שרת (503/500 או דף שגיאה של
  CDN) — רענן פעם אחת בלבד; עדיין שגיאה → FAILED:broken_page מיד, בלי חיפושים.
- זמינות — רק לפי מה שמוצג בפועל על המסך: דפים מכילים תבניות טקסט נסתרות (כמו
  "אזלו הכרטיסים"), וכלי extract או חיפוש-טקסט מוצא אותן גם כשהכול זמין. מוצגת
  דרך להמשיך (מפת מושבים, כפתור רכישה/הזמנה) → המשך, ואל תבזבז צעדים על אימותים
  חוזרים של טקסט נסתר; דיווח אזל/מלא רק כשההודעה מוצגת חזותית על המסך.
- אינך מצליח להתקדם — אל תדווח כישלון עיוור: קודם תבין מה הבעיה מקריאת הדף עצמו
  (כתוב שהמקום סגור? אין הזמנות אונליין? מלא במועד הזה? דורש התחברות?). בחר את
  הסיבה המדויקת: FAILED:no_availability (אין מקום פנוי במועד) / FAILED:closed
  (המקום סגור או לא פעיל) / FAILED:no_online_booking (לא מקבלים הזמנות באתר) /
  FAILED:login_required / FAILED:broken_page. בשורה שלפני שורת הסיום כתוב
  בעברית מה בדיוק ראית בדף."""

# זמינות-תחילה (ממצאי בטא #1+#7): גם כשאין כלום ביום המבוקש לא מסיימים בכשל יבש —
# הדף שכבר מולנו מציג מה כן זמין; אוספים את הימים הקרובים ומחזירים אותם באותו
# מנגנון OPTIONS, כדי שגבר יציע ללקוח לסגור בהם מאותה ריצה. משותף לשני ה-builders.
_ALT_DATES_RULE = (
    "אין אף שעה פנויה באותו יום, אבל הדף שמולך כן מציג ימים סמוכים עם זמינות → "
    "אל תדווח כישלון: עצור עם MISSING:date, ובשורה שלפני שורת הסיום OPTIONS: עם "
    "התאריכים הקרובים שכן זמינים בדיוק כפי שהם מוצגים בדף (עד 6), מופרדים ב-|, "
    "למשל: OPTIONS: 18.07 | 19.07 | 21.07. גם בימים סמוכים אין כלום → "
    "FAILED:no_availability."
)

# דיאטת צעדים (דוח הזירוז 17.7): ה-gap מ-pro הוא כמעט כולו פחות צעדים, לא צעד מהיר
# יותר — שרשור פעולות ופחות צעדי-סרק. הכוונת יעילות בלבד: לא נוגעת בחוקי הברזל,
# בעצירות (MISSING/FAILED) או באימות שאחרי בחירת השעה.
_STEP_DIET = """- חסוך צעדים: כמה פעולות שאפשר לבצע ברצף על אותו מסך — בצע באותו צעד (למשל מילוי
  כל שדות הטופס בזה אחר זה), אל תגלול כדי לחפש אלמנט שכבר נראה על המסך, ואל תוסיף
  צעדי וידוא חוזרים על מה שכבר אישרת חזותית — וידוא אחד אחרי בחירה מהותית מספיק.
  חיסכון בצעדים אף פעם לא מבטל את חוקי הברזל או את חובת העצירה עליהם."""

_PERK_BLOCK = """ראית בדף פרט ששווה ללקוח לדעת — הנחה, מבצע, הגבלת זמן שולחן, דרישת הגעה בזמן?
הוסף שורה נפרדת שמתחילה ב-PERK: ואחריה תיאור קצר בעברית (למשל
PERK: 10% הנחה על התפריט בשעה הזאת). אין — אל תוסיף."""

# dry_run=True → recon: עוצרים במסך הסיכום *לפני* האישור הסופי (לא סוגרים, גם בלי כרטיס).
_DRY_TAIL = """
*** חוק ברזל: עצור ב*מסך הסיכום*, *לפני* הכפתור הסופי. לעולם אל תלחץ על כפתור
הסיום ("אשר הזמנה" / "סיום" / "הזמן" / "שלם" וכדומה), ואל תזין פרטי כרטיס אשראי —
בין אם נדרש כרטיס ובין אם לא. המטרה היא רק להגיע עד מסך הסיכום, *בלי* לסגור. ***

כשהגעת למסך הסיכום, סיים את הדיווח במילה SUMMARY_REACHED ואחריה השעה שנבחרה
בפועל (למשל: SUMMARY_REACHED 21:00). אם המסך דורש פרטי כרטיס אשראי כדי לסיים —
הוסף גם את המילה CARD_REQUIRED, ובשורה שלפני שורת הסיום הוסף:
URL: <הכתובת המלאה שבשורת הכתובת של הדפדפן עכשיו> — כדי שהלקוח יקבל לינק
שמחזיר אותו בדיוק לנקודה שבה עצרת, לא לדף ההתחלה."""

# commit (dry_run=False) → סוגרים באמת, אבל אם נדרש כרטיס אשראי עוצרים שם (PCI).
_COMMIT_TAIL = """
*** חוק ברזל — כרטיס אשראי: אם בשלב כלשהו האתר דורש פרטי כרטיס אשראי / תשלום מראש,
עצור מיד. אל תזין שום פרטי כרטיס ואל תמשיך — מקום שדורש תשלום מראש לא נסגר אוטומטית.
במקרה כזה סיים את הדיווח במילה: CARD_REQUIRED ***

אם לא נדרש כרטיס אשראי: לחץ על כפתור האישור הסופי לסגירת ההזמנה ("אשר הזמנה" /
"סיום" / "הזמן" וכדומה). ודא שמופיע מסך/הודעת אישור שההזמנה נסגרה. רק כשההזמנה
נסגרה בהצלחה — סיים את הדיווח במילה BOOKED ואחריה מספר האישור אם הופיע."""


def _build_task(job: dict) -> str:
    # נווט אוטונומי — לא מתכון דטרמיניסטי. נותנים מטרה + עובדות + חוקי ברזל, והוא
    # מבין את המסך לבד. ה-task פלטפורמה-אגנוסטי (Ontopo / Tabit / אתר אחר) — אל
    # תקודד פה שמות כפתורים או רצף קבוע. ה-markers (SUMMARY_REACHED/CARD_REQUIRED/
    # BOOKED/MISSING) הם החוזה עם _parse_result — אותם תמיד לדווח בדיוק.
    if job.get("task_type") == "cinema":
        return _build_cinema_task(job)
    if job.get("task_type") == "events":
        return _build_concert_task(job)
    if job.get("task_type") == "insurance":
        return _build_insurance_task(job)
    plat = f" (מערכת {job['platform']})" if job.get("platform") else ""
    # חוק השעה — שתי רמות: לקוח גמיש (אמר "בסביבות"/"גמיש") מקבל חלון ±60 בלי
    # עצירה; ברירת המחדל נשארת ±30 + הצעת חלופות. בשתיהן ההחלטה חד-פעמית ומהירה.
    if job.get("time_flex"):
        time_rule = (
            "הלקוח גמיש בשעה: אם המבוקשת לא זמינה — קח את הזמינה הקרובה ביותר, עד "
            "60 דקות הפרש (קדימה או אחורה), בלי לעצור ולשאול; ציין בדיווח את השעה "
            "שנבחרה בפועל. אין אף שעה גם בטווח הזה אבל יש פנויות אחרות באותו יום → "
            "עצור עם MISSING:time ושורת OPTIONS: עם השעות הפנויות (עד 8, מופרדות "
            "ב-|). " + _ALT_DATES_RULE
        )
    else:
        time_rule = (
            "רק אם המבוקשת באמת לא מופיעה או מסומנת תפוסה — הזמינה הקרובה ביותר, "
            "עד 30 דקות הפרש לכל היותר (קדימה או אחורה). אין זמינות גם בטווח הזה "
            "אבל הדף כן מציג שעות פנויות אחרות באותו יום → אל תבחר בשביל הלקוח "
            "ואל תדווח כישלון: עצור עם MISSING:time, ובשורה שלפני שורת הסיום "
            "OPTIONS: עם השעות הפנויות בדיוק כפי שהן מוצגות בדף (עד 8 הקרובות "
            "למבוקשת), מופרדות ב-|, למשל: OPTIONS: 18:45 | 19:30 | 21:15. " + _ALT_DATES_RULE
        )
    time_rule += (
        " ההחלטה הזאת מיידית: קריאה אחת של לוח הזמינות מכריעה. אסור 'לחפש' את "
        "השעה המבוקשת דרך אזורי ישיבה, מסלולים או ניסיונות חוזרים — אין אותה "
        "בלוח? פעל לפי החוק הזה מיד, בצעד אחד."
    )
    if job.get("resume"):
        # pause-resume: הדפדפן פתוח בדיוק במסך שבו הריצה הקודמת עצרה (MISSING) —
        # ממשיכים משם עם הפרטים שהלקוח השלים, בלי לנווט מחדש.
        intro = f"""
אתה ממשיך הזמנת שולחן ל-{job["party_size"]} סועדים בתאריך {job["date"]} בשעה {job["time"]}
שכבר התחלת קודם. הדפדפן פתוח בדיוק במסך שבו עצרת — המשך מהמסך הנוכחי. אל תנווט
לכתובת אחרת ואל תתחיל את התהליך מההתחלה.
מה שקרה עד כה: {job["resume"].get("recap") or ""}
עצרת כי חסר פרט מהלקוח — הוא השלים אותו עכשיו, והפרטים המעודכנים מופיעים למטה."""
    else:
        intro = f"""
המשימה: להזמין שולחן ל-{job["party_size"]} סועדים בתאריך {job["date"]} בשעה {job["time"]}.
התחל מהכתובת: {job["url"]} (דף הזמנות מקום של המסעדה, בעברית{plat})."""
    steps = f"""{intro}

אתה נווט אוטונומי. האתר יכול להיות Ontopo, Tabit, או מערכת אחרת, וה-UI משתנה
ביניהם — אל תחפש רצף כפתורים קבוע. הבן כל מסך ופעל לפי העקרונות:
- בחר את מספר הסועדים, התאריך והשעה המבוקשים. השעה המבוקשת קודמת לכל: אם היא
  מוצגת כזמינה — חובה לבחור בדיוק אותה, אסור לקחת שעה אחרת כשהמבוקשת קיימת.
  אחרי הבחירה ודא שמה שנבחר בפועל הוא אכן השעה המבוקשת — לא, תקן לפני שתמשיך.
  {time_rule}
- תאריך או שעה שנראים זמינים אבל הלחיצה עליהם לא משנה כלום (אין סימון ואין מעבר
  מסך) — אל תסיק מזה שאין זמינות: לחץ על האלמנט הלחיץ עצמו (התא/הכפתור של השעה
  או התאריך), לא על עטיפה סביבו; עדיין כלום — שלח רצף אירועי עכבר מלא
  (mousedown/mouseup/click) על האלמנט הלחיץ, ונסה גם אזור אחר של אותו אלמנט.
- התקדם דרך כל שלבי ההזמנה (חיפוש שולחן, בחירת מועד, אישור תנאים וכו') עד שתגיע
  ל*מסך הסיכום* — המסך שמרכז את כל פרטי ההזמנה ממש לפני האישור/תשלום הסופי.
- באנר עוגיות/פרסומת — סגור והמשך. אלמנט שלא מגיב ללחיצה — נסה מקלדת (Tab/Enter)
  או גלול אליו; חזרת על אותה פעולה פעמיים בלי שינוי במסך — שנה גישה (חזור אחורה
  ונסה נתיב אחר) במקום לנסות שוב. אותו מחסום או אותה שגיאה חוזרים גם אחרי שתי
  גישות שונות — אל תמשיך לנסות: עצור מיד ודווח FAILED:<הסיבה המתאימה>.
- עבוד רק מול האתר שקיבלת. אסור לפתוח מנוע חיפוש (גוגל/DDG וכדומה) או לנחש
  כתובות אחרות כדי "למצוא" את המסעדה — כתובת היעד היא זו שבמשימה, במלואה, גם
  אם היא מוצגת על המסך מקוצרת (עם ...). דף שגיאת שרת (503/500 או דף שגיאה של
  CDN) — רענן פעם אחת בלבד; עדיין שגיאה → FAILED:broken_page מיד, בלי חיפושים.
- אינך מצליח להתקדם — אל תדווח כישלון עיוור: קודם תבין מה הבעיה מקריאת הדף עצמו
  (כתוב שהמקום סגור? אין הזמנות אונליין? מלא במועד הזה? דורש התחברות?). בחר את
  הסיבה המדויקת: FAILED:no_availability (אין מקום פנוי במועד) / FAILED:closed
  (המקום סגור או לא פעיל) / FAILED:no_online_booking (לא מקבלים הזמנות באתר) /
  FAILED:login_required / FAILED:broken_page. בשורה שלפני שורת הסיום כתוב
  בעברית מה בדיוק ראית בדף.
{_STEP_DIET}

*** חוק ברזל — פרטי לקוח והחלטות: השתמש *רק* בשם/אימייל/טלפון שניתנו בדיוק כאן
למטה. אסור להמציא או לנחש שום ערך (שם, מייל, שם משפחה). אם שדה חובה בטופס ריק
ואין לך אותו — אל תמלא ואל תמציא; עצור מיד וסיים את הדיווח במילה MISSING ואחריה
שם השדה, תמיד באנגלית: MISSING:name / MISSING:last_name / MISSING:email /
MISSING:phone. האתר שלח ללקוח קוד אימות (ב-SMS או במייל) ומבקש להזין אותו —
הקוד נמצא רק אצל הלקוח: לעולם אל תמציא ואל תנחש קוד, עצור מיד ודווח
MISSING:sms_code. האתר דורש מספר תעודת זהות שלא קיבלת — לעולם אל תמציא ת"ז,
עצור ודווח MISSING:id_number. אותו כלל לבחירות שהאתר כופה ולא קיבלת עליהן העדפה — אזור ישיבה
(פנים/בחוץ/בר), סוג תפריט, וכדומה: לעולם אל תבחר בשביל הלקוח, וגם ברירת מחדל
שכבר מסומנת בטופס היא בחירה כזאת — אל תשאיר אותה סתם, עצור ודווח
MISSING:<שם הבחירה באנגלית>, למשל MISSING:seating_area. כשאתה עוצר על בחירה
כזאת, הוסף בשורה שלפני שורת הסיום שורה שמתחילה ב-OPTIONS: עם האפשרויות בדיוק
כפי שהן מופיעות בדף, מופרדות ב-|, למשל: OPTIONS: בפנים | בר גבוה | מרפסת מעשנים.
חריג יחיד: כשקיימת באמת רק אפשרות אחת — קח אותה והמשך.
קיבלת העדפה מהלקוח אבל היא לא תואמת חד-משמעית אף אפשרות בדף (למשל "בר" מול
כמה סוגי בר) — אל תתלבט בניסיונות חוזרים: אם אחת קרובה בבירור בחר אותה,
ואחרת עצור מיד עם MISSING ושורת OPTIONS. ***
פרטי קשר: שם "{job.get("name") or ""}", אימייל "{job.get("email") or ""}", טלפון "{job.get("phone") or ""}".{_notes_line(job)}

{_PERK_BLOCK}

סימנת בדרך צ'קבוקסים של הסכמה (תקנון, מדיניות ביטול, תנאי אזור ישיבה, הגבלת
זמן)? הוסף שורה נפרדת שמתחילה ב-AGREED: עם תמצית קצרה בעברית של כל מה שאושר,
מופרד ב-|, למשל: AGREED: תקנון ומדיניות ביטול | האזור בחוץ הוא אזור עישון.
תמצית בלבד, לא ציטוט הנוסח המלא. לא סימנת שום הסכמה — אל תוסיף את השורה.

בסוף הדיווח ציין מה נבחר בפועל (תאריך, שעה, מספר סועדים), ואז את שורת הסיום —
תמיד השורה האחרונה של הדיווח, באותיות גדולות בדיוק:
SUMMARY_REACHED / CARD_REQUIRED / BOOKED <אישור> / MISSING:<שדה> / FAILED:<סיבה>.
שורת סיום אחת ויחידה: אם כמה מצבים נכונים יחד (הגעת לסיכום *וגם* נדרש כרטיס) —
כתוב את שניהם באותה שורה אחת (SUMMARY_REACHED 19:00 CARD_REQUIRED), לא בשורות
נפרדות. אל תשתמש במילים האלה בשום מקום אחר בדיווח.
"""
    return steps + (_DRY_TAIL if job.get("dry_run", True) else _COMMIT_TAIL)


def _build_cinema_task(job: dict) -> str:
    # ורטיקל קולנוע (פלאנט/רב-חן/סינמה סיטי) — אותם עקרונות: מטרה + חוקי ברזל, בלי
    # שמות כפתורים. אותם markers בדיוק; ההרחבה היחידה: אחרי SUMMARY_REACHED מגיעים
    # השעה שנבחרה ואז | ותיאור המושבים (הערך-שנבחר — הנקודה שהפלייבוק מתיר להרחיב).
    if job.get("resume"):
        # pause-resume: הדפדפן פתוח בדיוק במסך שבו הריצה הקודמת עצרה (MISSING).
        intro = f"""
אתה ממשיך קניית {job["party_size"]} כרטיסים לסרט '{job["movie"]}' בתאריך {job["date"]}
שכבר התחלת קודם. הדפדפן פתוח בדיוק במסך שבו עצרת — המשך מהמסך הנוכחי. אל תנווט
לכתובת אחרת ואל תתחיל את התהליך מההתחלה.
מה שקרה עד כה: {job["resume"].get("recap") or ""}
עצרת כי חסר פרט מהלקוח — הוא השלים אותו עכשיו, והפרטים המעודכנים מופיעים למטה."""
    else:
        intro = f"""
המשימה: לקנות {job["party_size"]} כרטיסים לסרט '{job["movie"]}' בתאריך {job["date"]},
בבית הקולנוע של הרשת ב-{job["city"]}, סביב השעה {job["time"]}.
התחל מהכתובת: {job["url"]} (דף הסרט באתר הרשת)."""
    # הוט סינמה בלבד: עובדות מה-recon (17.7) שהנווט לא יגלה לבד — דומיין הכרטוס
    # הנפרד הוא חלק מהזרימה, והחזקת המושבים קצובה.
    hot = (
        """
- ברשת הוט סינמה ההזמנה עוברת לדומיין נפרד tickets.hotcinema.co.il (אשף:
  כרטיסים→מושבים→תשלום) — זה חלק מהזרימה הרגילה, לא עזיבת האתר. כמות הכרטיסים
  נבחרת לפני מפת המושבים. מושבים שנבחרו מוחזקים 9 דקות בלבד — אל תתמהמה בין
  השלבים. קיר-התשלום (טופס פרטים + אשראי) הוא נקודת העצירה הרגילה."""
        if job.get("platform") == "hot-cinema"
        else ""
    )
    steps = f"""{intro}

אתה נווט אוטונומי. אתרי הרשתות שונים זה מזה וה-UI משתנה — אל תחפש רצף כפתורים
קבוע. הבן כל מסך ופעל לפי העקרונות:
- בחר את בית הקולנוע לפי העיר/הסניף שהלקוח ביקש ({job["city"]}). אין לרשת סניף
  בעיר הזאת → FAILED:no_cinema_in_city, ובשורה שלפני שורת הסיום כתוב בעברית מה
  ראית בדף.
- בחר הקרנה בתאריך המבוקש, הקרובה ביותר לשעה המבוקשת — עד 90 דקות סטייה לכל
  היותר (קדימה או אחורה). המבוקש לא זמין — אל תדווח כישלון יבש; הצע ממה שהדף
  שמולך כבר מציג: אין הקרנה בטווח אבל יש הקרנות אחרות באותו יום → עצור עם
  MISSING:time ושורת OPTIONS: עם שעות ההקרנה של אותו היום (עד 6, מופרדות ב-|).
  אין הקרנה בתאריך המבוקש בכלל, אבל לוח ההקרנות מציג תאריכים אחרים שכן יש בהם
  הקרנה → עצור עם MISSING:date ושורת OPTIONS: עם התאריכים הקרובים שכן קיימים
  בדיוק כפי שהם מוצגים בדף (עד 6, מופרדים ב-|, למשל:
  OPTIONS: 21.07 | 22.07 | 28.07). רק כשאין אף הקרנה בשום תאריך, או שאזלו
  הכרטיסים ואין אף חלופה להציע → FAILED:no_availability.
- פורמט ההקרנה הוא בחירה מהותית: אם לאותו חלון זמן יש יותר מפורמט אחד (רגיל /
  תלת-ממד / IMAX / 4DX / VIP / מדובב / כתוביות) והלקוח לא נתן העדפה — לעולם אל
  תבחר בשבילו: עצור ודווח MISSING:format, ובשורה שלפני שורת הסיום שורת OPTIONS:
  עם הפורמטים בדיוק כפי שהם מופיעים בדף, מופרדים ב-|.
  חריג יחיד: כשקיימת באמת רק אפשרות אחת — קח אותה והמשך.
- מושבים — ברירת מחדל מוצהרת (החריג היחיד שבו אתה כן בוחר בשביל הלקוח):
  {job["party_size"]} מושבים צמודים באמצע האולם — שורות אמצע, מרכז השורה,
  לא שורה ראשונה. אין צמודים באזור האמצע → הצמודים הטובים ביותר שקיימים. אין צמודים
  בכלל, או שכל הפנוי הוא מושבים בתוספת תשלום (VIP / כורסא / D-BOX) — עצור ודווח
  MISSING:seats עם שורת OPTIONS: של מה שבאמת פנוי (למשל:
  OPTIONS: זוג מפוצל שורות 4+5 | מושבי VIP בתוספת תשלום). מושב רגיל ללא תוספת
  תשלום → אל תעצור, קח והמשך.
- מפת המושבים היא לרוב SVG: קליק שלא נתפס — אל תנחש selectors בניסיונות עיוורים;
  בדוק קודם את מבנה האלמנטים בפועל (find_elements על ה-SVG) ורק אז בחר. לרוב יש
  אלמנט לחיץ ייעודי (hit-area) מעל ציור המושב, ו-click()‎ תכנותי לבדו לא נקלט —
  שלח רצף אירועי עכבר מלא (mousedown/mouseup/click) על האלמנט הלחיץ עצמו.
- מפה שהיא canvas (ציור אחד, אין טקסט אזורים ב-DOM, חיפוש שם האזור מחזיר 0):
  המקרא הוא רק תצוגה — לחיצות עליו לא בוחרות כלום. לחץ ישירות על האזור הצבוע
  במפה עצמה (קואורדינטות בתוך שטח האזור, רצף עכבר מלא). אם אחרי שתי לחיצות
  באזורים שונים שום בורר כמות לא נפתח — עצור ודווח FAILED:broken_page בכנות.
- המעבר משלב לשלב (ובמיוחד לתשלום) עשוי לכלול אימות אבטחה אוטומטי — כפתור שלא
  מגיב מיד אינו בהכרח תקוע: ודא שסימון אישור התנאים אכן נקלט, המתן ~15 שניות
  לאימות, ונסה שוב פעם אחת לפני שאתה משנה גישה.{hot}
{_NAV_GENERIC}
{_STEP_DIET}

*** חוק ברזל — פרטי לקוח והחלטות: השתמש *רק* בשם/אימייל/טלפון שניתנו בדיוק כאן
למטה. אסור להמציא או לנחש שום ערך (שם, מייל, שם משפחה). אם שדה חובה בטופס ריק
ואין לך אותו — אל תמלא ואל תמציא; עצור מיד וסיים את הדיווח במילה MISSING ואחריה
שם השדה, תמיד באנגלית: MISSING:name / MISSING:last_name / MISSING:email /
MISSING:phone. האתר שלח ללקוח קוד אימות (ב-SMS או במייל) ומבקש להזין אותו —
הקוד נמצא רק אצל הלקוח: לעולם אל תמציא ואל תנחש קוד, עצור מיד ודווח
MISSING:sms_code. האתר דורש מספר תעודת זהות שלא קיבלת — לעולם אל תמציא ת"ז,
עצור ודווח MISSING:id_number. אותו כלל לכל בחירה אחרת שהאתר כופה ולא קיבלת עליה העדפה (מלבד
חריג המושבים למעלה) — פורמט, גרסה (מדובב/כתוביות): עצור ודווח
MISSING:<שם הבחירה באנגלית>, למשל MISSING:format / MISSING:language, ולפני שורת
הסיום שורת OPTIONS: עם האפשרויות בדיוק כפי שהן מופיעות בדף, מופרדות ב-|.
קיבלת העדפה מהלקוח אבל היא לא תואמת חד-משמעית אף אפשרות בדף — אל תתלבט
בניסיונות חוזרים: אם אחת קרובה בבירור בחר אותה, ואחרת עצור מיד עם MISSING
ושורת OPTIONS. ***
פרטי קשר: שם "{job.get("name") or ""}", אימייל "{job.get("email") or ""}", טלפון "{job.get("phone") or ""}".{_notes_line(job)}

{_PERK_BLOCK}

בסוף הדיווח ציין מה נבחר בפועל (סניף, הקרנה, פורמט, מושבים), ואז את שורת הסיום —
תמיד השורה האחרונה של הדיווח, באותיות גדולות בדיוק, ובה *אחת בלבד* מהצורות:
SUMMARY_REACHED <שעת ההקרנה שנבחרה> | <תיאור מושבים קצר> (למשל:
SUMMARY_REACHED 21:30 | שורה 7 מושבים 11,12), או CARD_REQUIRED, או
BOOKED <אישור>, או MISSING:<שדה>, או FAILED:<סיבה>.
עצרת על שדה חסר או כישלון — שורת הסיום היא MISSING:<שדה> או FAILED:<סיבה>
לבדה, בלי SUMMARY_REACHED לפניה, גם אם כבר הגעת למסך מתקדם.
אל תשתמש במילים האלה בשום מקום אחר בדיווח.
"""
    # בקולנוע קיר התשלום הוא הסוף הטבעי תמיד — מסך התשלום הוא מסך הסיכום.
    tail = _DRY_TAIL if job.get("dry_run", True) else _COMMIT_TAIL
    return steps + tail + "\nמסך התשלום הוא מסך הסיכום שלך — עצור לפניו."


def _build_concert_task(job: dict) -> str:
    # ורטיקל הופעות (לאן/קופת ת"א) — אותם עקרונות ואותם markers כמו קולנוע. אין שעה
    # מבוקשת בכלל: השעה נגזרת מהמופע. המחיר נכנס למקטע ה-| של שורת הסיום —
    # אפס שינוי ב-_parse_result.
    if job.get("resume"):
        # pause-resume: הדפדפן פתוח בדיוק במסך שבו הריצה הקודמת עצרה (MISSING).
        intro = f"""
אתה ממשיך קניית {job["party_size"]} כרטיסים להופעה של '{job["artist"]}'
שכבר התחלת קודם. הדפדפן פתוח בדיוק במסך שבו עצרת — המשך מהמסך הנוכחי. אל תנווט
לכתובת אחרת ואל תתחיל את התהליך מההתחלה.
מה שקרה עד כה: {job["resume"].get("recap") or ""}
עצרת כי חסר פרט מהלקוח — הוא השלים אותו עכשיו, והפרטים המעודכנים מופיעים למטה."""
    else:
        when = f" בתאריך {job['date']}" if job.get("date") else ""
        where = f" ב-{job['venue']}" if job.get("venue") else ""
        intro = f"""
המשימה: לקנות {job["party_size"]} כרטיסים להופעה של '{job["artist"]}'{when}{where}.
התחל מהכתובת: {job["url"]} (דף האירוע באתר הכרטיסים)."""
    date_line = job.get("date") or "לא צוין"
    steps = f"""{intro}

אתה נווט אוטונומי. אתרי הכרטיסים שונים זה מזה וה-UI משתנה — אל תחפש רצף כפתורים
קבוע. הבן כל מסך ופעל לפי העקרונות:
- תאריך = אירוע בדיד: בחר את המופע בתאריך המבוקש ({date_line}). לא צוין תאריך:
  יש מופע קרוב אחד בלבד → קח אותו והמשך. יש כמה מועדים (או שהמבוקש לא קיים אבל
  יש אחרים) → לעולם אל תבחר בשביל הלקוח ואל תדווח כישלון: עצור עם MISSING:date,
  ובשורה שלפני שורת הסיום OPTIONS: עם המועדים בדיוק כפי שהם בדף כולל עיר/היכל,
  מופרדים ב-|, למשל: OPTIONS: 11/08 היכל מנורה ת"א | 15/08 היכל הפיס חיפה.
  המועד המבוקש אזל אבל הדף מציג מועדים אחרים שעדיין זמינים → אותו דין בדיוק,
  אל תדווח כישלון: MISSING:date עם שורת OPTIONS: של המועדים שכן זמינים (עד 6).
  כל המועדים אזלו לגמרי → FAILED:sold_out. המופע לא מתקיים כלל בעיר/היכל שביקש
  הלקוח → FAILED:no_event_in_city. הדף לא מציג אף מועד עתידי לרכישה (מידע על
  מופע שכבר עבר, טופס "הרשמו לעדכונים" בלבד, אין כפתור רכישה) — זה *לא* אזל:
  FAILED:no_upcoming_dates. FAILED:sold_out רק כשמוצג חזותית שהכרטיסים אזלו
  למועד קיים.
- קטגוריית מחיר = בחירה מהותית: יש יותר מקטגוריית מחיר/אזור אחד (פרטר/יציע/גולד
  וכו') והלקוח לא נתן העדפה → לעולם אל תבחר: עצור MISSING:price_category, ושורת
  OPTIONS: עם הקטגוריות והמחירים בדיוק כפי שבדף, למשל:
  OPTIONS: פרטר 320 ש"ח | יציע 180 ש"ח. חריג יחיד: קטגוריה אחת בלבד → קח והמשך.
- אזל-או-לא — רק לפי מה שמוצג בפועל: דפי אירועים מכילים תבניות טקסט נסתרות כמו
  "אזלו הכרטיסים", וכלי extract או חיפוש-טקסט מוצא אותן גם כשהמופע זמין לגמרי.
  מוצגת מפת מושבים אינטראקטיבית או כפתור רכישה → יש כרטיסים; המשך, ואל תבזבז
  צעדים על אימותים חוזרים של טקסט "אזלו". FAILED:sold_out רק כשההודעה מוצגת
  חזותית על המסך ליד המופע עצמו. הקטגוריות והמחירים חיים לרוב בתוך מפת המושבים
  או המקרא שלה — פתח אותה ולחץ על אזור כדי לראות מחיר, במקום לחפש טקסט בדף.
- מושבים — ברירת מחדל מוצהרת (החריג היחיד שבו אתה כן בוחר בשביל הלקוח), בתוך
  הקטגוריה שנבחרה: {job["party_size"]} מושבים צמודים באמצע — שורות אמצע, מרכז
  השורה, לא שורה ראשונה. אין צמודים באזור האמצע → הצמודים הטובים ביותר שקיימים.
  אין צמודים בכלל, או שכל הפנוי הוא מושבים בתוספת תשלום — עצור ודווח
  MISSING:seats עם שורת OPTIONS: של מה שבאמת פנוי. מושב רגיל ללא תוספת
  תשלום → אל תעצור, קח והמשך.
  באולמות גדולים המפה היררכית — ייתכן צעד בחירת גוש/מפלס לפני מפת המושבים;
  בחר את הגוש שמתאים לקטגוריה שנבחרה.
- מפת המושבים היא לרוב SVG: קליק שלא נתפס — אל תנחש selectors בניסיונות עיוורים;
  בדוק קודם את מבנה האלמנטים בפועל (find_elements על ה-SVG) ורק אז בחר. לרוב יש
  אלמנט לחיץ ייעודי (hit-area) מעל ציור המושב, ו-click()‎ תכנותי לבדו לא נקלט —
  שלח רצף אירועי עכבר מלא (mousedown/mouseup/click) על האלמנט הלחיץ עצמו.
- מפה שהיא canvas (ציור אחד, אין טקסט אזורים ב-DOM, חיפוש שם האזור מחזיר 0):
  המקרא הוא רק תצוגה — לחיצות עליו לא בוחרות כלום. לחץ ישירות על האזור הצבוע
  במפה עצמה (קואורדינטות בתוך שטח האזור, רצף עכבר מלא). אם אחרי שתי לחיצות
  באזורים שונים שום בורר כמות לא נפתח — עצור ודווח FAILED:broken_page בכנות.
- מפת מושבים בתוך iframe של דומיין אחר (הכתובת בתוך המפה שונה מהאתר עצמו):
  evaluate/JS רץ על הדף החיצוני ויחזיר ריק *תמיד* — אל תנסה לקרוא את המפה
  ב-JS ואל תחזור על evaluate שוב ושוב. עבוד רק בקליקים אמיתיים לפי מה שנראה
  חזותית על המסך (קואורדינטות בתוך המפה, רצף עכבר מלא). שני ניסיונות evaluate
  שחזרו ריקים = עצור בכנות מיד: MISSING:price_category עם שורת OPTIONS: של
  הקטגוריות/המחירים שנראים חזותית במפה או במקרא, או FAILED:broken_page אם
  המפה לא מציגה כלום.
- המעבר משלב לשלב (ובמיוחד לתשלום) עשוי לכלול אימות אבטחה אוטומטי — כפתור שלא
  מגיב מיד אינו בהכרח תקוע: ודא שסימון אישור התנאים אכן נקלט, המתן ~15 שניות
  לאימות, ונסה שוב פעם אחת לפני שאתה משנה גישה.
{_NAV_GENERIC}
{_STEP_DIET}

*** חוק ברזל — פרטי לקוח והחלטות: השתמש *רק* בשם/אימייל/טלפון שניתנו בדיוק כאן
למטה. אסור להמציא או לנחש שום ערך (שם, מייל, שם משפחה, תעודת זהות). אם שדה חובה
בטופס ריק ואין לך אותו — אל תמלא ואל תמציא; עצור מיד וסיים את הדיווח במילה
MISSING ואחריה שם השדה, תמיד באנגלית: MISSING:name / MISSING:last_name /
MISSING:email / MISSING:phone. חלק מהקופות בארץ דורשות ת"ז — אין לך אותה →
MISSING:id_number, לעולם אל תמציא. האתר שלח ללקוח קוד אימות (ב-SMS או במייל)
ומבקש להזין אותו — הקוד נמצא רק אצל הלקוח: לעולם אל תמציא ואל תנחש קוד, עצור
מיד ודווח MISSING:sms_code. קיר קוד כזה הוא עצירה לשאלת הלקוח, לא כישלון — אל
תדווח עליו FAILED:login_required גם כשהוא מופיע כחלק ממסך התחברות/הרשמה.
קיבלת קוד מהלקוח והאתר דחה אותו — אל תנסה לנחש קוד אחר: עצור ודווח
MISSING:sms_code שוב. אותו כלל לכל בחירה אחרת שהאתר כופה ולא קיבלת
עליה העדפה (מלבד חריג המושבים למעלה): עצור ודווח MISSING:<שם הבחירה באנגלית>,
ולפני שורת הסיום שורת OPTIONS: עם האפשרויות בדיוק כפי שהן מופיעות בדף, מופרדות
ב-|. קיבלת העדפה מהלקוח אבל היא לא תואמת חד-משמעית אף אפשרות בדף — אל תתלבט
בניסיונות חוזרים: אם אחת קרובה בבירור בחר אותה, ואחרת עצור מיד עם MISSING
ושורת OPTIONS. דרישת יצירת חשבון בלי אפשרות רכישה כאורח → FAILED:login_required. ***
פרטי קשר: שם "{job.get("name") or ""}", אימייל "{job.get("email") or ""}", טלפון "{job.get("phone") or ""}".{_notes_line(job)}

{_PERK_BLOCK}

בסוף הדיווח ציין מה נבחר בפועל (מועד, היכל, קטגוריה, מושבים, מחיר כולל), ואז שורת
הסיום — תמיד האחרונה, באותיות גדולות בדיוק, ובה *אחת בלבד* מהצורות:
SUMMARY_REACHED <שעת המופע> | <קטגוריה, מושבים וסה"כ מחיר> (למשל:
SUMMARY_REACHED 21:00 | פרטר שורה 12 מושבים 7,8 — סה"כ 640 ש"ח), או CARD_REQUIRED,
או BOOKED <אישור>, או MISSING:<שדה>, או FAILED:<סיבה>.
עצרת על שדה חסר או כישלון — MISSING/FAILED לבדה, בלי SUMMARY_REACHED לפניה,
גם אם כבר הגעת למסך מתקדם. אל תשתמש במילים האלה בשום מקום אחר בדיווח.
"""
    # כרטיס דיגיטלי ⇒ CARD_REQUIRED הוא הסוף הטבעי תמיד — כמו קולנוע.
    tail = _DRY_TAIL if job.get("dry_run", True) else _COMMIT_TAIL
    return steps + tail + "\nמסך התשלום הוא מסך הסיכום שלך — עצור לפניו."


# תאריך יום.חודש בלי שנה — הצינור מעביר "01.09" וטופס הביטוח דורש תאריך מלא
# (QA ביטוח 18.7 #6: נסיעה חוצת-שנה בסיכון). ההשלמה בצד ה-builder, מההקשר.
_DM_ONLY = re.compile(r"^(\d{1,2})\.(\d{1,2})$")


def _full_date(s: str, floor: "datetime.date | None" = None) -> str:
    """DD.MM בלי שנה → DD.MM.YYYY: התאריך העתידי הקרוב ביחס ל-floor (ברירת מחדל:
    היום) — ביטוח נקנה קדימה, אז תאריך שכבר עבר מקבל את השנה הבאה; תאריך חזרה
    שקטן מתאריך היציאה (floor=יציאה) גולש לשנה הבאה. עם שנה / לא-פריק → כמו שהוא."""
    s = (s or "").strip()
    m = _DM_ONLY.match(s)
    if not m:
        return s
    day, month = int(m.group(1)), int(m.group(2))
    floor = floor or datetime.date.today()
    for year in (floor.year, floor.year + 1):
        try:
            if datetime.date(year, month, day) >= floor:
                return f"{day:02d}.{month:02d}.{year}"
        except ValueError:  # 31.02 וכדומה — הטופס יכריע בעצמו
            return s
    return s


def _parse_full(s: str) -> "datetime.date | None":
    try:
        return datetime.datetime.strptime(s, "%d.%m.%Y").date()
    except ValueError:
        return None


# שמות המבטחים לתצוגה ב-task — מקומי בכוונה (bu_runner מייבא רק browser_use+stdlib,
# אסור לו לייבא את resolve). טסט חוזה מיישר מול resolve.INSURANCE_COMPANIES.
_INSURER_HE = {
    "passportcard": "פספורטכארד",
    "harel": "הראל",
    "phoenix": "הפניקס",
    "aig": "AIG",
    "migdal": "מגדל",
}


def _build_insurance_task(job: dict) -> str:
    # ורטיקל ביטוח (קבוצה סגורה של מבטחים) — אותם עקרונות: מטרה + חוקי ברזל, בלי
    # selectors. אותם markers; ההרחבות המותרות: payload אחרי SUMMARY_REACHED | (הפרמיה),
    # ו-MISSING מרובה-שדות (איסוף מרוכז פר-דף) — טופס עתיר שדות אישיים, עצירה פר-שדה
    # הייתה הופכת לעשרות סבבים.
    ins = job.get("insurance") or {}
    platform = job.get("platform") or "passportcard"
    comp = _INSURER_HE.get(platform, "חברת הביטוח")
    # חוק היעד: אצל פספורטכארד נוסח הדף מוכח-חי (חיפוש מדינות טקסט חופשי, אין
    # יבשות); אצל שאר המבטחים לא מניחים כלום על מבנה הדף — נוסח כללי, אותה עצירה.
    if platform == "passportcard":
        dest_rule = """- יעד: דף היעד מציג מדינות + שדה חיפוש טקסט חופשי. הקלד בשדה החיפוש את שם המדינה
  שנמסרה ובחר את ההתאמה המדויקת. אין שם אזורים/יבשות ("אירופה" לא מחזיר כלום) — רק
  מדינות. המדינה שנמסרה לא מופיעה גם אחרי חיפוש, או שאינך בטוח איזו אפשרות מתאימה —"""
    else:
        dest_rule = """- יעד: בחר את המדינה שנמסרה בדרך שהדף מציע (חיפוש או רשימה). היעד שנמסר לא
  מופיע, או שאינך בטוח איזו אפשרות מתאימה —"""
    travelers = ins.get("travelers") or []
    trav = "\n".join(f"  נוסע {i + 1}: תאריך לידה {t}" for i, t in enumerate(travelers))
    if job.get("resume"):
        answers = "".join(f"\n  {k} = {v}" for k, v in (job.get("form_answers") or {}).items())
        # רשימת הנוסעים גם ב-resume — הוכח ב-recon על AIG (18.7): ה-resume נשא רק
        # recap + תשובות, ה-agent שלא ראה את תאריכי הלידה עצר עם MISSING:p1_birth_date
        # על ערך שהלקוח כבר מסר — סבב שאלה מיותר ללקוח.
        trav_line = f"\nהנוסעים מהבקשה המקורית:\n{trav}" if trav else ""
        intro = f"""
אתה ממשיך מילוי הצעה לביטוח נסיעות לחו"ל שכבר התחלת קודם. הדפדפן פתוח בדיוק במסך
שבו עצרת — המשך מהמסך הנוכחי. אל תנווט לכתובת אחרת ואל תתחיל מההתחלה.
מה שקרה עד כה: {job["resume"].get("recap") or ""}{trav_line}
עצרת כי חסרו פרטים מהלקוח — הוא השלים אותם. אלה הערכים לשדות שדיווחת כחסרים
(המפתח = השם שנתת בדיווח; הזן כל ערך בשדה המתאים לו):{answers or " (אין)"}"""
    else:
        # תאריכים מלאים ל-task (DD.MM.YYYY): שנה חסרה מושלמת מההקשר — יציאה ביחס
        # להיום, חזרה ביחס ליציאה (נסיעה חוצת-שנה מקבלת את השנה הנכונה).
        depart = _full_date(job.get("date") or "")
        ret = _full_date(ins.get("return_date") or "", _parse_full(depart))
        intro = f"""
המשימה: להגיע להצעת מחיר לביטוח נסיעות לחו"ל באתר {comp} ({len(travelers)} נוסעים).
התחל מהכתובת: {job["url"]} (פאנל הרכישה, בעברית). זהו טופס רב-שלבי.
פרטי הנסיעה: יעד {ins.get("destination") or ""}, יציאה {depart}, חזרה {ret}.
הנוסעים:
{trav or "  (לא נמסרו)"}"""
    # שני חוקי-נוסעים שהוכחו ב-recon על AIG (18.7): המשך-ריצה עצר עם
    # MISSING:p1_birth_date על תאריך שנמסר, וההמשך אחריו הגיש SUMMARY_REACHED על
    # נוסע אחד מתוך שניים ($28.35 · "נוסע 1"). נפלטים רק כשיש רשימת נוסעים —
    # בלי נוסעים החוק ("כל 0 הנוסעים") היה הנחיה הפוכה.
    trav_rules = (
        f"""
- תאריכי הלידה שנמסרו הם הערכים לשדות תאריך הלידה בטופס — הזן אותם לפי המספור;
  תאריך לידה שנמסר לעולם אינו שדה "חסר", אל תעצור עליו עם MISSING.
- ההצעה חייבת לכסות את כל {len(travelers)} הנוסעים: הטופס נפתח עם פחות נוסעים —
  הוסף את החסרים לפני שתמשיך. מסך שמציג פרמיה למספר נוסעים שונה מהמבוקש אינו מסך
  הסיכום — אל תדווח עליו SUMMARY_REACHED."""
        if travelers
        else ""
    )
    steps = f"""{intro}

אתה נווט אוטונומי. הבן כל מסך ופעל לפי העקרונות:
- זו רכישה של לקוח חדש (לא "לקוח קיים") — אל תיכנס למסלול לקוח קיים ואל תנסה להתחבר.{trav_rules}
{dest_rule}
  אל תנחש: עצור עם MISSING:destination ושורת OPTIONS destination: עם האפשרויות
  בדיוק כפי שמופיעות בדף.
- הצהרת הבריאות: תשובות הלקוח — "{ins.get("health") or "אין"}". "אין" פירושו שהלקוח ענה
  שלילית בשיחה על ארבע הקטגוריות (מחלות קשות מהרשימה / מחלה כרונית או תרופות קבועות /
  טיפול או טיפול צפוי בחצי השנה האחרונה / הריון — אף נוסעת אינה בהריון) — רק לשאלות
  שתואמות בדיוק את הקטגוריות האלה מותר לסמן "לא". כל שאלה בריאותית אחרת, או שאלה
  שהתשובות שבידך לא עונות עליה חד-משמעית — זו הצהרה משפטית: לעולם אל תענה בשם הלקוח,
  עצור עם MISSING ושורת FIELD שמצטטת את נוסח השאלה מהדף.
- הרחבות (כיסויים אופציונליים — ביטול נסיעה, כבודה, סקי וכו'): סמן *רק* מה שהלקוח
  ביקש: "{ins.get("addons") or "שום הרחבה"}". הרחבה שהאתר סימן מראש ולא התבקשה — בטל
  את הסימון. מחיר של הרחבה שביקש הלקוח, כפי שמוצג בדף — דווח ב-PERK.
- התקדם שלב-שלב עד *הצעת המחיר* — המסך שמציג את הפרמיה לתשלום עבור הנסיעה. זהו
  מסך הסיכום שלך.
- כישלונות ייחודיים: הדף מודיע שנדרש אישור נציג / חיתום טלפוני (למשל בעקבות הצהרת
  בריאות או גיל) → FAILED:manual_underwriting. הדף מציע רק "השארת פרטים" או שיחה עם
  נציג במקום הצעה אונליין → FAILED:phone_only. האתר חוסם אותך (דף שגיאה 403, אימות
  אנושי שלא נעלם, חסימת בוטים) → FAILED:blocked. בשורה שלפני שורת הסיום כתוב בעברית
  מה בדיוק ראית.
{_NAV_GENERIC}

*** חוק ברזל — פרטים אישיים: השתמש *רק* בערכים שניתנו כאן. אסור להמציא או לנחש שום
ערך — תעודת זהות, תאריך לידה, שם, טלפון, מייל, תשובת בריאות. הזין ערך שנמסר ונדחה
על ידי הטופס (למשל ת"ז לא תקינה)? אל תנסה וריאציות — עצור עם MISSING על השדה, ושורת
FIELD שמסבירה שהערך נדחה ומה הטופס דורש.
נתקלת בדף עם שדות חובה שאין לך? אל תעצור על הראשון: קודם מלא את כל מה שכן יש לך,
עבור על *כל* הדף, אסוף את כל שדות החובה הריקים שאין לך ערך עבורם, ורק אז עצור ודווח
את כולם יחד:
- לכל שדה שורת FIELD <מפתח>: <תווית השדה בעברית כפי שמופיעה בדף>. המפתח באנגלית,
  אותיות קטנות ו-_, ייחודי (למשל id_number, passenger2_birth_date). שדה ששייך
  לנוסע מסוים — חובה שהתווית תכלול תיוג "(נוסע N)" לפי המספור שלמעלה, למשל:
  FIELD p2_gender: מגדר (נוסע 2) — בלעדיו הלקוח לא יודע על מי מהנוסעים השאלה.
- שדה שהוא בחירה מרשימה — הוסף גם שורת OPTIONS <מפתח>: עם האפשרויות בדיוק כפי
  שמופיעות בדף, מופרדות ב-|.
- שורת הסיום: MISSING:<מפתח1>|<מפתח2> — כל המפתחות בשורה אחת, מופרדים ב-| בלי
  רווחים, והיא לבדה (בלי SUMMARY_REACHED לפניה). אותו כלל לבחירות שהאתר כופה ולא
  קיבלת עליהן העדפה (נקודת איסוף הכרטיס, אזור יעד). חריג יחיד: כשקיימת באמת רק
  אפשרות אחת — קח אותה והמשך. ***
פרטי קשר: שם "{job.get("name") or ""}", אימייל "{job.get("email") or ""}", טלפון "{job.get("phone") or ""}".{_notes_line(job)}

{_PERK_BLOCK}

סימנת בדרך צ'קבוקסים של הסכמה (תקנון, תנאי פוליסה, הסכמות דיוור)? הוסף שורה נפרדת
שמתחילה ב-AGREED: עם תמצית קצרה בעברית של כל מה שאושר, מופרד ב-|. זה קריטי כאן —
שום הצהרה לא נחתמת בשקט. הצהרת בריאות היא לא צ'קבוקס תקנון — עליה חלות רק ההוראות
למעלה. לא סימנת שום הסכמה — אל תוסיף את השורה.

בסוף הדיווח ציין מה מולא בפועל (יעד, תאריכים, נוסעים, הרחבות), ואז את שורת הסיום —
תמיד השורה האחרונה, באותיות גדולות בדיוק, ובה *אחת בלבד* מהצורות:
SUMMARY_REACHED | <הפרמיה שהוצעה + תמצית> (למשל:
SUMMARY_REACHED | פרמיה $127.40 לכל הנסיעה · אירופה 03.08-17.08 · 2 נוסעים · כולל ביטול נסיעה),
או CARD_REQUIRED, או MISSING:<שדות>, או FAILED:<סיבה>.
אל תשתמש במילים האלה בשום מקום אחר בדיווח.
"""
    tail = _DRY_TAIL if job.get("dry_run", True) else _COMMIT_TAIL
    return steps + tail + "\nהצעת המחיר היא מסך הסיכום שלך — מסך שדורש פרטי תשלום הוא כבר מעבר לה."


def _marker_arg(line: str, marker: str) -> str:
    """הטקסט שאחרי marker בשורת הסיום ('MISSING:email' → 'email'). ריק-בטוח."""
    parts = line[line.find(marker) + len(marker) :].strip(" :–-").split()
    return parts[0].rstrip(".,") if parts else "unknown"


def _parse_result(final: str, *, commit: bool) -> dict:
    """דיווח ה-agent → תוצאת JSON. הנתיב הבטיחותי: markers נקראים *רק* מהשורה האחרונה
    ו-case-sensitive (החוזה ב-task: שורת סיום באותיות גדולות) — פרוזה כמו "fully booked"
    או "לא נדרש כרטיס" לא מדליקה כלום. בסגירה (commit) success=True רק אם באמת נסגר
    (BOOKED). ב-recon הצלחה = SUMMARY_REACHED בלי סגירה. MISSING:<שדה> = שדה חובה ריק →
    כישלון + השדה ב-details. FAILED:<סיבה> = לא הצליח להתקדם (אין זמינות/login/דף שבור)."""
    final = (final or "").strip()
    lines = [ln for ln in final.splitlines() if ln.strip()]
    # בלוק הסיום = עד 3 השורות האחרונות, לא רק האחרונה: ה-agent לפעמים מפצל את
    # שורת הסיום ("SUMMARY_REACHED 19:00" ואז "CARD_REQUIRED" בשורה נפרדת) —
    # נצפה חי פעמיים ב-15.7: ריצה שהגיעה למסך האשראי דווחה ללקוח ככישלון.
    # ה-markers עדיין case-sensitive באותיות גדולות בלבד, אז פרוזה לא מדליקה כלום.
    # חריג בטיחותי: BOOKED (רישום הזמנה אמיתית) נקרא רק מהשורה האחרונה ממש —
    # מרקר-הזמנה באמצע דיווח לעולם לא רושם הזמנה פנטום (הגנת R1 נשארת קשיחה).
    last = " ".join(lines[-3:])
    strict_last = lines[-1] if lines else ""
    card = "CARD_REQUIRED" in last
    # MISSING מרובה-שדות (ורטיקל הביטוח): MISSING:key1|key2|key3 — כל המפתחות בשורה
    # אחת בלי רווחים, אז הטוקן הראשון אחרי המרקר הוא כל הרשימה. missing (יחיד) נשאר
    # השדה הראשון — כל צרכן קיים (truthiness, מסלול שדה-בודד) עובד ללא שינוי.
    raw_missing = _marker_arg(last, "MISSING:") if "MISSING:" in last else ""
    missing_fields = [f for f in raw_missing.split("|") if f][:12]
    missing = missing_fields[0] if missing_fields else ""
    failed = _marker_arg(last, "FAILED:") if "FAILED:" in last else ""
    # בלוק סיום בלי אף marker = ה-agent מת בלי לדווח → browser_error מפורש, לא שתיקה.
    # נצפה חי פעמיים בענף ההופעות: דיווח ריק (סשן Browserbase נפל, HTTP 410, עצירה
    # אחרי 5 כשלים) וגם דיווח זבל לא-ריק ("Waited for 5 seconds" —
    # history.final_result() מהדהד את הפעולה האחרונה כשה-agent נקטע בלי done).
    # החוזה מחייב marker בשורת הסיום — בלעדיו אין תוצאה, יש דפדפן שמת באמצע.
    if not any(
        m in last for m in ("SUMMARY_REACHED", "CARD_REQUIRED", "BOOKED", "MISSING:", "FAILED:")
    ):
        failed = "browser_error"
    # השעה שנבחרה בפועל (החוזה: אחרי SUMMARY_REACHED) — כדי שגבר יציע חלופה ללקוח
    # ("יש 21:00 במקום 20:30, מתאים?") לפני הסגירה, וה-commit יסגור את מה שאושר.
    m = re.search(r"\b(\d{1,2}:\d{2})\b", last)
    chosen_time = m.group(1) if m else ""
    # קולנוע: תיאור המושבים אחרי | בשורת הסיום (SUMMARY_REACHED 21:30 | שורה 7 ...).
    # נקרא *רק* משורת ה-SUMMARY_REACHED עצמה (החוזה: המושבים על אותה שורה) — לא
    # מכל בלוק 3-השורות: שורת OPTIONS עם | בתוך הבלוק (עצירת MISSING:format) הייתה
    # מזהמת את seats. כל marker שהודבק אחרי המושבים נחתך: CARD_REQUIRED לפי חוזה
    # ה-recon, ו-MISSING/FAILED/BOOKED כהגנה על agent שערבב markers בשורה אחת
    # (נצפה חי באיטרציה 1: "... | שורה 5 מושבים 7,8 / MISSING:last_name").
    # מסעדות לא פולטות | בשורת הסיום — נשאר ריק, אין רגרסיה.
    # "|" ב-strip: נצפה חי — agent שכתב "... מושבים 7,8 | CARD_REQUIRED" השאיר
    # פייפ יתום בקצה אחרי חיתוך ה-marker ("שורה 5 מושבים 7,8 |").
    # בביטוח אותו payload נושא את הפרמיה — חוזר גם בשם 'extra' (שם השדה של ורטיקל
    # הביטוח; ערך אחד בשני שמות — איחוד השם נדחה לריצה חיה על ליבה משותפת).
    seats = ""
    sr_line = next((ln for ln in lines[-3:] if "SUMMARY_REACHED" in ln), "")
    if "|" in sr_line:
        seats = re.split(r"CARD_REQUIRED|MISSING:|FAILED:|BOOKED", sr_line.split("|", 1)[1])[0]
        seats = seats.strip(" :–-./|")[:120]
    extra = seats
    # PERK: פרטים ששווים ללקוח (הנחה/מבצע/מגבלה) שה-agent ראה בדף — עוברים להודעה.
    perks = [
        ln.split("PERK:", 1)[1].strip().replace("[", "").replace("]", "")[:120]
        for ln in final.splitlines()
        if "PERK:" in ln
    ]
    perk = " · ".join(p for p in perks if p)[:200]
    # OPTIONS: האפשרויות האמיתיות מהדף כשעוצרים על בחירה כפויה (MISSING) — גבר
    # מציג אותן ללקוח כרשימת בחירה במקום שאלה גנרית ("בפנים/בחוץ/בר").
    # שתי צורות: "OPTIONS: א | ב" (legacy, שדה בודד) ו-"OPTIONS <key>: א | ב"
    # (ממופתח, ה-MISSING המרובה של הביטוח). FIELD <key>: <תווית> — התווית מהדף,
    # כדי שגבר ישאל בעברית אמיתית גם על שדה שלא צפינו.
    options: list = []
    options_by_field: dict = {}
    field_labels: dict = {}
    for ln in final.splitlines():
        s = ln.strip()
        m = re.match(r"OPTIONS(?:[ \t]+([a-z0-9_]+))?:", s)
        if m:
            # cap 15: דף היעד של פספורטכארד הציג 11 מדינות ו-cap 10 חתך את גרמניה (ריצה חיה 1)
            vals = [o.strip()[:60] for o in s.split(":", 1)[1].split("|") if o.strip()][:15]
            if m.group(1):
                options_by_field[m.group(1)] = vals
            else:
                options = vals  # legacy — אותה סמנטיקה בדיוק (האחרון מנצח)
            continue
        m = re.match(r"FIELD[ \t]+([a-z0-9_]+):", s)
        if m:
            label = s.split(":", 1)[1].strip()
            # ה-agent לפעמים מדביק FIELD ו-OPTIONS באותה שורה ("מגדר · OPTIONS p1_gender:
            # זכר|נקבה" — נצפה חי בריצת ביטוח 2): מפרידים, כדי שהאופציות ייקלטו
            # ב-options_by_field והתווית תישאר נקייה.
            om = re.search(r"OPTIONS[ \t]+([a-z0-9_]+):", label)
            if om:
                tail_vals = label[om.end() :].split("|")
                options_by_field[om.group(1)] = [o.strip()[:60] for o in tail_vals if o.strip()][
                    :15
                ]
                label = label[: om.start()].strip(" ·-–")
            field_labels[m.group(1)] = label[:80]
    # גישור: agent שהשתמש בצורה הממופתחת על שדה בודד — לא שוברים את המסלול הישן
    if not options and len(missing_fields) == 1 and missing_fields[0] in options_by_field:
        options = options_by_field[missing_fields[0]]
    # AGREED: תמצית הצ'קבוקסים שה-agent אישר בשם הלקוח — גבר מגלה אותם בהודעת
    # הסיום ("אישרתי בשמך: תקנון...") במקום שהסכמות ייחתמו בשקט (בקשת אלון 15.7).
    agreed: list = []
    for ln in final.splitlines():
        if ln.strip().startswith("AGREED:"):
            agreed = [
                a.strip().replace("[", "").replace("]", "")[:80]
                for a in ln.split("AGREED:", 1)[1].split("|")
                if a.strip()
            ]
    agreed = agreed[:5]
    # URL: הכתובת שבה הדפדפן עצר (קיר כרטיס) — לינק שמחזיר את הלקוח לנקודת העצירה.
    page_now = ""
    for ln in final.splitlines():
        if ln.strip().startswith("URL:"):
            page_now = ln.strip()[4:].strip()[:300]
    if commit:
        # BOOKED <אישור> = נסגר באמת; כרטיס/שדה-חסר/כישלון גוברים — לא נרשמת הזמנה.
        booked = "BOOKED" in strict_last and not card and not missing and not failed
        confirmation = ""
        if booked:
            confirmation = strict_last[strict_last.find("BOOKED") + len("BOOKED") :].strip(" :–-")[
                :120
            ]
        return {
            "success": booked,
            "stage": final[:400],
            "card_required": card,
            "booked": booked,
            "confirmation": confirmation,
            "missing": missing,
            "missing_fields": missing_fields,
            "failed": failed,
            "time": chosen_time,
            "seats": seats,
            "extra": extra,
            "perk": perk,
            "options": options,
            "options_by_field": options_by_field,
            "field_labels": field_labels,
            "agreed": agreed,
            "page_now": page_now,
            "message": final,
        }
    # recon (dry_run): הצלחה = הגענו למסך הסיכום (SUMMARY_REACHED), בלי סגירה אמיתית.
    summary_reached = "SUMMARY_REACHED" in last
    return {
        "success": summary_reached and not missing and not failed,
        "stage": final[:400],
        "card_required": card,
        "booked": False,
        "confirmation": "",
        "summary_reached": summary_reached,
        "missing": missing,
        "missing_fields": missing_fields,
        "failed": failed,
        "time": chosen_time,
        "seats": seats,
        "extra": extra,
        "perk": perk,
        "options": options,
        "options_by_field": options_by_field,
        "field_labels": field_labels,
        "agreed": agreed,
        "page_now": page_now,
        "message": final,
    }


def _il_tz_hook(resume: bool):
    """on_step_start: שעון ישראל לדפדפן (Emulation.setTimezoneOverride).

    הדפדפן של Browserbase רץ בחו"ל, ואתר שמרנדר שעות ב-JS לפי שעון הדפדפן מציג
    ללקוח שעה אמריקאית — נצפה חי בסבב 3 של ההופעות (לאן, אירוע 5237): הדף הציג
    "13:00" (17:00 UTC בשעון החוף המזרחי) למופע שמתחיל 20:00 שעון ישראל.
    ההזרקה רצה בכל צעד (idempotent, מילישניות) כי טאבים/targets מתחלפים; reload
    חד-פעמי כי הדף הראשון כבר רונדר לפני ה-override — אבל לא ב-resume, שם המסך
    החי מחזיק בחירות (מושבים) ש-reload היה מוחק."""
    state = {"first": True}

    async def hook(agent) -> None:
        try:
            s = await agent.browser_session.get_or_create_cdp_session()
            await s.cdp_client.send.Emulation.setTimezoneOverride(
                params={"timezoneId": "Asia/Jerusalem"}, session_id=s.session_id
            )
            if state["first"] and not resume:
                await s.cdp_client.send.Page.reload(session_id=s.session_id)
            state["first"] = False
        except Exception:  # noqa: BLE001 — best-effort: שעה שגויה עדיפה על ריצה שמתה
            pass

    return hook


PAGE_READY_TIMEOUT_S = 3.0  # במקום 8 הקשיחות של browser-use — SPA לא יורה 'load' לעולם


def _shorten_page_readiness() -> None:
    """מקצר את ה-Page readiness timeout של הניווט מ-8 שנ' ל-3.

    ב-browser-use 0.13.1 אין פרמטר BrowserProfile לזה: הערך קשיח בתוך
    BrowserSession._navigate_and_wait‏ (‏timeout = 3.0 if same_domain else 8.0‏),
    ואף dispatch של NavigateToUrlEvent לא מעביר timeout_ms. אתרי SPA‏ (Ontopo וכו')
    לא יורים אירוע 'load' אף פעם, אז כמעט כל ניווט שרף את מלוא 8 השניות (44 מופעים
    ב-40 ריצות — דוח הזירוז 17.7). עטיפה דקה על המתודה: timeout שלא הועבר במפורש
    → 3 שנ'; timeout מפורש מכובד כרגיל. הסיכון נמוך: מסך שנקרא רגע מוקדם מדי נקרא
    שוב בצעד הבא — ה-agent ממילא מאמת חזותית."""
    from browser_use.browser.session import BrowserSession

    orig = BrowserSession._navigate_and_wait

    async def _fast(self, url, target_id, timeout=None, wait_until="load", nav_timeout=None):
        if timeout is None:
            timeout = PAGE_READY_TIMEOUT_S
        return await orig(
            self, url, target_id, timeout=timeout, wait_until=wait_until, nav_timeout=nav_timeout
        )

    BrowserSession._navigate_and_wait = _fast


def _profile_kwargs(job: dict) -> dict:
    # cross_origin_iframes: מפות מושבים של הופעות חיות ב-OOPIF (seatmap.vivenu.com
    # בתוך לאן — QA חי 18-19.7). ב-0.13.1 זו כבר ברירת המחדל (True) — מקובע מפורשות
    # כדי ששדרוג עתידי של browser-use לא יכבה אותו בשקט ויפיל את הוורטיקל.
    kwargs: dict = {"headless": job.get("headless", True), "cross_origin_iframes": True}
    if job.get("cdp_url"):  # browserbase / remote — stealth+captcha חיים שם
        kwargs["cdp_url"] = job["cdp_url"]
        # ה-keepAlive של Browserbase מגן רק מפני *ניתוק*; בלעדי keep_alive כאן
        # browser-use סוגר את הדפדפן בסוף הריצה (Browser.close) והורג את הסשן
        # שה-pause-resume צריך חי. השחרור בפועל: release_session בכל נתיב סיום + sweeper.
        kwargs["keep_alive"] = True
    elif job.get("chrome_path"):  # local dev — בלי keep_alive, שלא יישאר Chrome תלוי
        kwargs["executable_path"] = job["chrome_path"]
    if job.get("record_dir"):
        kwargs["record_video_dir"] = job["record_dir"]
    return kwargs


def _make_llm(model: str):
    """ספק לפי קידומת שם המודל: bu-→ChatBrowserUse, claude-→ChatAnthropic,
    gpt-→ChatOpenAI, אחרת ChatGoogle (ברירת המחדל של היום — אפס שינוי למסלול הקיים).
    המפתחות מגיעים מה-env של ה-subprocess (browser_book מעביר אותם מ-settings):
    GOOGLE_API_KEY / ANTHROPIC_API_KEY / OPENAI_API_KEY / BROWSER_USE_API_KEY."""
    from browser_use.llm import ChatAnthropic, ChatBrowserUse, ChatGoogle, ChatOpenAI

    if model.startswith("bu-"):
        return ChatBrowserUse(model=model)
    if model.startswith("claude-"):
        return ChatAnthropic(model=model)
    if model.startswith("gpt-"):
        return ChatOpenAI(model=model)
    return ChatGoogle(model=model)


async def _run(job: dict) -> dict:
    from browser_use import Agent, BrowserProfile

    _shorten_page_readiness()
    key = os.getenv("GEMINI_API_KEY") or os.getenv("GOOGLE_API_KEY")
    if key:
        os.environ["GOOGLE_API_KEY"] = key
        os.environ["GEMINI_API_KEY"] = key

    rec = job.get("record_dir")
    if rec:
        os.makedirs(rec, exist_ok=True)

    profile = BrowserProfile(**_profile_kwargs(job))
    # בלי fallback מקודד — המודל מגיע תמיד מה-job (settings.model_name); שם מיושן
    # כאן היה נשלף בשקט בדיבוג ידני ומסתיר את הקונפיגורציה האמיתית.
    llm = _make_llm(job["model"])
    agent_kwargs: dict = {
        "task": _build_task(job),
        "llm": llm,
        "browser_profile": profile,
        # דגלי מהירות — A/B חי מול Ontopo (poc/spike_speed.py, 15.7.26): ‎~34%‎ פחות זמן
        # ריצה, זמן-לצעד ירד מ-24-35 שנ' ל-17.5 שנ', אותה איכות עצירה (MISSING נכון,
        # פרטים נכונים, בלי לופים, PERK עדיין מדווח).
        # flash_mode: משמיט thinking/evaluation_previous_goal/next_goal/planning מסכמת
        # הפלט של כל צעד (prompt מערכת קצר יותר) — פחות טוקנים = צעד מהיר יותר.
        "flash_mode": True,
        # use_judge=False: מדלג על קריאת LLM-שופט נוספת בסוף הריצה — ה-markers של
        # _parse_result הם השופט שלנו ממילא.
        "use_judge": False,
    }
    if job.get("resume"):
        # ברירת המחדל של browser-use מנווטת אוטומטית לכל URL שמופיע ב-task —
        # ב-resume זה היה הורס את המסך הקפוא. חובה לכבות.
        agent_kwargs["directly_open_url"] = False
    if rec:  # הקלטה: GIF + הנמקת ה-agent צעד-צעד, לניתוח אחר כך
        agent_kwargs["generate_gif"] = os.path.join(rec, "run.gif")
        agent_kwargs["save_conversation_path"] = os.path.join(rec, "conversation")

    agent = Agent(**agent_kwargs)
    history = await agent.run(
        max_steps=job.get("max_steps", 40),
        on_step_start=_il_tz_hook(bool(job.get("resume"))),
    )
    final = (history.final_result() or "").strip()
    return _parse_result(final, commit=not job.get("dry_run", True))


def main() -> None:
    job = json.load(sys.stdin)
    try:
        result = asyncio.run(_run(job))
    except Exception as e:  # noqa: BLE001 — כל כשל הופך לתוצאה כנה, לא ל-traceback אילם
        result = {
            "success": False,
            "stage": "error",
            "card_required": False,
            "message": f"{type(e).__name__}: {e}",
        }
    with open(job["result_path"], "w", encoding="utf-8") as f:
        json.dump(result, f, ensure_ascii=False)


if __name__ == "__main__":
    main()
