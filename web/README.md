# web — דף נחיתה של גבר

`index.html` הוא דף נחיתה סטטי, עצמאי, ללא build (תלות חיצונית יחידה: Google Fonts).

**Deploy:** העלה את `index.html` לכל static host — Coolify (Static / Nixpacks ללא build), Vercel/Netlify (drag-and-drop של התיקייה), GitHub Pages, או S3/CDN. אין שלב build ואין dependencies להתקין.

**TODO לפני launch:**
- החלף `WHATSAPP_NUMBER` בכל ה-`wa.me/` בקישורים (4 מופעים) במספר הבינלאומי האמיתי (בלי `+`, בלי רווחים, למשל `9725XXXXXXXX`).
- החלף את ה-social proof (סקשן "מה גבר סגר השבוע" + ה-trust bar) בנתוני אמת ברגע שיש מדידה — כרגע placeholder מטושטש, בלי מספרים מומצאים.
- חבר את כפתורי הוייטליסט (`#waitlist`) לטופס/CRM אמיתי.
- ודא שהתמחור (₪0 / ₪29 / ₪79, מכסות 5 / 30 / ללא הגבלה) תואם להחלטה הסופית.
