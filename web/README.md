# web — דף נחיתה של גבר

`index.html` הוא דף נחיתה סטטי, עצמאי, ללא build (תלות חיצונית יחידה: Google Fonts).

**Deploy:** העלה את `index.html` לכל static host — Coolify (Static / Nixpacks ללא build), Vercel/Netlify (drag-and-drop של התיקייה), GitHub Pages, או S3/CDN. אין שלב build ואין dependencies להתקין.

**TODO לפני launch:**
- החלף `WHATSAPP_NUMBER` בכל ה-`wa.me/` בקישורים (3 מופעים) במספר הבינלאומי האמיתי (בלי `+`, בלי רווחים, למשל `9725XXXXXXXX`).
- חבר את כפתורי הוייטליסט (`#waitlist`) לטופס/CRM אמיתי — כרגע כל ה-CTA רק גוללים לסקשן הסופי, אין לכידת מייל.
- ודא שהתמחור (₪0 / ₪29, מכסות 5 / ללא הגבלה) תואם להחלטה הסופית. הגרסה הקודמת כללה מסלול שלישי (פרו ₪79) — הוסר בעיצוב המשודרג.
- הדמו בהירו הוא תרחיש מומחז (M25 בשוק הכרמל) — לא נתון אמת, אלא המחשה.
