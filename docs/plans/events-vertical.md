# ורטיקל הופעות (`task_type="events"`) — פלטפורמה, עיצוב, סטטוס

שכפול מינימלי של תקדים הקולנוע: שלישיית resolve+task+שדות, DRY_RUN בלבד.
מומש במלואו ב-branch `events-vertical` — 7 קומיטים מקומיים, בלי push, בלי אף
ריצת Browserbase/browser-use.

## הפלטפורמה שנבחרה ולמה

- **ראשית: לאן (leaan.co.il)** — `/events/<slug>/<id>`, זרימת רכישה מלאה בעמוד
  האירוע, נגיש מ-IP זר.
- **גיבוי: קופת תל אביב (kupat.co.il / tickets.kupat.co.il)** — `/show/<slug>`.
- **נימוק:** איוונטים (eventim.co.il) היא אמנם בעלת הכיסוי הגדול בישראל (כולל
  60% מזאפה), אבל אומת ישירות (15.07.26) שהאתר מחזיר 403 Forbidden לכל IP
  לא-ישראלי (דף חסימה בסגנון Akamai) — כלומר חסימת אוטומציה/גיאו ידועה מול
  Browserbase. נדחתה לשלב-ב (פרוקסי IL).

## העיצוב

אותו תקדים כמו קולנוע — שלושה מגעים בלבד, אפס שינוי במנגנונים המשותפים:

1. **resolve** — `_EVENT_PLATFORMS` (לאן ראשית, קופת גיבוי) + `resolve_event_url`;
   חיתוך זנבות כותרת בלבד — התאריך וההיכל נשארים בכוונה (many עם מועדים
   אמיתיים = רשימת הבחירה של הלקוח).
2. **task** — `_build_concert_task`: תאריך=אירוע בדיד (MISSING:date+OPTIONS),
   קטגוריית מחיר (MISSING:price_category עם מחירים), מושבים כמו קולנוע + מפה
   היררכית, ת"ז (MISSING:id_number), FAILED:sold_out / no_event_in_city.
   המחיר חי במקטע ה-| — אפס שינוי ב-`_parse_result`.
3. **שדות pipeline** — artist/venue ב-extract/schema (בלי time; date לא עוצר
   שיחה), קיר-כרטיס נוקב בסכום לפני התשלום + לינק עטוף + רמז דחיפות.

## מה מומש (7 קומיטים, לפי סדר)

- **שלב 0 — מיזוג** (`a5a3763`): `cinema-vertical` מוזג; 4 קונפליקטים נפתרו
  לשימור שני הצדדים — `resolve_reservation_url` עובר דרך `_pick` המשותף וממשיך
  ל-fallbackים של Phase 4-lite (מסעדות בלבד) בענף none; קיר-הכרטיס בקולנוע
  אימץ את `live_link.wrap` של main (הטסט עודכן ללינק הממותג); `run_commit`
  שומר heartbeat + פרמטרי קולנוע; טסטים אדיטיביים משני הצדדים
  (+`_fake_search_list` לחוזה).
- **resolve** (`b6f1f66`): `_EVENT_PLATFORMS` + `resolve_event_url` ב-
  `app/automation/resolve.py`, כולל regex-ים לחיתוך זנבות של לאן וקופת.
- **task** (`fd2a948`): `_build_concert_task` ב-`app/automation/bu_runner.py`.
- **book** (`1d339a6`): artist/venue ב-`book_table_bu` + תקרת 900s גם להופעות.
- **pipeline** (`dc59cd2`): extract/schema, routing ל-`resolve_event_url`,
  קיר-כרטיס עם מחיר, `_human` ל-date/price_category/id_number, `_failure_reply`
  עם sold_out / no_event_in_city (venue כמיקום), `_pending_commit` נושא
  artist/venue; +419 שורות טסטים ב-`tests/test_events_pipeline.py`.
- **ספייק** (`0cadbdf`): `poc/spike_events.py` — DRY_RUN קשיח, ברירת מחדל בלי
  תאריך (מתרגל MISSING:date+OPTIONS על קובי פרץ, שני מועדים בלאן נכון ל-15.07.26).
- **ניקיון** (`1214cea`): הסרת סימלינקי venv שנכנסו במיזוג + gitignore.

## ריצות חיות — סבב 1 (15.07.26, לאן, קובי פרץ, DRY_RUN)

- **ריצה 1 (בלי תאריך): כשל תשתית, לא כשל עיצוב.** סשן ה-Browserbase מת באמצע —
  עמוד לאן (SPA כבד) לא הפיק DOM tree (CDP snapshot/dom_tree/ax_tree ב-timeout
  בכל צעד 1–5, ה-agent עבד עיוור מצילומי מסך), ה-websocket נפל (keepalive ping
  timeout 1011), reconnect קיבל HTTP 410 (הסשן נהרג בצד Browserbase), וה-agent
  נעצר אחרי 5 כשלים רצופים *בלי שורת סיום*. `_parse_result("")` החזיר תוצאה
  ריקה לגמרי — בלי failed, בלי message, בלי זכר שהדפדפן מת. לא הודלף סשן
  (waiting=False ⇒ release_session נקרא בתוך `book_table_bu`).
- **ריצה 2 (זהה): עצירה כנה ולגיטימית — בדיוק תרחיש ברירת המחדל שהספייק תוכנן
  לתרגל.** ה-agent סגר פופאפ דחייה + באנר עוגיות, זיהה שדף האירוע עצמו בלי
  כפתור קנייה, חיפש באתר, מצא בקטגוריית מוזיקה שני מועדים אמיתיים ועצר נכון:
  `MISSING:date` + `OPTIONS: "10/08/2026 היכל מנורה מבטחים, תל אביב |
  11/08/2026 היכל מנורה מבטחים, תל אביב"` (אומתו מול האתר). הסשן שוחרר
  במפורש (released 68dd8451-…).
- **ריצה 3 (עם תאריך 11.08, לדחוף לקטגוריות מחיר)** — שוגרה ועדיין באוויר בעת
  סגירת הסבב; הספייק משחרר את הסשן בסופו (הוכח בריצה 2). יומן: bu_recordings
  האחרון + פלט המשימה בסקרצ'פד. **עדכון (סבב 2):** אובחנה בדיעבד — נהרגה
  ב-SIGTERM בלי result JSON, ראו למטה.
- **תיקון שנגזר (קומיט `b969620`):** ב-`_parse_result`, דיווח סופי ריק ⇒
  `failed="browser_error"` במקום תוצאה ריקה-אילמת (success נשאר False בשני
  המסלולים); +טסט `test_empty_final_report_is_browser_error` ב-
  `tests/test_bu_runner.py` (ריק/רווחים ⇒ browser_error בשני מצבי commit;
  דיווח בלי markers נשאר בלי failed). טסטים ירוקים.
- **הערה:** קטגוריות מחיר עוד לא נצפו — העצירה הכנה שהושגה היא MISSING:date
  עם מועדים אמיתיים מהדף, לא MISSING:price_category.

## ריצות חיות — סבב 2 (16.07.26, לאן, קובי פרץ 11.08, DRY_RUN)

- **דיאגנוזת ריצה 3 מסבב 1 (`bu_recordings/1784123655313_570778`):** נהרגה
  ב-SIGTERM בלי result JSON. שורש הבעיה שנחשף ביומן: כלי ה-extract מצא טקסט
  תבנית נסתר "אזלו הכרטיסים" (תבניות i18n שקיימות ב-HTML של לאן — אומת
  ב-curl: 564 אזורי מפה עם is_soldout:false ו-48 סוגי כרטיסים 169/199/269/349
  ש"ח), וה-agent בזבז ~20 צעדים בלולאת אימותים עד שה-websocket מת.
- **המשמר החדש (קומיט `e60f9d0`, "ev2 iter 1"):** בולט "אזל-או-לא — רק לפי מה
  שמוצג בפועל" ב-`_build_concert_task` (`app/automation/bu_runner.py`): תבניות
  טקסט נסתרות כמו "אזלו הכרטיסים" לא קובעות sold_out; מפה אינטראקטיבית/כפתור
  רכישה ⇒ יש כרטיסים וממשיכים בלי אימותים חוזרים; FAILED:sold_out רק לפי
  הודעה מוצגת חזותית; קטגוריות ומחירים נקראים ממפת המושבים/המקרא (קליק על
  אזור) ולא מחיפוש טקסט בדף. +אסרטים על המשמר ("תבניות טקסט נסתרות" +
  "חזותית") ב-`test_concert_task_keeps_contract_and_event_rules`
  (`tests/test_bu_runner.py`). טסטים ירוקים. בלי push.
- **ריצה חיה (11.08, `bu_recordings/1784196678245_77231b`): עצירה כנה
  `FAILED:sold_out`, exit 0.** המשמר עבד — ה-agent לא נלכד בטקסט הנסתר, ניגש
  ישר למפת המושבים, ניסה לבחור, ועצר רק אחרי אישוש חזותי: הודעה מוצגת מהאמן
  שהמופעים נדחים עקב המצב הביטחוני + מפת מושבים שלא מאפשרת בחירה. הסשן
  שוחרר (session_id:null — release בתוך `book_table_bu` במסלול FAILED).
- **מסקנה:** המכירה לאירוע 5514 קפואה בפועל (ה-JSON המוטמע הוא seed ישן) —
  מצב עולם אמיתי, לא באג קוד. יעד הסבב (MISSING:price_category או
  CARD_REQUIRED) אינו בר-השגה על האירוע הזה כרגע; העצירה שהושגה היא terminal
  state לגיטימי שה-pipeline מתרגם ל-sold_out ללקוח.
- **המשך מומלץ לסבב הבא:** אירוע חי אחר בלאן (לא קובי פרץ — כל מופעיו נדחו)
  או מסלול הגיבוי קופת ת"א.

## ריצות חיות — סבב 3 (16.07.26, לאן+קופת, DRY_RUN)

- **בדיקת זמינות זולה לפני דפדפן (httpx):** לאן היא Next.js — `__NEXT_DATA__`
  בדף האירוע מכיל אובייקט אירוע מלא (`event_start`, `is_soldout`,
  `has_multiple_prices`, `prevent_checkout`, `sell_start/end`). ה-site-map
  (444 אירועים) + הסורק איתרו יעד חי: **כנר על הגג, היכל התרבות ת"א, 29/08/26
  (אירוע 5237)** — זמין, ריבוי קטגוריות מחיר, resolve מחזיר "one". קופת: דף
  ה-WP `/show/<slug>` מכיל תאריך/שעה/מיקום גלויים + לינק booking
  (`tickets.kupat.co.il/booking/features/<id>`).
- **שינוי ספייק (מינימלי):** ארגומנט 5 = notes — העדפת קטגוריית מחיר עוברת
  ל-task בדיוק כמו בצינור האמיתי (מתרגל את ריצת-ההמשך שאחרי MISSING).
- **ריצה 1 (כנר על הגג 29.08, בלי העדפה; `bu_recordings/1784199029868_11a85c`):
  עצירה כנה `MISSING:price_category` — הפעם הראשונה שהוורטיקל מגיע למסכי
  קטגוריות המחיר.** OPTIONS אמיתיים מהמקרא: "אולם 299-325 | אולם אמצע 249-325 |
  אולם אחורי 199-325 | יציע 139 | יציע אחורי 119 | גזוזטרה 119 ש"ח". הפרסר
  הרים את כל השש ל-options. הסשן שוחרר.
- **ריצה 2 (זהה + notes "יציע 139"; `bu_recordings/1784199240595_e78b23`):
  ההעדפה כובדה עד קיר-הזיהוי — עצירה כנה `MISSING:id_number`.** הקטגוריה
  נבחרה, מפה היררכית (גוש ביציע) נוהגה, מושבים צמודים נבחרו (גוש ג' שורה 25,
  37-38, סה"כ 278 ש"ח), PERK נתפס (הנחות 50-75 ש"ח לכרטיסי אשראי). ואז
  **ממצא מוצרי: בלאן קיר-זיהוי לפני קיר-התשלום** — ת"ז+טלפון להתחברות/הרשמה,
  ולכן CARD_REQUIRED מושג רק עם ת"ז אמיתית של הלקוח (מגיעה ב-resume בצינור
  האמיתי; בבדיקות לא ממציאים ת"ז — חוק הברזל). הסשן שוחרר.
- **ריצה 3 (היהודים 30.07 אמפי MAX ראשל"צ, קופת, notes "הזולה ביותר";
  `bu_recordings/1784199706460_a454b3`): מסלול הגיבוי אומת עד מסך הקטגוריות —
  עצירה כנה `MISSING:price_category` על שוויון.** resolve "one" →
  `/show/hayehudim` → ה-booking SPA של `tickets.kupat.co.il`; שתי קטגוריות
  באותו מחיר ("דשא 199 | טריבונה 199 ש"ח") ⇒ "הזולה ביותר" לא חד-משמעית,
  וה-agent עצר בדיוק לפי כלל אי-ההתלבטות. הסשן שוחרר.
- **הבאג שנחשף ותוקן (timezone): שעת מופע בשעון אמריקאי.** לאן מרנדרת את
  השעה ב-JS לפי שעון הדפדפן, והדפדפן של Browserbase רץ בחו"ל: הדף הציג
  "13:00" (= 17:00 UTC בשעון החוף המזרחי) למופע שה-JSON שלו אומר
  20:00 שעון ישראל. בלי תיקון הלקוח היה מקבל שעה שגויה בכמה שעות. אומת
  (16.07.26): ל-API של Browserbase (create-session) אין שדה timezone,
  ול-`BrowserProfile` של browser-use 0.13 אין tz — לכן התיקון ב-CDP:
  `_il_tz_hook` (`app/automation/bu_runner.py`) — `Emulation.setTimezoneOverride`
  ל-Asia/Jerusalem ב-on_step_start של כל צעד (idempotent) + reload חד-פעמי
  (הדף הראשון רונדר לפני ה-override), בלי reload ב-resume (מסך חי מחזיק
  בחירות). חל על כל הוורטיקלים. ‎+3 טסטים (`tests/test_bu_runner.py`) —
  ריצה טרייה/resume/כשל-CDP. **אימות חי של התיקון — בסבב הבא** (תקציב
  שלוש הריצות נוצל).
- **מסקנת הסבב:** יעד "מסכי קטגוריות המחיר" הושג בשתי הפלטפורמות עם OPTIONS
  אמיתיים; CARD_REQUIRED עדיין לא נצפה חי — בלאן הוא שמור מאחורי ת"ז, ובקופת
  הריצה נעצרה כדין על שוויון מחירים. לסבב הבא: ריצת קופת עם קטגוריה נקובה
  ("דשא") — הסיכוי הקרוב ביותר ל-CARD_REQUIRED; ולאן — ריצת resume עם ת"ז
  אמיתית של בעל הבטא. אגב היגיינה: בבדיקת הסשנים נצפו 4 סשני RUNNING זרים
  בפרויקט (של הפרוד החי, אחד פעיל) — לא נגענו.

## ריצות חיות — סבב 4 (17.07.26, קופת, DRY_RUN)

- **מטרת הסבב:** CARD_REQUIRED בקופת — "היהודים" 30.07 עם notes "דשא"
  (הקטגוריה הנקובה שסבב 3 המליץ עליה). פרטי קשר אמיתיים בספייק באישור אלון
  (שם/מייל/טלפון בלבד — ת"ז/תשלום לעולם לא).
- **ריצה 1 (00:18, `bu_recordings/1784236721466_9e1f80`): הדפדפן נרצח באמצע —
  בלי marker.** ה-agent פיצח את מפת ה-canvas (קליק קואורדינטות אחרי שה-legend
  לא הגיב), פתח את בורר הכמות של "דשא עמידה" (199 ₪), ובאמצע קליקי הכמות:
  הטאב נעלם ("No tabs remain"), ה-WS נסגר, ושלושת נסיונות ה-reconnect נדחו
  ב-HTTP 410 (הסשן עצמו מת). ה-agent נעצר אחרי 5 כשלים רצופים בלי done,
  והתוצאה חזרה **שקטה לגמרי**: בלי failed, בלי marker,
  stage="Waited for 5 seconds".
- **תחקיר מוות הסשן (Browserbase API): רצח, לא קריסה.** שלושה סשני RUNNING של
  הפרויקט הסתיימו בתוך 1.2 שניות (21:23:28.2 / 21:23:28.9 / 21:23:29.5Z), בסדר
  חדש→ישן — טביעת האצבע של `sweep_orphan_sessions()` (לולאה על רשימה
  newest-first). שרת עלה באמצע הריצה (redeploy של הפרוד או uvicorn מקומי)
  והנחת ה-sweep — "בעליית השרת כל סשן RUNNING הוא יתום" — שקרית כשריצה חיה
  רצה במקביל ממקום אחר על אותו פרויקט Browserbase. **לקח תפעולי מיידי: לא
  מרימים שרת בזמן ספייק חי.** תיקון אמיתי (תיוג userMetadata וסינון ה-sweep
  לפי בעלים) — מחוץ לתכולת הסבב, נרשם כמשימת המשך.
- **הבאג שנחשף ותוקן (56c80a0): מוות-דפדפן עם final לא-ריק היה שתיקה.** רשת
  ה-safety מ-ev iter 1 נדרכה רק על final ריק, אבל `history.final_result()`
  מהדהד את הפעולה האחרונה ("Waited for 5 seconds") כשה-agent נקטע בלי done —
  לא-ריק, בלי marker, ושום דבר לא סימן שהדפדפן מת. מעכשיו ב-`_parse_result`:
  אין marker בשורה האחרונה ⇒ `FAILED:browser_error`, תמיד. הטסט עודכן עם
  המחרוזת שנצפתה חי (`test_markerless_final_report_is_browser_error`).
- **ריצה 2 (01:33, `bu_recordings/1784241221868_fe2642`): עצירה כנה
  `FAILED:login_required` — ממצא מוצרי מבני: קופת נועלת את ה-checkout מאחורי
  אימות SMS.** הבחירה הצליחה במלואה — 2× דשא עמידה, סה"כ 398 ₪, אמפי MAX
  ראשל"צ 30.07 — ואחרי "לרכישה" קפץ מודאל אימות טלפון: האתר שלח קוד SMS
  למספר שהוזן, ובלי הקוד אין מסך פרטים/סיכום. ⇒ **CARD_REQUIRED אינו בר-השגה
  אוטונומית בקופת**; הדרך: הלקוח-בלולאה — גבר מבקש את הקוד בוואטסאפ ועושה
  resume, או מסירת Live View. (מקביל לקיר-הת"ז של לאן מסבב 3: לכל קופה
  ישראלית קיר-זיהוי משלה לפני קיר-התשלום.)
- **טיימר החזקת מושבים: לא נצפה.** אף state בשתי הריצות לא הכיל ספירה
  לאחור/טקסט החזקה עד קיר-ה-OTP — הטיימר של קופת מתחיל (או מוצג) כנראה רק
  אחרי האימות. שאלת ה-TTL מול פקיעת המושבים נשארת פתוחה לריצה שתעבור את הקיר.
- **אימות חי של tz hook מסבב 3 — עבר.** שתי הריצות הציגו
  "יום חמישי | 30 יולי | 20:30" — שעון ישראל, יום-שבוע נכון (30.07.26 = חמישי),
  בלי ארטיפקט שעון-אמריקאי כמו ה-"13:00" של סבב 3.
- **היגיינת סשנים:** סשן ריצה 1 נרצח ב-sweep; סשן ריצה 2 שוחרר במסלול FAILED
  (`session_id:null`); אומת מול ה-API בסוף הסבב — 0 סשני RUNNING בפרויקט.

## ריצות חיות — סבב 5 (17.07.26, קופת, DRY_RUN) — אימות צנרת לקוח-בלולאה

- **מטרת הסבב:** לאמת בצד הדפדפן את חוזה הליבה של feature/customer-in-loop
  (21e2ded, לא מוזג): קיר ה-OTP של קופת ⇒ עצירת `MISSING:sms_code` (לא
  `FAILED:login_required` כמו בסבב 4), סשן ממתין, ו-resume שמזין את הקוד
  באותו מסך.
- **השינוי (קומיט זה):** הוראת קיר-קוד ב-`_build_concert_task` — קוד אימות
  שנשלח ללקוח ⇒ עצור `MISSING:sms_code`, במפורש לא `FAILED:login_required`
  גם כשהקיר חלק ממסך התחברות; קוד שנדחה ⇒ `MISSING:sms_code` שוב, בלי לנחש.
  `browser_book` כבר משאיר סשן חי על missing (waiting) — אפס שינוי שם. טסט
  עוגן: `test_concert_task_sms_code_wall_is_missing_not_failed`.
- **ריצה 1 (04:20, `bu_recordings/1784251243611_5f5296`): עצירה כנה
  `MISSING:sms_code` עם סשן ממתין — ההוראה החדשה עבדה על הקיר האמיתי.**
  היהודים 30.07, notes "דשא"; resolve "one" → `kupat.co.il/show/hayehudim`;
  2× דשא עמידה, 398 ₪, אמפי MAX ראשל"צ. אחרי הזנת הטלפון האתר שלח SMS אמיתי
  ל-0544820137 וביקש קוד — ה-agent עצר בדיוק לפי ההוראה (בסבב 4 אותו קיר
  נגמר `FAILED:login_required`). `session_id` חזר ב-details (`c9b276ee`),
  הסשן לא שוחרר בכוונה. אגב: challenge של Cloudflare באמצע עבר לבד (stealth).
- **ריצה 2 (04:27, resume, `bu_recordings/1784251597104_d90695`): הקוד הוזן,
  האתר דחה, והדיווח נשאר כן — `MISSING:sms_code` שוב, לא BOOKED ולא המצאה.**
  אותו `session_id`, notes בדיוק בפורמט שהליבה מעבירה — `"דשא; sms_code: 000000"`.
  ראיה ביומן: `evaluate` על `input[autocomplete="one-time-code"]` מילא
  "000000", והמסך הציג "הקוד אינו נכון". pause-resume חי: המסך שרד ~2.5 דקות
  בין הריצות, וקיר ה-OTP מהריצה הראשונה עוד המתין.
- **ממצא טכני לריצת קוד אמיתי:** שדות ה-OTP של קופת הם 6 קוביות
  `input[autocomplete="one-time-code"]`, אבל עץ הנגישות של browser-use חשף רק
  5 — ה-agent גישר עם JS ישיר על ה-selector. עם קוד אמיתי צפוי לעבוד באותו
  מסלול; נקודת שבירה מועמדת אם קופת ישנו מבנה.
- **טיימר החזקת מושבים: עדיין לא נצפה** — אף טקסט ספירה/החזקה בשני היומנים,
  גם אחרי דחיית הקוד. השאלה מסבב 4 נשארת פתוחה; כנראה מוצג רק אחרי אימות מוצלח.
- **היגיינת סשנים:** הסשן שוחרר בסוף ריצה 2; אומת מול ה-API — COMPLETED.
- **מסקנת הסבב:** צד הדפדפן של הלקוח-בלולאה מאומת מקצה לקצה על קיר אמיתי —
  עצירה-לשאלה, סשן ממתין, resume עם הקוד, הזנה בפועל, ודיווח כן על דחייה.
  נשאר ללקוח-בלולאה מלא: מיזוג feature/customer-in-loop + מבחן OTP אמיתי עם
  אלון דרך הצינור המלא.

## מה מחכה לריצות החיות

- ~~ריצת `poc/spike_events.py` ראשונה מול לאן (DRY_RUN)~~ — **בוצע** (סבבים
  1–2 לעיל): ה-task וה-OPTIONS של המועדים אומתו בדף אמיתי, וגם FAILED:sold_out
  כנה; קטגוריות מחיר עוד לא נצפו — דורש אירוע חי אחר (מופעי קובי פרץ נדחו).
- ~~ריצה מול קופת ת"א (גיבוי) — אימות זרימת `/show/<slug>`~~ — **בוצע** (סבב 3,
  ריצה 3): resolve→booking SPA→קטגוריות עובד. סבב 4 הראה ש-CARD_REQUIRED שם
  חסום מאחורי אימות SMS — דורש לקוח-בלולאה (קוד בוואטסאפ + resume), לא עוד
  ריצה אוטונומית. צד הדפדפן של הלולאה אומת בסבב 5; נשאר קוד אמיתי דרך
  הצינור המלא (אחרי מיזוג feature/customer-in-loop).
- **sweeper / פקיעת לינק קיר-הכרטיס** מול טיימר החזקת המושבים של הקופות —
  הלינק הממותג חי TTL_S=1800, אבל המושבים משוחררים הרבה קודם. ההתנהגות
  האמיתית (כמה דקות מחזיקים? מה קורה בפקיעה?) תתברר רק בריצה חיה; אז נחליט
  אם צריך הודעת תזכורת/פקיעה.

## שלב-ב (מחוץ לתכולה הנוכחית)

- **איוונטים (eventim.co.il)** דרך פרוקסי IL של Browserbase — כיום 403 Akamai
  ל-IP לא-ישראלי (נבדק 15.07.26). הפלטפורמה הגדולה בארץ; שווה ורטיקל-משנה.
- **Tickchak LIVE** כשכבת discovery — אינדקס מופעים רוחבי כשה-resolve הרגיל
  מחזיר none.
