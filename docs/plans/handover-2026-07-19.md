# Handover — 19.07.2026 בוקר (סוף סשן charming-banzai / adoring-curie)

מסמך העברה לסשן חדש. **קרא קודם:** `CLAUDE.md` (שיטת עבודה+סטאק) → המסמך הזה →
`docs/plans/merge-ledger.md` (יומן מלא של כל הסבבים, עודכן לבוקר 19.7). קבצי הזיכרון
ב-`~/.claude/projects/-Users-alonb-Desktop-GeverAI/memory/` נטענים אוטומטית לכל סשן
ועודכנו — הם התקציר; הלדג'ר הוא הפירוט.

## מצב עכשיו (19.7 בוקר)

- **פרוד:** `https://geverai.duckdns.org` — main == origin/main == `48c47da`, בריא.
  deploy = push ל-main → Coolify מגלגל לבד. **DRY_RUN=true** (החלטת אלון עד יציבות).
- **בטא סגורה חיה:** 4 חברים + אלון (0542773331) על מספר הטסט של Meta
  (+1-555-671-8899, allow-list עד 5). פידבקים טובים; אלון עושה לייב-טסט אינטנסיבי
  בלילות ושולח צילומי מסך — כל באג מטופל באותו לילה.
- **ורטיקלים חיים:** מסעדות (אונטופו/טאביט דו-שלבי) · קולנוע (פלאנט/רב-חן/סינמה
  סיטי/**הוט**) · הופעות (לאן/קופת) · ביטוח נסיעות (PassportCard) · **המלצות**
  (Gemini Maps grounding, $0 בבטא, "עוד" מהבאפר) · **הודעות קוליות** (תמלול Gemini)
  · סטיקר חגיגה אחרי סגירה · זמינות-תחילה (אין את המבוקש → מה כן) · אונבורדינג
  מרוכז בשיחה ראשונה · **שער גישה ACCESS_GATE — בקוד, כבוי**.
- **מודלים (לא לגעת בלי אלון):** שיחה `gemini-3.5-flash`, נווט
  `google/gemini-3-flash-preview` (מוזרק דרך env; app/config.py ברירות מחדל).

## המשימות שאלון הגדיר לסשן הבא

1. **סבב QA נוסף** — לפי השיטה: ריצה→תחקיר מעמיק→תיקונים נקודתיים→פוש→בדיקת
   התיקונים. רעיונות פתוחים: בדיקת עשן ל"עוד המלצות" (48c47da) ולסטיקר/קוליות
   בפרוד; רגל resume מלאה של ביטוח; ריצות קולנוע הוט עד קיר.
2. **אישור עסק במטא (Business Verification)** — אלון מתחיל; ללוות אותו
   (business.facebook.com → Security Center). לא חוסם בטא אך נדרש לשם התצוגה
   "גבר" ולהיקפים, ולפי הפורומים משחרר ביקורות תקועות.
3. **ביקורת ה-WABA** (המספר האמיתי) — ממתינים; נרשם שישי 18.7, צפי שני-שלישי.
   כשמאושר — ראה "מעבר למספר האמיתי" למטה.

## מעבר למספר האמיתי (כשביקורת Meta עוברת) — הצעדים המדויקים

- eSIM: `+972-53-621-5067` · Phone Number ID: `1152525501286629` · WABA:
  `2060429931265612`. המספר כבר רשום ב-WABA (סטטוס Pending review).
- כשה-Register נפתח: Register עם **PIN בן 6 ספרות ולשמור אותו** → Subscribe
  webhooks ל-app → החלפת `PHONE_NUMBER_ID` ב-Coolify env ל-1152525501286629 →
  ייתכן שצריך הרשאת asset ל-System User → הודעת בדיקה → תמונת פרופיל (חסר קובץ
  ריבועי "גבר." — להכין) → **לפני פתיחה לזרים:** להריץ `scripts/approve_all.py`
  (מאשר את כל הקיימים) → `ACCESS_GATE=true` + `INVITE_CODES=...` ב-Coolify →
  redeploy. מספר אמיתי לא מאומת = ללא allow-list, 250 שיחות-יזומות-עסק/יום,
  נכנסות ללא הגבלה.
- גבייה בהמשך: רגל Lemon Squeezy — webhook חתום מדליק `prefs.approved`; בלי
  סיסמאות (הזהות=מספר הוואטסאפ). עוצב, לא נבנה.

## שיטת העבודה עם אלון (חשוב — זה מה שעבד)

- **אלון מריץ את הפושים בעצמו.** אני מכין ענף בדוק + שער ירוק ונותן פקודה אחת:
  `cd ~/Desktop/GeverAI && git merge --ff-only <branch> && git push`. תמיד סיכום
  בצ'אט לפני. git merge/push שלי ל-main חסום ע"י ההרשאות.
- **הכל בסוכנים/worktrees:** כל משימה = worktree נפרד מ-origin/main + venv משלו
  (`python3 -m venv .venv && .venv/bin/pip install -q -e ".[dev]"`), סוכן אחד,
  שער `ruff check && ruff format --check && pytest` ירוק ×2 עם exit code אמיתי,
  ביקורת ponytail, קומיט בעברית. אינטגרציה: מיזוג לענף merge/roundN עם מעבר
  שורה-שורה מול ההורים (היו התנגשויות שקטות: _pick, שורת "כרטיסים לא נתמכים").
- **סוכני דפדפן נתקעים באמצע תור** ("אחכה להתראה") — לנסח מראש "הישאר בתוך
  התור, הרץ סינכרונית", ולהחזיר ב-SendMessage כשקורה.
- **ריצות דפדפן QA בטור בלבד** — מקביליות עם אותו owner=dev גרמה לפראטריסייד
  (סוכנים הרגו סשנים זה של זה). לא משחררים סשן RUNNING שלא יצרת.
- **BB_SESSION_OWNER=dev** בריצות מקומיות (פרוד מתויג prod; sweep מסנן לפי תג).
- **הקול החופשי — עקרון מוצר של אלון:** אפס ניסוחים קבועים למשתמש; הכל דרך
  `_say` (מפת INTENTS ב-pipeline.py) עם fallback pools בלבד. חריג מתועד יחיד:
  הודעת השער לזרים (הגנת עלות). ה-conftest מנטרל את המודל בטסטים.
- **עלויות:** תקרת ההוצאה ב-Gemini היא מנגנון השליטה של אלון
  (https://ai.studio/spend) — להכריז מראש על באצ'ים כבדים עם אומדן. ערכי env
  למסירה רק בבלוק קוד נקי (תקרית U+200F הפילה את הפרוד).
- אלון מעדיף עברית, תשובות תמציתיות-מובנות, טבלאות קצרות, ופרואקטיביות מלאה
  ("אני רוצה לקום לפיצ'ר מוכן"). כשהוא שולח צילום — לתחקר שורש, לא סימפטום.

## פערים פתוחים (מדורג)

1. **חלופת-סניף בעלות אפס** — "תפוס בפ"ת, פנוי בנמל ת"א": ה-resolver מכיר את כל
   הסניפים + availability API של אונטופו עונה בלי דפדפן. **ממתין לאישור אלון** —
   הוצע פעמיים, טרם הוכרע. (רקע: QA סבב 4 בלדג'ר.)
2. **הוט סינמה — מפת מושבים SVG**: browser-use מקפל תוכן SVG, אין אינדקסים;
   כיוון: קליק-קואורדינטות (getBoundingClientRect→pointer events) אחרי 2 כשלי
   selector. (אימות E2E לילה 2.)
3. **לאן — מושבים ב-iframe דומיין-זר (vivenu)**: חוק עקיפה קיים בפרומפט; פתרון
   מלא אולי בשדרוג browser-use 0.13.6 (זהירות: monkeypatch _shorten_page_readiness
   על session.py תלוי-גרסה!). + שלב-1 פנימי ללאן (אינדוקס חלקי ב-Brave).
4. **ביטוח:** מסלול SENSITIVE לת"ז בחבילה מרובה (חוב PII, form_answers
   ב-_pending_commit) · שאלת אישור-לקוח לצ'קבוקס תנאים לפני commit אמיתי.
5. קטנים: בדיקת שעה-שעה יקרה באונטופו (8 שעות=20 צעדים) · חוק standby לא מעוגן ·
   "תיאום טלפוני" בהודעת כשל (חלקית טופל — טלפון המקום כן נשלח) · רעש aclose ·
   ניטור פורמט maps.text (התנהגות נצפית, לא חוזה) · gpt-5.4-mini כמועמד עתידי
   לשיחה (זול בחצי; החלטת אלון: בינתיים לא).

## איפה מוצאים מה (הפניות)

- **`docs/plans/merge-ledger.md`** — יומן הסבבים המלא (לילה 1-2, סבבים 3-5, כל
  ממצאי הבטא, recon הוט, מחקר ההמלצות, QA ביטוח/הופעות, שער הגישה). המקור המרכזי.
- `docs/plans/beta-roadmap.md` — שלבי A-D · `docs/ops-coolify.md` — פרוד/deploy ·
  `docs/ops-tunnel.md` — ngrok לדב · `README.md` — סטטוס-על.
- זיכרון (נטען אוטומטית): gever-project (סטטוס+החלטות), gever-realbooking-livetest
  (כל באגי הלייב), gever-working-rules (שיטת העבודה+לקחי תפעול), gever-convmemory,
  gever-model-research-jul2026.
- קוד מפתח: `app/pipeline.py` (שיחה, INTENTS/_say, שער, אונבורדינג, המלצות,
  handle_voice) · `app/automation/bu_runner.py` (פרומפטי הנווט, _parse_result) ·
  `app/automation/resolve.py` (דו-שלבי, סינמה, הופעות, _with_city) ·
  `app/automation/browser_book.py` (subprocess, סשנים) · `app/llm/recommend.py` ·
  `app/llm/transcribe.py` · `app/db/memory.py` (prefs: _chat/_flow/approved) ·
  `scripts/approve_all.py` · `poc/persona_eval.py` · `poc/nav_bench.py` ·
  `poc/spike_events.py` / `spike_insurance.py` (זהירות: ממלא ת"ז-דמה) ·
  `assets/stickers/` + `scripts/make_stickers.py` · `web/index.html` (דף העסק).
- הקלטות ריצות: `bu_recordings/` בריפו + דשבורד Browserbase (פרוד מקליט שם).
- ענפים שנשארו בגיט (worktrees נוקו): rehearsal/cinema-merge וכל ענפי הסבבים —
  הכל כבר ב-main; אפשר למחוק ענפים בבטחה.

## עובדות תפעול שחוזרות

- Supabase דרך `app.config.settings` (service key ב-.env); users.prefs מחזיק
  _chat (שיחה, שורד redeploy), _flow (זרימה), approved, gender. מחיקת user
  חסומה ב-FK של bookings — איפוס = PATCH ל-name/email/prefs (גיבוי של אלון
  מ-19.7 נשמר ב-scratchpad של הסשן הישן — scratchpad הוא בן-חלוף, אל תסמוך).
- Meta test number: הודעות template בלבד ליזום; שיחת שירות נפתחת כשהלקוח כותב.
  webhook עם _is_stale (600ש') נגד שידורים חוזרים אחרי downtime.
- אלון: פלאפון 0542773331, מייל abazak@gmail.com — פרטי הבדיקה המאושרים לריצות
  (0544820137 כטלפון משני בספייקים). ת"ז/תשלום — לעולם לא.
- ה-`.venv-bu` (subprocess הנווט) חי בשורש הריפו הראשי; worktrees מפנים אליו
  דרך BU_VENV_PATH אם צריך ריצות חיות מ-worktree.
