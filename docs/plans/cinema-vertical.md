# ורטיקל קולנוע — סיכום עיצוב + סבבי live

מצב: **CARD_REQUIRED מלא בכל שלוש הרשתות** — פלאנט ראשל"צ (עצירה כנה על
`MISSING:last_name`), סינמה סיטי ראשל"צ (`SUMMARY_REACHED 19:50 | שורה 7
מושבים 10,11` + `CARD_REQUIRED`) ורב-חן גבעתיים (`SUMMARY_REACHED 20:00 |
שורה 8 מושבים 5,6` + `CARD_REQUIRED`), הכל ‏DRY_RUN‏. הקוד על branch
`cinema-vertical`, לא נדחף ולא מוזג.

## העיצוב שנבחר ולמה

עיקרון מנחה: **שכפול מינימלי של פלייבוק המסעדות, לא מערכת חדשה.** כל שכבה
קיבלה הרחבה נקודתית באותו חוזה בדיוק:

- **resolve** (`app/automation/resolve.py`): `_CINEMA_PLATFORMS` — פלאנט
  (כולל `yesplanet` legacy), רב-חן, סינמה סיטי — עם regex לדפי סרט בלבד
  (`/films/...`, `/movie/<id>`), כך שאין צורך בסינון listings.
  `resolve_cinema_url` מחזיר בדיוק את החוזה של `resolve_reservation_url`
  (`one|many|none` + fallback); `_pick` משותף לשני הוורטיקלים.
  שאילתת Brave: `"<סרט> כרטיסים קולנוע"`. רב-חן לא מאונדקסת ב-Brave —
  `chain="rav-hen"` גוזר את ה-URL מה-match של פלאנט ומאמת ב-GET
  (`_ravhen_from_planet`, ראו סבב רב-חן 1).
- **task** (`bu_runner._build_cinema_task`): אותם עקרונות — מטרה + חוקי ברזל,
  בלי שמות כפתורים, אותם markers בדיוק. ההרחבות היחידות:
  - בחירת סניף לפי עיר (`FAILED:no_cinema_in_city` אם אין).
  - חלון הקרנה ±90 דק' מהשעה המבוקשת (`FAILED:no_availability` מחוצה לו).
  - פורמט (2D/3D/IMAX/מדובב/כתוביות) = בחירה מהותית → `MISSING:format`
    + שורת `OPTIONS:` מהדף; חריג: אפשרות יחידה נלקחת.
  - מושבים = **ברירת המחדל המוצהרת היחידה** שבה ה-agent כן בוחר: צמודים
    באמצע האולם; אין צמודים / רק בתוספת תשלום → `MISSING:seats` + OPTIONS.
  - `SUMMARY_REACHED <שעה> | <מושבים>` — התוספת היחידה לפורמט שורת הסיום.
- **pipeline** (`app/pipeline.py`): `task_type=cinema` + `movie`/`city`
  בסכמת ה-extract, routing ב-`run_booking`, הודעות קיר-כרטיס/pending עם
  סרט+שעה+מושבים, `_failure_reply` מודע ל-task_type.
- **ספייק** (`poc/spike_cinema.py`): DRY_RUN קשיח (לא פרמטר), שחרור סשן
  Browserbase תמיד בסיום.

למה כך: החוזה (markers, resume, קיר-כרטיס, parse) כבר עבר את מסלול ה-live-QA
של המסעדות; כל סטייה ממנו הייתה קונה מחדש את כל הבאגים שכבר תוקנו שם.

## מהלך הסבבים

### iter 1 — ריצה חיה ראשונה (קומיט `607e10f`)

"חינה אמריקאית" בפלאנט ראשל"צ, 15.7 ~20:00, 2 כרטיסים. הסרט נבחר מראש מול
ה-API הציבורי של פלאנט כסרט שבאמת מוקרן (19:30/21:30, פורמט יחיד).

- resolve חד-משמעי מ-Brave (`planetcinema.co.il/films/american-hina/8256s2r`,
  fallback סינמה סיטי).
- ניווט נקי לחלוטין: **14 צעדים בלי אף לופ** — עוגיות → סניף → תאריך →
  הקרנת 19:30 (בטווח) → "הזמינו כאורח" → 2 פופאפים → מפת מושבים SVG
  (שורה 5, מושבים 7-8, דרך evaluate אחרי קליק ישיר שנכשל פעם אחת) →
  2 כרטיסים רגילים (107 ש"ח) → טופס פרטים.
- עצירה כנה: פלאנט דורש שם משפחה, הלקוח נתן רק "אלון" → `MISSING:last_name`
  לגיטימי, עם סיכום תואם מציאות.
- **הבאג היחיד שנמצא:** ה-agent ערבב שני markers בשורה אחת
  (`SUMMARY_REACHED 19:30 | שורה 5 מושבים 7,8 / MISSING:last_name`) כי תבנית
  הדוגמה הפרידה אלטרנטיבות ב-"/", והזנב זיהם את `details.seats` — טקסט שהיה
  דולף להודעת הלקוח. **תוקן בשניים:**
  1. ה-task: "אחת בלבד מהצורות"; עצירת MISSING/FAILED = שורת סיום לבדה.
  2. `_parse_result`: `re.split` על כל marker
     (`CARD_REQUIRED|MISSING:|FAILED:|BOOKED`) חותך כל זנב אחרי המושבים,
     לא רק CARD_REQUIRED.
  + טסט רגרסיה `test_parse_result_mixed_markers_live_iter1_regression` על
  השורה המעורבת המדויקת. כל הטסטים ירוקים.

הקלטת הריצה: `bu_recordings/1784051108359_142af8/` (steps log, run.gif,
conversation). הסשן שוחרר מיד בסיום.

### סבב סינמה סיטי 1 — CARD_REQUIRED מלא (15.07.26)

"חינה אמריקאית" בסינמה סיטי ראשל"צ, 16.7 ~20:00, 2 כרטיסים, שם מלא "אלון ישראלי".
**עצירה כנה מלאה:** `SUMMARY_REACHED 19:50 | שורה 7 מושבים 10,11` + `CARD_REQUIRED`,
מחיר 109.60 ש"ח, `URL: tickets.cinema-city.co.il/order/827162` — אומת מול ה-API
הציבורי של סינמה סיטי (EventId 827162 = 16/07 19:50 בראשל"צ). הסשן שוחרר.
הקלטה: `bu_recordings/1784116013958_ec9f27/`.

מה נדרש כדי להגיע לזה:

1. **כיוון resolve לרשת** — "חינה אמריקאית" קיים גם בפלאנט, ופלאנט ראשונה בסדר
   התיעדוף → בלי steering הסרט תמיד נפתר לפלאנט. `resolve_cinema_url(movie,
   chain=...)` מסנן את הפלטפורמות; הספייק מקבל רשת כארג' 6. בנוסף כותרות
   סינמה סיטי ("<סרט> - סינמה סיטי") — שם האתר נחתך כדי שגרסאות
   (מדובב/לרוסית) יישארו מובחנות.
2. **לקח מפת מושבים (Titan):** המושבים הם `rect.seat-hit-area` מעל ציור המושב,
   ו-click()‎ תכנותי לא נקלט — רק רצף mousedown/mouseup/click עבד. ה-agent גילה
   את זה לבד אחרי ~8 צעדי ניסוי; הרמז נוסף ל-task (חוסך ~3 דק' לריצה).
3. שדה תעודת זהות בטופס: חובה לתשלום אך לא חוסם את המעבר לקיר-הכרטיס —
   הלקוח משלים אותו יחד עם הכרטיס ב-Live View. אין שינוי קוד.

**ממצא תשתית (לא באג שלנו):** ‎~30%‎ מסשני Browserbase היום קפאו/מתו שניות אחרי
ניווט (WS keepalive ping timeout → reconnect נדחה ב-HTTP 410). שוחזר גם ב-CDP
גולמי מינימלי וגם מול דף פלאנט שעבד אתמול נקי; לא תלוי ב-proxies /
solveCaptchas / blockAds / חסימת ה-YouTube embed (כולם נבדקו A/B). שתי הריצות
הראשונות של הסבב נפלו על זה; השלישית עברה נקי מקצה לקצה. אם זה חוזר —
לבדוק סטטוס Browserbase לפני שחופרים בקוד.

### סבב ליטוש (קומיטים `e5deca0` + `b058230`, 15.07.26)

מה שנסגר אחרי הריצה החיה, בלי push:

1. **זנב `|` במושבים** — הריצה האמיתית החזירה `שורה 5 מושבים 7,8 |`;
   `_parse_result` ב-`bu_runner.py` מוסיף `|` לסט ה-strip של seats, כך שהמחרוזת
   נחלצת נקייה גם עם `| CARD_REQUIRED` וגם עם פייפ יתום. רגרסיה:
   `test_parse_result_seats_trailing_pipe_live_regression`.
2. **הודעות ללקוח** (`app/pipeline.py`):
   - שני כשלי הקולנוע ב-`_failure_reply` (`no_availability`, `no_cinema_in_city`)
     היו מחרוזת בודדת → עברו ל-`_vary` עם 3 וריאנטים נושאי-עוגנים
     (שם הסרט/"הקרנה"; העיר/"רשת אחרת"), ניטרלי-מגדר, אימוג'י מהפלטה בלבד.
   - וריאנט קיר-הכרטיס השלישי אמר "התשלום" פעמיים — תוקן.
   - הודעת "סגור ✅" של commit קולנוע מדברת כרטיסים/הקרנה, לא סועדים/שולחן.
3. **טסטים**: קיר-כרטיס קולנוע דרך `live_view_url` מוכח בטסט; לולאות 12x נועלות
   את כל הווריאנטים; עוגני `_vary` בטסטים קדם-קיימים רוככו (שניים הבהבו על
   הווריאנט השלישי — נתפסו בשער הירוק, `e5deca0`).

`ruff` + `pytest` ירוקים על כל הסבב.

### סבב רב-חן 1 — CARD_REQUIRED מלא בריצה אחת (16.07.26)

"חינה אמריקאית" ברב חן גבעתיים, 17.7 בדיוק 20:00, 2 כרטיסים, "אלון ישראלי".
**עצירה כנה מלאה בריצה החיה הראשונה:** `SUMMARY_REACHED 20:00 | שורה 8 מושבים 5,6`
+ `CARD_REQUIRED`, מחיר 107.00 ש"ח, `URL: tickets5.rav-hen.co.il/order/284740` —
ההקרנה אומתה מראש מול ה-API הציבורי של רב-חן (tenant `10104`, בסיס
`/rh/data-api-service`; לרשת 3 סניפים בלבד: גבעתיים 1058, דיזנגוף 1071,
קרית אונו 1062). הסשן שוחרר. הקלטה: `bu_recordings/1784199228556_eae8fa/`
(27 צעדים).

**הבאג היחיד — resolve, תוקן לפני הריצה:** Brave בכלל לא מאנדקס את דפי הסרט של
רב-חן — לשאילתת סרט שרץ שם בפועל חזרו רק פלאנט וסינמה סיטי, ו-`chain="rav-hen"`
נפל ל-many עם מועמדים מרשתות אחרות. התיקון: `_ravhen_from_planet` ב-`resolve.py` —
רב-חן חולקת פלטפורמה ומזהי סרטים עם פלאנט, אז כש-chain=rav-hen ואין תוצאת rav-hen
מ-Brave, ה-URL נגזר מה-match של פלאנט (אותו נתיב `/films/<slug>/<id>s<id>r`) ומאומת
ב-GET בלי redirects: 200 = הסרט באמת מוקרן ברב-חן, 302 = לא (נבדק חי מול סרטי
planet-only). הגזירה רצה רק עם chain מפורש — בלי chain פלאנט ממילא מנצחת, אין GET
מיותר. 3 טסטים חדשים ב-`test_resolve.py` (גזירה, 302/כשל-רשת, אין-גזירה-בלי-chain).

הבדלים מהרשתות האחרות שנצפו בריצה:

1. **Cloudflare challenge במעבר לסאב-דומיין הכרטוס** (`tickets5.rav-hen.co.il`):
   הניסיון הראשון נתקע על challenge + "תקלה בהצגת נתוני האירוע"; refresh וניווט
   ישיר לא עזרו, ומה שעבד הוא חזרה לדף הסרט ולחיצה מחדש על ההקרנה (צעדים 6-13).
   ה-agent התאושש לבד בזכות ההנחיה הקיימת "המתן ~15 שניות לאימות" — אין שינוי קוד.
2. **מפת מושבים Titan כמו סינמה סיטי** (`seat-hit-area` + רצף mousedown/mouseup/
   click) — הרמז שנוסף בסבב הקודם עבד גם כאן. טוויסט: פופאפ "המושב כבר אינו זמין"
   על שורות 5-7 למרות שנראו פנויות (חוסר התאמה בין אינדקס פנימי לתצוגה); ה-agent
   גלש לשורה 8 ובחר צמודים באמצע — בדיוק לפי מדיניות ברירת המחדל.
3. טופס פרטים: שם פרטי+משפחה, אימייל+אימות אימייל, טלפון וצ'קבוקס תנאים — בלי
   שדה תעודת זהות (שיש בסינמה סיטי). "אלון ישראלי" עבר נקי עד קיר-הכרטיס.

`ruff check` + `ruff format --check` + `pytest` (176) ירוקים.

### חיווט chain — מהשיחה ועד ה-resolve (17.07.26)

הפער האחרון לפני merge: הרשת הגיעה רק דרך הספייק (ארג' 6) — עכשיו מחווטת
מקצה לקצה. קריטי לרב-חן, שה-resolve שלה תלוי ב-chain מפורש (`_ravhen_from_planet`).

- **extract** (`app/pipeline.py`): שדה `chain` בסכמה (enum `_CINEMA_CHAINS` =
  planet / rav-hen / cinema-city) + הנחיה ב-`_EXTRACT`: רק כשהלקוח נקב ברשת או
  בשם סניף שלה; לא נקב → משמיטים (רשת היא לא ניחוש); "רב חן גבעתיים" =
  chain **וגם** city.
- **run_booking**: `chain` נקרא מה-fields עם בלימה של ערך זר מהמודל (chain לא
  מוכר היה מרוקן את הפלטפורמות ב-resolve ומחזיר none מבלבל) → עובר
  ל-`resolve_cinema_url(name, chain=...)`. קאש `_resolved`: "תנסה ברב חן" על
  אותו סרט אחרי resolve לפלאנט — chain ששונה מהפלטפורמה שבקאש פוסל אותו.
- **טסטים** (3 חדשים, 179 ירוקים): chain מגיע ל-resolver ונבלם כשהוא זר; פסילת
  הקאש; וטסט חוזה — `_CINEMA_CHAINS` ⇔ מפתחות `_CINEMA_PLATFORMS` ⇔ ה-enum בסכמה.
- **extract חי (Gemini אמיתי)**: "ברב חן גבעתיים" → `chain=rav-hen` +
  `city=גבעתיים`; "בסינמה סיטי ראשון" → `cinema-city`; בלי רשת → chain ריק
  (לא הומצאה העדפה). שלוש/שלוש.
- **אימות חי (DRY_RUN)**: fields כמו-מה-extract עם `chain="rav-hen"` דרך
  `run_booking` האמיתי (וואטסאפ/Supabase מדומים; resolve + דפדפן אמיתיים) —
  resolve חזר one/rav-hen, והריצה נעצרה בכנות על קיר-הכרטיס:
  `SUMMARY_REACHED 20:00 | שורה 6 מושבים 5,6` + `CARD_REQUIRED`,
  `tickets5.rav-hen.co.il/order/284741` (ההקרנה אומתה מראש מול ה-API הציבורי:
  חינה אמריקאית, גבעתיים, 18.7 20:00 אולם 6; פרטי לקוח אמיתיים באישור אלון).
  הסשן שוחרר. הקלטה: `bu_recordings/1784240129614_46b571/`.

## סנכרון מול main

נקודת המיזוג האחרונה מ-main היא `152cb80` (כולל main עד `d2df6ea` — חלופות
שעה אמיתיות). מאז main התקדם בכ-10 קומיטים שעוד לא כאן: דף נחיתה מוכן-לבטא,
דפדפן-גבר (`/b/{token}` + מקלדת מובייל), תיקוני תחקיר 15.7, resolve Phase 4-lite,
גילוי הסכמות. אין התנגשות צפויה בקבצי הקולנוע, אבל `pipeline.py` ו-`resolve.py`
נגעו בשני הצדדים — במיזוג הבא לעבור עליהם ידנית.

## מה נשאר לליטוש (עם אלון)

- ~~להגיע ל-CARD_REQUIRED מלא~~ — הושג בסבב סינמה סיטי 1 (15.07.26).
- **טעימת ניסוחים**: כל הודעות הקולנוע (קיר-כרטיס, pending, כשלי `_vary`) —
  לעבור עליהן בעין של אלון לפני לקוחות.
- **החלטת merge**: הענף מאחורי main בכ-10 קומיטים; להחליט מתי ממזגים
  (עדיף אחרי טעימת הניסוחים), עם מעבר ידני על `pipeline.py`/`resolve.py`.
- **pause-resume קולנועי מהוואטסאפ**: תרחישי MISSING:format / MISSING:seats
  בעולם האמיתי (סרט עם כמה פורמטים, אולם כמעט מלא) מקצה לקצה.
- ~~לכסות live גם רב-חן~~ — הושג בסבב רב-חן 1 (16.07.26); כל שלוש הרשתות
  מכוסות בפועל.
- ~~חיווט chain ל-pipeline~~ — הושג (17.07.26): extract → run_booking →
  resolve_cinema_url, כולל extract חי ואימות DRY_RUN על רב-חן.

## איך מריצים את הספייק

```bash
# מתוך שורש ה-worktree (בשביל ה-.env):
.venv/bin/python poc/spike_cinema.py ["שם סרט"] ["עיר"] [DD.MM] [HH:MM] [כרטיסים]
# ברירות מחדל: "האודיסאה", ראשון לציון, מחר, 20:00, 2
.venv/bin/python poc/spike_cinema.py "חינה אמריקאית" "ראשון לציון" 15.7 20:00 2
```

DRY_RUN מקודד קשיח בספייק — עצירה ב-CARD_REQUIRED/MISSING/OPTIONS/FAILED היא
הצלחה. הפלט מדפיס resolve, ‏ActionResult‏, נתיב ה-steps log, ומשחרר את סשן
ה-Browserbase אוטומטית. לפני קומיט: `ruff check . && pytest`.
