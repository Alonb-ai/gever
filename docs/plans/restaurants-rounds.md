# סבבי למידה — ורטיקל המסעדות (16.07.2026)

סבב חיזוק על סמך בנצ'מרק קו-הבסיס (nav_bench v1, 8 מסעדות אמיתיות, DRY_RUN) +
חקירת resolver חיה + 3 ריצות ממוקדות. branch: `restaurants-rounds`.

## 1. ניתוח בנצ' v1 (ריצה בלי פרטי intake — עצירות MISSING כנות)

| מסעדה | תוצאה | אבחנה |
|---|---|---|
| אסה איזקאיה | missing:last_name (182ש', 8 צעדים) | ניווט נקי; flex בחר 19:15 כשה-20:00 waitlist; חריג "אפשרות יחידה" עבד (אזור עישון); PERK+AGREED נאספו |
| טאיזו / קלארו / רוסטיקו / הדסון | missing:seating_area (79-87ש', 5-6 צעדים) | עצירות איכותיות עם OPTIONS נקיים — by-design כשאין העדפת ישיבה |
| מסא | failed:no_online_booking (41ש') | **resolve שגוי**: נבחרה MAZA (מסעדה אחרת) מרשימת many חלשה |
| אנימאר | resolve:none | כנה — המסעדה כנראה נסגרה (אין שום דף פלטפורמה; phone_hint עבד: 03-5759060) |
| OCD | missing:menu_selection (137ש', 9 צעדים) | flex בחר 21:00; עצירת תפריט עשירה כולל אזהרת "אין טבעוני/ללא-לקטוז" |

**דפוסים:** (1) אפס כשלי ניווט אמיתיים — כל העצירות כנות; הצוואר הוא ה-intake,
לא הנווט. (2) ה-resolve החזיר many לכל 7 המשימות — כותרות באנגלית (ASA, MAZA,
OCD, Hudson) לא עוברות match חזק מול בקשה בעברית (פער חוצה-כתב). (3) שתי
בחירות-ראשונות שגויות ברשימת many חלשה (מסא→MAZA, הדסון→דדה).
(4) `steps=null` בכל השורות — nav_bench מצלם `/tmp/steps_*.log` אבל היומנים
נכתבים ל-`bu_recordings/<run>/` (ברירת המחדל של `bu_record_dir`).

## 2. חקירת resolver (probes חיים מול Brave)

- **"הדסון ראשון לציון"** → הרשימה החלשה דווקא מכילה את סניפי הדסון האמיתיים
  (לילינבלום, רמת החייל) — אבל "דדה ראשון לציון" ראשונה, רק כי חלקה את שם העיר.
- **"גרקו הרצליה"** → none למרות שדף הטאביט האמיתי היה בתוצאות Brave:
  `tabitisrael.co.il/online-reservations/create-reservation?step=search&orgId=...`
  — הרגקס `_TABIT` תופס רק `/site/`. אחרי פסילת דף-הרפאים של Ontopo (bcd88a0)
  לא נשאר כלום.
- **"מסא תל אביב"** → הרשימה החלשה [MAZA, CASAMAYA] היא רעש בלתי-נמנע ברמת
  ה-resolver (אין לדף שם עברי תואם); שכבת השיחה היא המקום לברר.

## 3. תיקונים (קומיטים מקומיים ב-branch)

| קומיט | מה | אימות |
|---|---|---|
| `3b43c55` | **fix(resolve)**: תבנית `_TABIT_ORG` לדיפ-לינק orgId (חלה גם על site-fallback); `_brand_first` — מיון רשימת many חלשה: מילת המותג קודם | חי: גרקו→one/tabit (היה none); הדסון לילינבלום ראשון (היה שני). +4 טסטים |
| `d92dc74` | **fix(tests)**: טסט time_flexible יצר (והדליף) סשן Browserbase אמיתי בכל pytest, ונפל 401 תחת conftest | הסוויטה ירוקה בלי רשת |
| `189241a` | **fix(bu-task)**: חוק ברזל — בלי מנועי חיפוש; שגיאת שרת → רענון אחד ואז FAILED:broken_page מיד | ריצה C למטה. +טסט |

גייט: `ruff check` + `ruff format --check` + `pytest` — **197 עוברים**.

## 4. ריצות ממוקדות (DRY_RUN, סשן שוחרר בסוף כל ריצה)

**A — גרקו הרצליה על טאביט (דיפ-לינק מהתיקון):** `failed:browser_error`,
386ש', 10 צעדים. סיבת שורש סביבתית: **טאביט כולו במפולת** — CloudFront 503
("Lambda function... invalid") אומת גם בדפדפן אמיתי וגם ב-curl מקומי. אבל
נחשף באג פרומפט אמיתי: הסוכן פירש URL שמוצג מקוצר כ"קטוע" ויצא לספירלת
חיפושי גוגל/DDG (כולל חסימות CAPTCHA) ודומיינים מנוחשים.

**B — OCD, הענף הקשיח (time_flex=False) על שעה תפוסה:** מושלם. 3 צעדים,
55ש', עצירה מיידית: `MISSING:time` + `OPTIONS: 18:00 | 21:00` — בדיוק לפי חוק
האנטי-התלבטות (18f7a92), שהבנצ' (כולו flex) לא כיסה.

**C — גרקו שוב, אחרי תיקון הפרומפט (המפולת כ-fixture):** `FAILED:broken_page`
נקי ב-2 צעדים ו-48 שניות — פי 8 מהיר מריצה A, marker כן במקום browser_error.

## 5. מוכן לדחיפה ל-main (אחרי אישור)

1. `3b43c55` — שני תיקוני ה-resolver (מאומתים חי + טסטים).
2. `189241a` — חוק הכשל-המהיר ב-task (מאומת חי, לפני/אחרי דרמטי).
3. `d92dc74` — תיקון דליפת הסשן בטסט.

## 6. פתוח / המלצות המשך

- **מפולת טאביט (16.7):** לעקוב; כשהאתר חוזר — ריצת Tabit מלאה עד סיכום היא
  פער הכיסוי הגדול שנותר (הבנצ' 100% אונטופו). ה-resolve כבר מחזיר את
  הדיפ-לינק הנכון.
- **בנצ' v3 (רץ במקביל, פרטים מלאים):** שתי הריצות הראשונות נתקעו עד timeout
  ‏(602ש') — אסה: הווידג'ט לא הציג תוצאות ("מצאו לי שולחן" בלופ); טאיזו:
  **טופס הטלפון של אונטופו דחה את "0501234567" כלא-תקין** והסוכן ניסה פורמטים
  שוב ושוב. המלצות: טלפון בנצ' אמין יותר; לשקול חוק "שדה שנדחה פעמיים →
  MISSING:phone_invalid" כדי לא לשרוף timeout.
- **nav_bench:** לתקן את צילום ה-steps — לגלוב גם `bu_recordings/*/steps_*.log`
  (או `settings.bu_record_dir`), לא רק `/tmp`.
- **ניסוח many חלש ב-pipeline:** כשאף מועמד לא עבר match חזק, "יש כמה כאלה —
  איזה מהם?" מטעה (מציג את MAZA כאילו היא סניף של מסא). שווה דגל `weak` +
  ניסוח "לא מצאתי את X, אולי התכוונת לאחד מאלה?".
- **פער חוצה-כתב (עברית↔לטינית) ב-match:** הסיבה שכל הבנצ' חזר many. תעתוק
  מלא מסוכן; אפשרות זולה: השוואת שם מול ה-slug/og:title האנגלי של הדף.
