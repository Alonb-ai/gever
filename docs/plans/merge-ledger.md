# Merge Ledger — מפת הקומיטים והתיקונים הרוחביים (מתעדכן כל סבב)

מטרה: כשנמזג הכל ל-main — שום תיקון לא הולך לאיבוד. כל שורה: מה, איפה, סטטוס
רוחבי. מקור האמת לקומיטים הוא הענפים עצמם; זה האינדקס.

## ב-main (נדחף)

| קומיט | מהות | מקור |
|---|---|---|
| 3b43c55 | resolve: דיפ-לינק orgId טאביט + מותג-ראשון ברשימת many | restaurants-rounds |
| d92dc74 | tests: סגירת דליפת סשן BB אמיתי בכל pytest | restaurants-rounds |
| 189241a | bu-task: איסור מנועי חיפוש + כשל-מהיר על אתר מת | restaurants-rounds |
| ffd6afb | docs: סיכום סבב הלמידה מסעדות 16.7 | restaurants-rounds |
| fbc3d33 | bu: שעון ישראל לדפדפן (tz hook) — cherry-pick נקי של 96a3011 | events-vertical |

## בענפים (ממתין למיזוג)

- **events-vertical:** b969620 (browser_error), e60f9d0 (משמר "אזלו" נסתר — חזותי בלבד),
  b1c1651+8ff8be4 (docs סבבים 2-3), 96a3011 (tz — *כבר ב-main* כ-fbc3d33; במיזוג
  יתאחד, השארית: ספייק+טסטי הופעות).
- **insurance-vertical:** 378fbef (סבב 2), 8cc23b2 (הצהרת בריאות 4 קטגוריות כולל
  הריון + _EXTRACT), a540eac+ef14dcf (docs), 9b68579+932dd4f (סבב 4 — חיווט pipeline
  מלא: פרסונה סוגרת ביטוח, _SCHEMA required, _ins_draft צבירה בין תורות; E2E הוכח
  עד עצירת ספק על נתוני-דמה). נשאר: ריצת פרמיה עם זהות עקבית דרך הצינור המלא.
- **cinema-vertical:** ליטוש מלא + רב-חן 508babf (_ravhen_from_planet). חיווט chain — בעבודה.
- **restaurants-rounds:** התמזג כולו ל-main ✓ (הענף נשאר לסבבים הבאים).
- **feature/nudge:** 0cfaf31 — נדנוד עדין (תזכורת אחת ב-5 נק' המתנה, ביטול על כל
  הודעה נכנסת). עבר ביקורת ponytail ✓, 208 ירוקים ×2. **מוכן לדחיפה הבאה.**
- **feature/model-compare:** 4153c54 — ספק-לפי-קידומת (_make_llm), מפתחות ל-subprocess,
  persona_eval רב-ספקי (שופט תמיד Gemini), runbook + nav_bench בענף. ביקורת ponytail ✓,
  202 ירוקים, אפס תלויות חדשות. **מוכן לדחיפה** (עובד גם בלי המפתחות — ברירת מחדל זהה).
- **fix/cross-lessons:** a00561d — משמר דיווח-זבל ב-_parse_result (הלקח הרוחבי של
  56c80a0 מההופעות, מוטמע פעם אחת לליבה; מאחד לתוכו את משמר ה"final ריק").
  ruff+‏201 pytest ירוקים. **מוכן למיזוג ל-main.**
- **fix/bb-session-owner:** 8e62de7 — ה-sweep משחרר רק סשנים עם תג owner שלו
  (userMetadata ביצירה; ברירת מחדל prod, dev ב-.env מקומי — כבר הוסף ל-.env של
  המכונה; אפס פעולה ב-Coolify). סוגר את באג רצח-הריצות של 17.07 00:23.
  203 ירוקים. **מוכן למיזוג ל-main.**

## תיקונים רוחביים — מפת החלה

| ממצא | הוחל ב- | רלוונטי גם ל- | סטטוס |
|---|---|---|---|
| tz שעון ישראל (אתרים מרנדרים שעה ב-JS) | main (כל הוורטיקלים דרך bu_runner) | הכל | ✓ סגור |
| איסור מנועי חיפוש + כשל-מהיר על אתר מת | main (_build_task מסעדות) | task builders של הופעות/קולנוע/ביטוח בענפים | ⚠ למיזוג: להעתיק את שני חוקי הברזל (בלי מנועי חיפוש; 503→רענון אחד→FAILED:broken_page) ל-_build_concert_task / _build_cinema_task / _build_insurance_task — הם לא יורשים מ-_build_task |
| נתוני דמה קנוניים נחסמים בצד השרת (0501234567/example.com/ת"ז 123456782) | insurance spike, nav_bench | כל ספייק/בנצ' עתידי | ✓ מתועד בזיכרון + כאן |
| "אזלו" נסתר ב-SPA לא קובע sold_out — אימות חזותי | events (e60f9d0, בתוך _build_concert_task) | קולנוע (מושבים "פנויים" שנחסמים), מסעדות | ⚠ למיזוג: אין ל-main בית משותף להנחיות task (ה-builders ענפיים; _NAV_GENERIC חי רק בענפים) — במיזוג להוסיף את עקרון "רק מה שמוצג חזותית" ל-_NAV_GENERIC/builders של קולנוע+מסעדות; לא הוטמע ב-main כי שינוי פרומפט המסעדות בלי ריצה חיה מסוכן |
| MISSING מרוכז לכל הדף (לא פר-שדה) | insurance (פרוטוקול מלא, תואם-לאחור: 695ddea+378fbef) | הופעות (ת"ז+טלפון בלאן) | ⚠ למיזוג: מגיע לליבה אוטומטית עם מיזוג הביטוח (_parse_result+pipeline); אחרי המיזוג לאמץ ב-_build_concert_task |
| דיווח סופי בלי אף marker (זבל "Waited for 5 seconds" כשהדפדפן נהרג) → FAILED:browser_error | events (56c80a0) | כל הוורטיקלים (ליבת _parse_result) | ✓ הוטמע ב-fix/cross-lessons (a00561d) — מכסה גם את משמר ה"final ריק" הישן; במיזוג events הגרסה הענפית ב-_parse_result מתיישבת על של main |
| release_session בניסיון יחיד — נפילת רשת בשחרור = סשן דולף RUNNING | insurance (8ad0954: retry x3) | כל הוורטיקלים (browser_book.py משותף, כולל מסעדות בפרוד) | ⚠ למיזוג: מגיע עם מיזוג הביטוח; לא שוכפל ל-fix/cross-lessons בכוונה (שכפול = קונפליקט); אם מיזוג הביטוח מתעכב — לשקול cherry-pick של חלק ה-browser_book בלבד |
| ה-decoding המוגבל של gemini-3.5-flash משמיט בשיטתיות שדות סכמה שאינם ב-required (הוכח בבידוד: בלי סכמה — חילוץ מושלם) | insurance (9b68579: _SCHEMA required לשדות ביטוח) | כל שדה סכמה עתידי בכל ורטיקל — שדה קריטי חייב required או גארד | ⚠ עיקרון תכן קבוע מעכשיו; במיזוג הביטוח מגיע לליבה |
| **לקוח-בלולאה** — אתרים דורשים קלט חי של הלקוח באמצע הריצה (OTP ב-SMS בקופת אחרי "לרכישה"; ת"ז אמיתית בלאן לפני תשלום): גבר מבקש בוואטסאפ → הלקוח עונה → resume מאותו מסך עם הקלט | עוד לא — **הפיצ'ר הבא (החלטת אלון 17.7)** | הופעות (OTP/ת"ז — החוסם היחיד ל-CARD_REQUIRED), ביטוח (ת"ז/פרטים רגישים), קולנוע (אימות אבטחה שנצפה), מסעדות (כבר פתור חלקית: קיר-כרטיס דרך דפדפן-גבר) | 🎯 לבנות בליבה פעם אחת: הרחבת פרוטוקול MISSING/resume הקיים לקלט-אמצע-ריצה (התשתית — pause-resume + keepAlive — כבר קיימת); ת"ז/OTP עוברים דרך גבר, פרטי תשלום לעולם לא |

## נכסי בנצ' (session worktree)

- poc/nav_bench.py — קו הבסיס הקבוע של הנווט (סט 8 המסעדות). v1=פרוטוקול בלי
  intake, v4=פרטים מלאים. persona_eval קו בסיס שיחה: 15/16 (16.7).
