# Merge Ledger — מפת הקומיטים והתיקונים הרוחביים (מתעדכן כל סבב)

מטרה: כשנמזג הכל ל-main — שום תיקון לא הולך לאיבוד. כל שורה: מה, איפה, סטטוס
רוחבי. מקור האמת לקומיטים הוא הענפים עצמם; זה האינדקס.

## ב-main (נדחף)

| קומיט | מהות | מקור |
|---|---|---|
| 3b43c55 | resolve: דיפ-לינק orgId טאביט + מותג-ראשון ברשימת many | restaurants-rounds |
| d92dc74 | tests: סגירת דליפת סשן BB אמיתי בכל pytest | restaurants-rounds |
| 189241a | bu-task: איסור מנועי חיפוש + כשל-מהיר על אתר מת | restaurants-rounds |
| ffd6afb | docs: סיכום סבב הלמידה מסעדות 16.7 | restaurants-rounds |
| fbc3d33 | bu: שעון ישראל לדפדפן (tz hook) — cherry-pick נקי של 96a3011 | events-vertical |

## בענפים (ממתין למיזוג)

- **events-vertical:** b969620 (browser_error), e60f9d0 (משמר "אזלו" נסתר — חזותי בלבד),
  b1c1651+8ff8be4 (docs סבבים 2-3), 96a3011 (tz — *כבר ב-main* כ-fbc3d33; במיזוג
  יתאחד, השארית: ספייק+טסטי הופעות).
- **insurance-vertical:** 378fbef (סבב 2), 8cc23b2 (הצהרת בריאות 4 קטגוריות כולל
  הריון + _EXTRACT), a540eac+ef14dcf (docs), 9b68579+932dd4f (סבב 4 — חיווט pipeline
  מלא: פרסונה סוגרת ביטוח, _SCHEMA required, _ins_draft צבירה בין תורות; E2E הוכח
  עד עצירת ספק על נתוני-דמה). נשאר: ריצת פרמיה עם זהות עקבית דרך הצינור המלא.
- **cinema-vertical:** ליטוש מלא + רב-חן 508babf (_ravhen_from_planet) + חיווט chain
  f7c1b9a ✓. **מוזג סופית על push-cinema** (ראו סעיף "מיזוג קולנוע סופי" למטה) —
  מוכן לדחיפת אלון.
- **restaurants-rounds:** התמזג כולו ל-main ✓ (הענף נשאר לסבבים הבאים).
- **feature/nudge:** 0cfaf31 — נדנוד עדין (תזכורת אחת ב-5 נק' המתנה, ביטול על כל
  הודעה נכנסת). עבר ביקורת ponytail ✓, 208 ירוקים ×2. **מוכן לדחיפה הבאה.**
- **feature/model-compare:** 4153c54 — ספק-לפי-קידומת (_make_llm), מפתחות ל-subprocess,
  persona_eval רב-ספקי (שופט תמיד Gemini), runbook + nav_bench בענף. ביקורת ponytail ✓,
  202 ירוקים, אפס תלויות חדשות. **מוכן לדחיפה** (עובד גם בלי המפתחות — ברירת מחדל זהה).
- **fix/cross-lessons:** a00561d — משמר דיווח-זבל ב-_parse_result (הלקח הרוחבי של
  56c80a0 מההופעות, מוטמע פעם אחת לליבה; מאחד לתוכו את משמר ה"final ריק").
  ruff+‏201 pytest ירוקים. **מוכן למיזוג ל-main.**
- **fix/bb-session-owner:** 8e62de7 — ה-sweep משחרר רק סשנים עם תג owner שלו
  (userMetadata ביצירה; ברירת מחדל prod, dev ב-.env מקומי — כבר הוסף ל-.env של
  המכונה; אפס פעולה ב-Coolify). סוגר את באג רצח-הריצות של 17.07 00:23.
  203 ירוקים. **מוכן למיזוג ל-main.**
- **feature/freeform-voice** (worktree ‏`freeform-voice`‏, מבוסס ‏push-cinema‏):
  ‏ae83347‏ (ליבה: ‏INTENTS‏ 28 כוונות + ‏`_say`/`_presay`‏ עם ולידציה
  דטרמיניסטית ו-fallback שקוף למאגרים) + ‏6d7bec1‏ (הסבת כל אתרי הקריאה
  הגנריים: פעימות/נדנודים/שחרור עם ‏pre-generate‏ בזמן ההמתנה — אפס לטנציה;
  ‏`_maybe_ack(intent)`‏ בודק חלון לפני חילול; ‏`_failure_reply`‏ async עם ctx
  מלא; ‏resolve/retry/missing/alt-time/failures/resume-ack/busy‏ — הכל ‏`_say`‏
  עם נוסחי ה-inline כ-fallback). נשארו קשיחות בכוונה: ‏pending_confirm‏ /
  ‏card_wall‏ / ‏booked_confirmed‏ (הרכבת f-string רב-חלקים). טסטים: מודל כבוי
  גלובלית ב-conftest (מסלול המאגרים דטרמיניסטי), טסט חיווט כוונה→אתר + ‏E2E‏
  לטיימר עם טקסט מחולל. שער: ruff נקי + 283 ירוקים ×2. בלי ריצות דפדפן —
  אימות חי של איכות החילול (ולידציה מול מודל אמיתי) לפני מיזוג.

## תיקונים רוחביים — מפת החלה

| ממצא | הוחל ב- | רלוונטי גם ל- | סטטוס |
|---|---|---|---|
| tz שעון ישראל (אתרים מרנדרים שעה ב-JS) | main (כל הוורטיקלים דרך bu_runner) | הכל | ✓ סגור |
| איסור מנועי חיפוש + כשל-מהיר על אתר מת | main (_build_task מסעדות) | task builders של הופעות/קולנוע/ביטוח בענפים | ⚠ למיזוג: להעתיק את שני חוקי הברזל (בלי מנועי חיפוש; 503→רענון אחד→FAILED:broken_page) ל-_build_concert_task / _build_cinema_task / _build_insurance_task — הם לא יורשים מ-_build_task. קולנוע: הוחל בחזרת המיזוג (e45e939) |
| נתוני דמה קנוניים נחסמים בצד השרת (0501234567/example.com/ת"ז 123456782) | insurance spike, nav_bench | כל ספייק/בנצ' עתידי | ✓ מתועד בזיכרון + כאן |
| "אזלו" נסתר ב-SPA לא קובע sold_out — אימות חזותי | events (e60f9d0, בתוך _build_concert_task) | קולנוע (מושבים "פנויים" שנחסמים), מסעדות | ⚠ למיזוג: אין ל-main בית משותף להנחיות task (ה-builders ענפיים; _NAV_GENERIC חי רק בענפים) — במיזוג להוסיף את עקרון "רק מה שמוצג חזותית" ל-_NAV_GENERIC/builders של קולנוע+מסעדות; לא הוטמע ב-main כי שינוי פרומפט המסעדות בלי ריצה חיה מסוכן. קולנוע: הוחל בחזרת המיזוג (e45e939); מסעדות — עדיין ממתין לריצה חיה |
| MISSING מרוכז לכל הדף (לא פר-שדה) | insurance (פרוטוקול מלא, תואם-לאחור: 695ddea+378fbef) | הופעות (ת"ז+טלפון בלאן) | ⚠ למיזוג: מגיע לליבה אוטומטית עם מיזוג הביטוח (_parse_result+pipeline); אחרי המיזוג לאמץ ב-_build_concert_task |
| דיווח סופי בלי אף marker (זבל "Waited for 5 seconds" כשהדפדפן נהרג) → FAILED:browser_error | events (56c80a0) | כל הוורטיקלים (ליבת _parse_result) | ✓ הוטמע ב-fix/cross-lessons (a00561d) — מכסה גם את משמר ה"final ריק" הישן; במיזוג events הגרסה הענפית ב-_parse_result מתיישבת על של main |
| release_session בניסיון יחיד — נפילת רשת בשחרור = סשן דולף RUNNING | insurance (8ad0954: retry x3) | כל הוורטיקלים (browser_book.py משותף, כולל מסעדות בפרוד) | ⚠ למיזוג: מגיע עם מיזוג הביטוח; לא שוכפל ל-fix/cross-lessons בכוונה (שכפול = קונפליקט); אם מיזוג הביטוח מתעכב — לשקול cherry-pick של חלק ה-browser_book בלבד |
| ה-decoding המוגבל של gemini-3.5-flash משמיט בשיטתיות שדות סכמה שאינם ב-required (הוכח בבידוד: בלי סכמה — חילוץ מושלם) | insurance (9b68579: _SCHEMA required לשדות ביטוח) | כל שדה סכמה עתידי בכל ורטיקל — שדה קריטי חייב required או גארד | ⚠ עיקרון תכן קבוע מעכשיו; במיזוג הביטוח מגיע לליבה |
| **לקוח-בלולאה** — אתרים דורשים קלט חי של הלקוח באמצע הריצה (OTP ב-SMS בקופת אחרי "לרכישה"; ת"ז אמיתית בלאן לפני תשלום): גבר מבקש בוואטסאפ → הלקוח עונה → resume מאותו מסך עם הקלט | ליבה נבנתה ב-feature/customer-in-loop (21e2ded): MISSING:sms_code/id_number ב-task, שאלה דחופה-אך-רגועה, ניתוב דטרמיניסטי ל-resume באותו סשן, הערך לא נשמר בשום מקום קבוע (_sensitive בזיכרון + _scrub ל-recap/tail/debug), נדנוד מהיר ל-OTP | הופעות (OTP/ת"ז — החוסם היחיד ל-CARD_REQUIRED), ביטוח (ת"ז/פרטים רגישים), קולנוע (אימות אבטחה שנצפה), מסעדות (כבר פתור חלקית: קיר-כרטיס דרך דפדפן-גבר) | 🔄 ליבה נבנתה ב-feature/customer-in-loop (21e2ded); נשאר: החלת קופת/לאן בענף ההופעות + מבחן OTP אמיתי עם אלון |

## חזרת מיזוג קולנוע (afb40af + e45e939, ‏17.07)

חזרה גנרלית על worktree ‏`cinema-merge`‏ (branch ‏`rehearsal/cinema-merge`‏ מ-main
‏cb135af): מיזוג cinema-vertical ‏(f7c1b9a) + החלת ה-⚠. **לא נדחף; לא מוזג ל-main.**
שער סופי: ruff נקי + **250 pytest ירוקים** (מסעדות+קולנוע).

**קונפליקטים ופתרונם** (הכלל: התנהגות main נשמרת למסעדות; הקולנוע מתווסף):

- `bu_runner._build_task`: פרומפט המסעדות של main נשמר inline מילה-במילה;
  `_NAV_GENERIC` של הענף משרת את ה-builder הקולנועי בלבד (איחוד מלא = רק אחרי
  ריצה חיה של מסעדות על נוסח משותף).
- `bu_runner._parse_result` (הקונפליקט המכוון): git איחד נקי — משמר-הזבל של
  a00561d ‏(browser_error) + חילוץ המושבים הקולנועי חיים יחד; ההצטלבות האמיתית
  היחידה תוקנה ידנית — seats נקרא רק משורת ה-SUMMARY_REACHED, לא מכל בלוק
  3-השורות של main (שורת OPTIONS עם `|` זיהמה אותו; הרגרסיה של הענף תפסה).
- `resolve.py`: ‏`resolve_reservation_url` של main נשמר במלואו (דף-רפאים,
  brand_first, Phase 4-lite, phone_hint); ‏`_pick` = ליבת קולנוע בלבד;
  ‏`search_cinema` על ‏`_brave_raw`‏; ב-`_candidate` נשמר `pattern is _TABIT`
  (הגרסה הענפית `platform == "tabit"` קדמה ל-`_TABIT_ORG` והייתה מדביקה slug
  הקסדצימלי לכותרות orgId — רגרסיית מסעדות שנמנעה).
- `pipeline.py`: ענף none — הודעת קולנוע לפני phone_hint; קיר-כרטיס קולנועי
  קיבל `_arm_nudge` (הנדנוד נחת ב-main אחרי הפיצול); `_attempt` =
  resume+time_flex של main + task_type/movie/city; heartbeat של main נשמר גם
  ב-run_commit; שחרור-סשן-אחרי-כישלון של main נשמר בשני המסלולים.
- טסטים: קיר-כרטיס קולנועי יושר לדפדפן-גבר (לינק `/b/{token}`, לא URL גולמי);
  `_fake_cinema` נפרד (search_cinema מחזיר רשימה, search_reservation — tuple).

**⚠ שהוחלו (e45e939):** שני חוקי הברזל (איסור מנועי חיפוש; שגיאת-שרת→רענון-אחד→
FAILED:broken_page) + עקרון "רק מה שמוצג חזותית" (e60f9d0, מנוסח גנרית) —
ל-`_NAV_GENERIC` הקולנועי בלבד, עם עוגני רגרסיה בטסט ה-task. פרומפט המסעדות לא
נגע. שאר ה-⚠ (MISSING מרוכז, release_session retry) מגיעים עם מיזוג הביטוח — לא נגעו.

**להחלטת אלון (מעבר לניסוחים):**
1. טעימת ניסוחים — כל הודעות הקולנוע (קיר-כרטיס, pending, כשלי `_vary`) — כמתוכנן.
2. שתי הכרעות התנהגות קטנות שהוכרעו בחזרה וצריכות אישור: (א) `_arm_nudge` על
   קיר-כרטיס קולנועי (יישור לנדנוד של main); (ב) הודעת קיר-הכרטיס הקולנועית
   *בלי* `_agreed_line` (כמו שהענף כתב; במסעדות היא כן מצורפת) — להשוות?
3. תיקון ה-seats (קריאה משורת ה-SUMMARY_REACHED בלבד) — השינוי הסמנטי היחיד
   מעבר לפתרון קונפליקטים; מכוסה טסטים, שווה מבט של 30 שניות.
4. איחוד `_NAV_GENERIC` עם הבלוק ה-inline של המסעדות — נדחה עד ריצה חיה.

## מיזוג קולנוע סופי — push-cinema (‏17.07, מוכן לדחיפת אלון)

worktree ‏`cinema-final`‏, branch ‏`push-cinema`‏ מ-main ‏c545068 (העדכני — כולל
לקוח-בלולאה 21e2ded ו-card-idle ‏452cec9). שני קומיטים, **לא נדחף** (אלון דוחף):

- **20a1564** — Merge cinema-vertical ‏(f7c1b9a): אותם פתרונות מהחזרה (afb40af)
  מילה-במילה, כולל ה-⚠ של e45e939 (חוקי הברזל + אימות חזותי ב-_NAV_GENERIC,
  עם עוגני הרגרסיה) ותיקון ה-seats (קריאה רק משורת SUMMARY_REACHED). חדש מול
  החזרה: הוראות לקוח-בלולאה (MISSING:sms_code/id_number) הוחלו מילה-במילה גם
  על ה-task הקולנועי + עוגן בטסט; מסלול ה-pipeline המשותף (SENSITIVE_FIELDS,
  ניתוב דטרמיניסטי ל-resume) חל על קולנוע אוטומטית. אפס קונפליקטים חדשים
  מעבר לחזרה — הדלתא של main נבלעה נקי (אומת בדיפרנציאל מול rehearsal).
- **1a07dec** — החלטות אלון 17.7: (א) נדנוד קיר-כרטיס קולנועי עם session_id
  (card-idle — נדנוד→שחרור סשן נטוש — חל גם על קולנוע); (ב) _agreed_line
  מצורפת להודעת קיר-הכרטיס הקולנועית אחרי סיכום ההקרנה+מושבים. עוגני טסטים.

שער: ruff נקי + **264 pytest ירוקים ×2** (בשני הקומיטים בנפרד).
נשאר פתוח (לפי החזרה): טעימת ניסוחי קולנוע ע"י אלון; איחוד _NAV_GENERIC עם
פרומפט המסעדות — רק אחרי ריצה חיה.

## עקרון resolver (החלטת אלון 17.7, בעקבות באג הנחיתות השגויות)

resolve דו-שלבי: **שלב 1 — חיפוש פנימי באגרגטורים בלבד** (אונטופו/טאביט; לאן/קופת;
רשתות הקולנוע — קבוצות סגורות שמכסות שוק שלם), **שלב 2 — fallback גנרי גמיש**
(חיפוש רחב → אתר המקום עצמו → טלפון). כלל קבלה לשלב 1: אגרגטור + שימוש חוזר +
endpoint יציב. אתר בודד לעולם לא מקבל אינטגרציה ייעודית. הנווט עצמו נשאר
לא-דטרמיניסטי — אפס מתכונים לזרימת ההזמנה (העיקרון מה-roadmap נשמר).

## נכסי בנצ' (session worktree)

- poc/nav_bench.py — קו הבסיס הקבוע של הנווט (סט 8 המסעדות). v1=פרוטוקול בלי
  intake, v4=פרטים מלאים. persona_eval קו בסיס שיחה: 15/16 (16.7).
- follow-up קטן ל-persona_eval הרב-ספקי: הריצה הראשונה של Haiku נתקעה שעות על
  httpx.ReadTimeout (קריאת REST ל-Anthropic בלי timeout/retry) — להוסיף timeout
  מפורש (~60ש') + ניסיון חוזר אחד בענף feature/model-compare לפני שימוש הבא.

## fix/resolver-targeting (worktree resolver-fix): 1ceb612 + 6fa0a12 — ה-resolver הדו-שלבי חי

מימוש עקרון ה-resolver מסעיף ההחלטה: **שלב 1 = החיפוש הפנימי של אונטופו
(`/api/unified_search`→`/api/venue_profile`→page slug; venue slug≠page slug) וטאביט
(`bridge.tabit.cloud/organizations/search?q=` — aliases דו-לשוניים, `_id`=orgId)**,
עטוף try רחב שנופל בשקט ל-Brave (שלב 2). תיקוני fallback: משקל מותג (אסה↛גייג'ין),
הכרעת/חסימת סניף (בזל→בזל, לא רוטשילד בשקט), גישור תעתיק עברית↔אנגלית
(אסה↔ASA, הדסון↔Hudson), ותיקון דף-רפאים: המרקר "לא פעיל" פסל דפים חיים
(i18n `nonActive` בכל דף אונטופו) → הוחלף ב-`out_of_business`+"אינה זמינה להזמנות"
(אומתו חי משני הצדדים). resolve-only לפני/אחרי על סט הבנצ' (מצב חדש
`NAV_BENCH_RESOLVE_ONLY=1` + מדד "יעד נכון", 1ceb612): לפני — אסה→גייג'ין,
בזל→רוטשילד, מסא→MAZA-רפאים, הדסון→רשימה עם דדה; אחרי — 5 one נכונים
(4 internal, אסה דרך brave+תעתיק), הדסון=many של שני סניפי הדסון בלבד (סניף
ראשל"צ לא קיים בשום פלטפורמה — אומת), מסא=none נכון (רפאים נפסל), אנימאר=none
(סגורה). דטרמיניסטי בשתי ריצות רצופות. שער: ruff נקי + 241 ירוקים. בלי ריצות
דפדפן (resolve-only בלבד) — הבנצ' המלא עם הנווט הוא הצעד הבא.

## feature/speedups (worktree speedups, מבוסס push-cinema): 481a2ee — זירוז 3+4+5

פריטי הדפדפן מדוח הזירוז (17.7): (3) `_max_steps` לפי ורטיקל — מסעדות 40→25,
קולנוע/הופעות/ביטוח נשארים 40 + חוק המחסום העיקש ב-task (אותו מחסום אחרי 2 גישות
שונות → FAILED מיד, שני הוורטיקלים); (4) בלוק `_STEP_DIET` משותף — שרשור פעולות
באותו צעד, בלי scroll-חיפוש לאלמנט שנראה, בלי וידוא-חוזר; חוקי הברזל בעינם;
(5) `_shorten_page_readiness` — **אין פרמטר BrowserProfile ל-readiness ב-0.13.1**:
קשיח ב-`BrowserSession._navigate_and_wait` (3 same-domain / 8 cross-domain);
עטיפה דקה מזריקה 3 שנ' כשלא הועבר timeout מפורש. שער: ruff נקי + 269 ירוקים
פעמיים. בלי ריצות דפדפן — אימות בבנצ' 8-המסעדות על ענף האינטגרציה לפני פרוד
(פרומפט המסעדות מוכח-חי — הלקח מהלדג'ר).

## fix/landing-beta (worktree landing-fixes): f19477d+f244fa5 — תיקוני דף הנחיתה מה-audit

מוכן למיזוג. יישום ממצאי ה-audit שאלון אישר (17.7), בלי שום תוספת "בטא לחברים":
(1) `web/og-image.png` ‏1200×630 (Pillow, ‏Arial Hebrew Bold, היפוך bidi ידני —
אומת ויזואלית: "גבר." קריא, הנקודה כתומה משמאל; 15.6KB) + `web/favicon.svg`
("ג" כתומה על ריבוע מעוגל) + מטא: og:image ‏(URL מלא ל-Pages), og:title מעודכן,
twitter `summary_large_image`, ‏`link rel=icon`; (2) ‏`section[id]{scroll-margin-top:110px}`
(nav נמדד ~98px); (3) כל 4 קישורי wa.me → מספר הטסט 15556718899, פקודת ה-sed
ב-`web/README.md` עודכנה להחלפה הקבועה; (4) ה"כדאי": br של ה-CTA הסופי מוסתר
מתחת ל-900px (+רווח נגד הידבקות מילים — נתפס בבדיקה חיה), דיאטת פונטים
(Alef 700 + Plex Hebrew 400-700, ‏Frank Ruhl הוסר — grep נקי), הרוטור נערם לשתי
שורות מתחת ל-480px, ה-+ ב-FAQ מתהפך ל-− ב-`details[open]` (CSS בלבד, בלי JS).
אומת חי בדפדפן (דסקטופ 1280 + מובייל 375): תגיות מאוזנות (html.parser), אפס
overflow אופקי, FAQ +/− עובד. בלי push.

## push-17-7c — ענף האינטגרציה הסופי (17.07, מוכן לדחיפת אלון)

worktree ‏`push-17-7c`‏, branch ‏`push-17-7c`‏ מ-main ‏09dbdae. שלושה קומיטי מיזוג,
**לא נדחף; main לא נגוע**:

- **024f509 — Merge feature/freeform-voice** (נקי, אפס קונפליקטים): מביא את כל
  שרשרת push-cinema (מיזוג הקולנוע 20a1564 + החלטות אלון 1a07dec + heartbeat
  מרווח 802a8c6) + הקול החופשי (ae83347+6d7bec1+7bc4499) + אינטייק מקבילי
  (bac36d8) + רוטציית כינויים (e348c33).
- **07db64c — Merge feature/speedups** (נקי): 481a2ee — זירוז 3+4+5
  (max_steps 25 למסעדות, חוק מחסום עיקש + _STEP_DIET ב-task, readiness 8→3 שנ').
- **f0d332c — Merge fix/resolver-targeting** (1ceb612+6fa0a12) — הקונפליקט הצפוי
  ב-resolve.py, נפתר לפי הכלל "שני העולמות חיים יחד":
  - כותרת הקובץ: אוחדה — התיאור הדו-שלבי (העדכני) + שורת הקולנוע.
  - `_candidate`/`_from_brave`: נשמרה חתימת הקולנוע (`platforms=_PLATFORMS` +
    חיתוך כותרות סינמה סיטי + `pattern is _TABIT`); הענף הדו-שלבי לא שינה את
    גופן — אפס אובדן.
  - **התנגשות שקטה ש-git לא סימן**: שני העולמות מגדירים `_pick` — הקולנועי
    הסינכרוני הוגדר אחרון והצל על האסינכרוני החדש של המסעדות (לולאת דף-הרפאים)
    → `resolve_reservation_url` היה נשבר בריצה. נתפס במעבר שורה-שורה מול שני
    ההורים; הקולנועי שונה ל-`_pick_cinema` (+עדכון הקריאה ב-resolve_cinema_url
    ודוקסטרינג). המסלול הדו-שלבי למסעדות נכנס במלואו (internal sources, `_select`
    מודולרי, async `_pick`, via, dead_hit) — כל פונקציות הקולנוע במלואן.
  - אומת בדיפרנציאל מלא מול שני ההורים: מול resolver-targeting — רק תוספות
    קולנוע; מול הבסיס הממוזג — רק עולם המסעדות החדש + שינוי השם.
  - הערה סמנטית מודעת: הדיסאמביגואציה הקולנועית עוברת עכשיו דרך
    `_match_restaurant` החדש (brand-token/שלד-תעתיק) — כל טסטי הקולנוע ירוקים.
- ודא (שלב 4): main כבר כולל את fix/stale-webhook (cd7828e) ואת fix/landing-beta
  (f19477d+f244fa5) — rev-list ריק לשניהם, אין דריסה.

שער: ruff check + format נקיים ×2 + **310 pytest ירוקים ×2** (מעל צפי ה-296:
291 של freeform + טסטי speedups/resolver, אחרי ניכוי חפיפות). בלי ריצות דפדפן
ובלי קריאות Gemini (מודל מנוטרל ב-conftest). הצעד הבא לפי הלדג'ר: בנצ'
8-המסעדות החי על הענף הזה (speedups+resolver יחד) לפני פרוד.

## ממצאי בטא חיה (17.7 ערב — חברים ראשונים)

1. **הצעות-תאריכים בקולנוע** (ממצא בטא #1, משתמש 0144): הסוכן ראה בלוח ההקרנות
   "עד 21.07 ואז 28.07" אבל הודעת הכשל לא הציעה את התאריכים שכן קיימים, ובפולו-אפ
   גבר כבר לא זכר אותם ("אין לי לוח בשלוף"). התיקון: דפוס alt-times של המסעדות
   מוחל על תאריכי קולנוע — MISSING:date + OPTIONS מהריצה (כמו בהופעות). סבב הבא.
2. **שאלת-שם כפולה** (ממצא בטא #2, משתמשת 7775): תשובת MISSING:name נשמרת לפרופיל
   (מוצפן) אבל ה-relaunch לא מקבל אותה ישירות — מרוץ מול כתיבת ה-DB ⇒ הריצה השנייה
   יצאה עם שם ריק ושאלה שוב. התיקון: להשחיל את תשובת השם ישירות ל-job של ה-relaunch
   (כמו notes/ישיבה), הפרופיל נשאר persistence משני. סבב הבוקר.
3. כיסוי רשת הוט סינמה (משתמשת 7775 ביקשה) — רשת רביעית לבקלוג הקולנוע.
4. **קליקים לא נתפסים באונטופו של גאיג'ין** (ממצא בטא #4, משתמש 5858, 2 ריצות):
   "השעה והתאריך נראו זמינים אך הלחיצות לא הובילו למעבר דף" — טביעת אצבע של
   בעיית hit-area/רצף-עכבר. ההנחיה המלאה (mousedown/mouseup/click על האלמנט
   הלחיץ) קיימת ב-builders של קולנוע/הופעות אך לא בפרומפט המסעדות — עכשיו יש
   ריצה חיה שמצדיקה את ההחלה (ה-⚠ מהלדג'ר). סבב הבוקר.
5. **"האודיסאה" = סרט, סווג כמסעדה** (ממצא בטא #5, תיקון אלון): task_type החמיץ —
   שם סרט טופל כרשת מסעדות ("אין סניף בכפר סבא"). חיזוק ה-extract לזיהוי סרטים
   בהקשר + אולי אימות מול לוח סרטים פעיל. סבב הבוקר.
6. **אונבורדינג מרוכז** (בקשת אלון): שיחה ראשונה = איסוף רשמי ומרוכז של שם+מייל
   (+העדפות בסיס) פעם אחת → פרופיל; סוף לשאלות שם/מייל באמצע זרימות (פותר גם את
   שורש ממצא #2). פיצ'ר לסבב הקרוב.
7. **זמינות-תחילה (feature, אלון)**: כשאין את המבוקש — גבר כבר יודע מה כן (הדף
   מולו): לדווח מתי/מה זמין ולהציע לסגור שם. מאחד את ממצא #1 עם הרעיון; עיצוב:
   ריצת recon מדווחת זמינות → הצעה → סגירה. עדיפות ראשונה.
8. **המלצות (feature, אלון)**: "תמליץ לי" — עיגון בדירוגי Google Maps/מקורות
   טרנד אזוריים → 2-3 הצעות עם שורה לכל אחת → סגירה באותו סשן. אחרי סבב התיקונים.

## חבילת לילה 2 (17→18.7) — אושרה ע"י אלון, worktree night2 / ענף fix/night2 מ-a60f460

סבב תיקונים: (א) זמינות-תחילה #1+#7 — עדיפות ראשונה; (ב) השחלת שם ל-relaunch #2;
(ג) רצף-עכבר לפרומפט מסעדות #4; (ד) סיווג סרט/מסעדה **מההקשר בשכבת השיחה, ואם לא
בטוח — שואל** (הנחיית אלון, לא רשימת שמות); (ה) אונבורדינג מרוכז קצר בשיחה ראשונה #6.
סבב פיצ'רים: (ו) המלצות — **קודם מחקר מנגנון** (Google Places API מול Gemini
grounding מול Brave) כי אלון שאל אם Maps הוא באמת המנגנון הנכון; מפתח חדש בתשלום =
החלטת בוקר. (ז) הוט סינמה לפי שיטת העבודה (recon→debrief→fix). סבב מיזוגים: הופעות
(+27) + ביטוח (+14) אל ענף אינטגרציה + עדכון דף נחיתה לורטיקלים. עדיפות המשך של
אלון: **המספר האמיתי** (חסום על ביקורת Meta) — ראשון בבוקר. DRY_RUN נשאר true.

### recon הוט סינמה (לילה 2, אומת חי 17.7)
- באג חמור קיים: "הוט סינמה" לא ב-_CINEMA_CHAINS ⇒ ה-chain נמחק וה-resolver מתאים
  בשקט לפלאנט (הרשת הלא נכונה). התיקון: entry רביעי ב-_CINEMA_PLATFORMS
  (hotcinema.co.il/movie/{id}), שלב-1 פנימי מהקטלוג המוטמע בדף הבית (app.movies=
  [{ID,Name,PageUrl}]), מיפוי 'הוט סינמה'→hot-cinema בפרומפט (לא "הוט" לבד).
- כרטוס בדומיין נפרד tickets.hotcinema.co.il (אשף Angular: כרטיסים→מושבים→תשלום);
  מושבים = SVG ב-DOM (לא canvas); דיפ-לינק /order?theaterId&eventId נוחת ישר באשף;
  API הקרנות פתוח: /tickets/movieevents?movieid&date. מושבים מוחזקים 9 דק' בלבד —
  שורת builder מותנית: לא להתמהמה בין שלבים.

### המלצות — מנגנון הוכרע (לילה 2): Gemini Grounding with Google Maps — GO
- מחקר: Places API ישיר = $35/1000 + billing חדש; Maps grounding בתוך Gemini = אותם
  נתוני מפות (דירוג, ביקורות, פתוח-עכשיו, שעות) על המפתח הקיים, 5K חינם/חודש
  (בטא=$0, סקייל ~$56-112/חודש). Brave לא מספיק כמנוע המלצות (מקור דירוגים לא
  מתועד). לסרטים: Search grounding באותו מנגנון.
- ניסוי אימות חי (11 קריאות, $0): 10/10 שאילתות החזירו 5 מקומות אמיתיים עם place_id
  ולינק maps; אימות מדגמי 5/5; עברית תקינה. GO.
- ידע לבנייה: SDK google-genai 2.8.0 תומך — tools=[Tool(google_maps=GoogleMaps())] +
  retrieval_config lat_lng; הדירוג/ביקורות בתוך grounding_chunks[].maps.text
  (markdown עקבי "**Rating:** 4.4 (4526 reviews)"), לא שדות מובנים; prompts באנגלית
  בלבד (תרגום בשכבת intent); לטנציה ~26ש' עם thinking — לבדוק thinking_budget נמוך;
  לשקלל כמות ביקורות (5.0 על 141 ביקורות ≠ 4.5 על 9K); uri נקי לוואטסאפ; חובת
  attribution "מקור: Google Maps"; ל-DB רק place_id. סקריפט+פלט מלא ב-scratchpad
  (maps_grounding_trial.py / trial_output.txt).

### לילה 2 — מיזוגי הורטיקלים: merge/night2-full (worktree night2-merge, 18.7)

ענף `merge/night2-full` מ-fix/night2 ‏(72efa4f). שלושה קומיטים, **לא נדחף**:
**8e2a9e6** Merge events-vertical · **185ad5c** Merge insurance-vertical ·
**f4236ed** דף הנחיתה. שער אחרי כל שלב: ruff check+format נקיים + pytest ×2 —
הופעות **396** (364 בסיס + הופעות בניכוי חפיפות), ביטוח **432**.

**הופעות (8e2a9e6) — קונפליקטים והכרעות** ("שני העולמות חיים יחד"; מנגנון חדש גובר):
- resolve.py: _select+_pick האסינכרוני (דו-שלבי מסעדות) נשמרו; search_events +
  _EVENT_PLATFORMS + חיתוכי כותרות לאן/קופת נוספו; `pattern is _TABIT` נשמר.
  **התנגשות שקטה (אותו לקח של push-17-7c):** resolve_event_url של הענף קרא
  ל-_pick הסינכרוני הישן — חוּוט ל-_pick_cinema; search_cinema כפול הושמט.
- pipeline.py: task_type=events נוסף לצד recommend/unsure (extract+_SCHEMA
  אוחדו); נוסחי ההופעות הוסבו לקול החופשי (_say עם ה-fallback pools);
  קיר-כרטיס הופעות קיבל _arm_nudge card+session_id (יישור למנגנון החדש);
  האונבורדינג המרוכז חל גם על events. **התנגשות שקטה:** טבלת sold_out/
  no_event_in_city של הענף עטפה מאגרים ב-_vary( ) — string בודד שהיה מתפוצץ
  לתווים ב-_say(fallback=pool); הוסב ל-tuples. **כפל מפתח:** 'date' הוגדר
  פעמיים במפת ה-MISSING (זמינות-תחילה מול מועדי הופעות) — אוחד (ruff F601).
- bu_runner: _build_concert_task נכנס כלשונו (כולל sms_code); _parse_result
  של HEAD גובר (seats מ-SUMMARY_REACHED בלבד, משמר browser_error).
- טסטים שעודכנו להתנהגות חדשה: enum הסכמה (unsure+events), failure_reply async.

**ביטוח (185ad5c) — קונפליקטים והכרעות:**
- resolve.py: גרסת HEAD נשמרה במלואה (הענף ניסה להחזיר _pick/_candidate
  ישנים); התוספת האמיתית היחידה — INSURANCE_URL+resolve_insurance_url.
- browser_book: _timeout_s תלת-דרגתי (600/900/1200) + _max_steps אוחד
  (25/40/80); insurance/form_answers לצד movie/city/artist/venue;
  **⚠ release_session retry ×3 (8ad0954) נכנס — ה-⚠ מהטבלה נסגר**.
- bu_runner: _build_insurance_task + פרוטוקול MISSING מרובה על גרסת HEAD:
  missing_fields, OPTIONS ממופתח (cap 15) + FIELD labels + גישור לשדה בודד —
  **ה-⚠ "MISSING מרוכז" מהטבלה נסגר**; payload שורת הסיום חוזר גם כ-seats וגם
  כ-extra (איחוד השם נדחה לריצה חיה). failed=infra של הענף לא הוחזר — משמר
  ה-browser_error (a00561d) מכסה ורחב ממנו; טסט ה-infra הוסר כמוכל.
- pipeline: insurance נוסף לכל השרשרת (extract/_SCHEMA+required/גארדים
  85+בריאות/_ins_draft/_merge_insurance/_multi_ask/ניתוב answers); שוב
  התנגשות _vary-סביב-מאגר בסיבות *9912 → tuples; **באג איחוד שנתפס במעבר
  שורה-שורה:** בלוק הקולנוע בקיר-הכרטיס איבד את ה-return שלו באיחוד (היה
  מדליף להודעת מסעדות) — הוחזר. _failure_reply נשאר async.
- intent.py (מיזוג אוטומטי שנסקר): שורת יכולות הפרסונה אוחדה — מסעדות +
  כרטיסי קולנוע/הופעות + ביטוח; "כרטיסים" הוסר מרשימת ה"עוד לא" (סתירה
  שהייתה גורמת לפרסונה לסרב לורטיקלים חיים — הלקח של סבב 4 בביטוח).
- טסטים שעודכנו: max_steps מסעדה 25 (זירוז), enum מלא, failure_reply async.

**דף נחיתה (f4236ed):** שורת היכולות — קולנוע/הופעות/ביטוח נסיעות "כבר עכשיו",
תורים לבד ב"בקרוב"; כרטיס "ומה בדרך"→"כרטיסים וביטוח נסיעות" (נוסח כן: מוכן
לתשלום, התשלום אצל הלקוח); המלצות בכרטיס המסעדות; FAQ+meta עודכנו; רוטור עם
דוגמאות סרט/הופעה/ביטוח/המלצה. המספר וכל קישורי wa.me לא נגעו. תגיות אומתו.

נשאר פתוח: ריצות חיות של הופעות/ביטוח דרך הענף הממוזג; איחוד seats/extra
ו-_NAV_GENERIC-מסעדות — אחרי ריצה חיה.

### לילה 2 — סטטוס ביניים על fix/night2 (364 ירוקים ×2)
2c79c6d זמינות-תחילה · 3d79b13 שם/מייל→relaunch · c610fe8 רצף-עכבר מסעדות ·
f74238a סיווג מההקשר+unsure · d432490 אונבורדינג מרוכז · c72d2f0 הוט סינמה
(שלב-1 פנימי מהקטלוג, אומת חי) · 72efa4f המלצות (Maps grounding, אומת חי,
לטנציה 4-6ש'; אזהרת ניטור: פורמט maps.text הוא התנהגות נצפית לא חוזה — אם
ישתנה, דירוגים ריקים אך המקומות יוצגו). נותר: אימות E2E חי + מיזוג הופעות/ביטוח.

### אימות E2E חי — לילה 2 (3 ריצות DRY_RUN, <$1, כל הסשנים שוחררו)
1. קולנוע/תאריך-חסר (הוט פ"ת 27.07): MISSING:date + OPTIONS 18-23.07 — התאמה 1:1
   ל-API. רצף-עכבר הופעל בפועל בצעד 18. ✅
2. מסעדה עמוסה (OCD שבת): MISSING:date + חלופות שמדלגות על ימי הסגירה האמיתיים
   (נקרא מהדף, לא הומצא) — 3 צעדים בלבד. ✅
3. הוט E2E: resolver פנימי→דומיין כרטוס→סניף/הקרנה/כמות נכונים; נעצר כנה במפת
   מושבים: browser-use מקפל תוכן SVG ("SVG content collapsed") — אין אינדקסים,
   הסוכן ניחש selectors ולא ניסה קליק-קואורדינטות. פריט המשך מוגדר: הנחיית
   קליק-לפי-קואורדינטות (getBoundingClientRect→pointer events) אחרי שני כשלי
   selector, או hint בהוט-בלוק. לא רגרסיה; ללקוח = עצירה כנה עם מושבים אמיתיים.
- אפס תיקוני קוד נדרשו; חולשת מודל מתועדת: ~10 קליקים חוזרים על תאריך מת בריצה 1
  למרות חוק המחסום העיקש.

### לילה 2 — סגירה: merge/night2-full מוכן לפוש (f4236ed, 432 ירוקים ×2)
8e2a9e6 מיזוג הופעות (396) · 185ad5c מיזוג ביטוח (432; סגר את ⚠ retry×3 של
release_session) · f4236ed דף נחיתה (ורטיקלים חיים; מספר+לינקים לא נגעו; אזכורי
בטא = תוכן קיים מאושר). 5 התנגשויות שקטות נתפסו במעבר שורה-שורה, המסוכנת:
שורת פרסונה ב-intent שטענה "כרטיסים עוד לא נתמכים" (הייתה גורמת לסירוב).
שער עצמאי של המפקח אומת: ruff 0, 432 passed. ניקוי worktrees: 22 הוסרו, נשארו
events/insurance (יתנקו אחרי פוש), rehearsal/cinema-merge (2 קומיטים לא ב-main —
לוודא שמוכלים ב-night2-full), condescending-mendel (dirty). פקודת הבוקר לאלון:
cd ~/Desktop/GeverAI && git merge --ff-only merge/night2-full && git push
פריטי המשך: SVG מושבים בהוט (קליק-קואורדינטות) · המספר האמיתי (ביקורת Meta) ·
ניטור פורמט maps.text.

### תיקון קול ההמלצות (פידבק אלון 18.7 בוקר) — fix/recommend-voice 479f183
ההגשה עברה לדמות: שמות בלבד כעוגנים, שורה חיה לכל מקום, אפס גוגל/מקור/לינק/
דירוגים מספריים (forbid דטרמיניסטי כולל \d\.\d ו"N ביקורות"); attribution הוסר
בהחלטת בעלים מודעת (תנאי Maps grounding — תועד הסיכון). דירוגים נשארו שימוש
פנימי (שקלול+רקע info1..3). 433 ירוקים ×2, אומת חי. **עין בלייב-טסטים:** השורות
החיות מגיעות מידע כללי של המודל (לא נאכף דטרמיניסטית) — לנטר המצאות עובדתיות.

## סבב שיפור 18.7 — QA חי ביטוח (main f4236ed, אין שינויי קוד)
ריצה 2 = הצלחה מלאה: 26 צעדים עד קיר MISSING מרובה של 8 שדות (p1/p2 ת"ז+מגדר+
שמות), פרוטוקול 8/8 מול _parse_result, addons="" כובד (3 כיסויים הוסרו), אין
AGREED, הודעת _multi_ask יוצאת מושלמת. פערים מדורגים:
1. גבוה: דליפת סשן במות-דפדפן — browser_error מחזיר session_id:null אבל הסשן
   RUNNING ומחויב (שוחרר ידנית); לשחרר בנתיב הקריסה (browser_book.py).
2. בינוני: ניתוק CDP תחת מקביליות (2 ריצות dev יחד מתו ב-23:43) → זיהוי
   bb_disconnected + retry אחד. **לקח תפעולי: ריצות QA דפדפן בטור בלבד.**
3. נמוך: תיוג "(נוסע 1)" חסר ב-FIELD labels · _multi_ask עוקף קול חופשי ·
   ת"ז בחבילה מרובה בלי מסלול SENSITIVE (חוב PII מתועד) · תאריכים בלי שנה
   ל-builder (נסיעה חוצת-שנה בסיכון).
חוב: רגל resume של 8 השדות→בריאות→מחיר→CARD לא נבדקה חיה (spike_insurance
יודע). יומנים ב-scratchpad + bu_recordings.

## QA חי הופעות (main f4236ed, 18-19.7 לילה) — פערים מדורגים
1. בלוקר: מפת מושבים לאן = iframe דומיין-זר (seatmap.vivenu.com, OOPIF) —
   browser-use 0.13.1 לא מכניס לעץ; 19 צעדי evaluate ריקים. תיקון: לנסות
   cross_origin_iframes=True / שדרוג 0.13.6 + חוק iframe ב-_build_concert_task
   (בלי JS; קליקים בקואורדינטות; 2 evaluate ריקים → עצירה כנה).
2. דפי רפאים בהופעות (עומר אדם kupat בלי מועדים) — בדיקת חיות בסגנון
   _ontopo_dead ב-resolve_event_url.
3. FAILED:sold_out כוזב → להוסיף FAILED:no_upcoming_dates + הודעה כנה.
4. סינון מועמדי שנה-שעברה ב-many (עדן חסון 2024).
5. _ALT_DATES_RULE לא חל על הופעות · 6. _STEP_DIET חסר בהופעות ·
7. לאן מאונדקס חלקית (מועד אחד מ-2) — לשקול שלב-1 פנימי · 8. רעש video_recorder.
עבד טוב: one/many 4/5, fallback לאן→kupat, עצירה כנה מלאה, היגיינת סשנים.
### תקרית תהליך (לקח מחייב): שני סוכני QA במקביל עם BB_SESSION_OWNER=dev
הרגו זה לזה סשנים (פראטריסייד) — מעכשיו: ריצות QA דפדפן בטור בלבד; אסור
לשחרר סשן RUNNING שלא נוצר בריצה שלך.

## קראש ה"עמוס" (דיווח אלון 18.7) — פוענח ותוקן: fix/busy-crash dfd36ef
פורנזיקה: מופע יחיד (משתמשת 2963, 13:40, שיחת המלצות ברים ב"ש); טביעת אצבע —
busy בלי תור user לפניו = קראש לפני שמירת התור בתוך converse; לא מכסה (ה-_say
הצליח שניות אחרי). 22 ריצות שחזור חיות: אפס קריסות דטרמיניסטיות מהלילה. השורש:
לקריאת המודל המובנית של converse לא היו הגנות — 5xx רגעי / resp.text=None
(json.loads(None)=TypeError) / JSON קטוע → except הכללי → "עמוס" + הודעת הלקוח
נמחקת. התיקון: retry אחד עם chat טרי על (APIError,ValueError), text=None →
ValueError ברור, שימור דגל דף-חדש; 5 טסטים אדומים-על-הישן. 438 ירוקים ×2.
לא מומש (מסוכן כמו-שהוא): תור עדות ל-_chat מפיל את בניית ההיסטוריה של ה-SDK
(role זר) — אם רוצים התרעה: שם החריגה בתוך טקסט ה-busy המותמד (role=model).
ממצא אגבי לסבב הבא: באג מגדר — גבר מדבר על עצמו בלשון נקבה כשפרופיל הלקוחה
female + סיווג gender שגוי בפרופיל של 2963.

## סבב 3 (19.7) — merge/round3 מוכן לפוש (452 ירוקים ×2)
מאחד את fix/verticals-round (6 קומיטים: דליפת סשן במות-דפדפן — הוכח חי;
חוק iframe זר + cross_origin_iframes מקובע; דפי רפאים בהופעות + no_upcoming_dates
כן; זמינות-תחילה+STEP_DIET להופעות; שנה-שעברה demote; תאריכי ביטוח מלאים +
תיוג נוסע + _multi_ask בקול חופשי) עם fix/busy-crash (dfd36ef — retry לקריאת
המודל של converse). מיזוג נקי, אומת דיפרנציאלית מול שני ההורים. אימותים חיים:
קובי פרץ (החוק נקלט, עצירה כנה, session_id דווח בכשל) + עומר אדם (דף רפאים
נפסל אחרי תיקון false-alive של מרקר ה-title). המשך מתועד: עצירה מהירה על דפי
דחיית-מופעים · שלב-1 פנימי ללאן · רגל resume ביטוח · באג המגדר.

## שער גישה (feat/access-gate fe2c285, 462 ירוקים ×2) — מוכן, כבוי כברירת מחדל
prefs.approved + cache 5ד' (fail-closed) · _gate בראש handle_inbound: זר = תשובת
GATE_MSGS אחת (חריג _vary מתועד, אפס Gemini, אפס עקבות) ואז שתיקה 6ש' · INVITE_CODES
ב-env: קוד תקף→approve+ברוך-הבא (send_text לא-מוקלט כדי לא להרוג את האונבורדינג) ·
ACCESS_GATE=false ברירת מחדל · scripts/approve_all.py לקיימים. הפעלה במעבר למספר
האמיתי: approve_all → ACCESS_GATE=true + INVITE_CODES ב-Coolify → redeploy.
נקודת מודעות: gate דלוק + Supabase למטה ממושך = השתקת כולם (fail-closed; cache
מגשר הפסקות קצרות). איפוס 3331 בוצע (19.7, גיבוי ב-scratchpad/backup_3331.json).
עתידי: רגל Lemon Squeezy — ה-webhook החתום מדליק approved (הזהות=המספר, בלי סיסמאות).

## QA סבב 4 (19.7 לילה, 3 ריצות בטור, ~23 דק' דפדפן, אפס תיקונים)
1. שעה תפוסה (מלגו מלבר 20:30): מושלם — הסוכן זיהה לבד ש"רשימת המתנה"≠זמין,
   OPTIONS 1:1 מול ה-API, הודעת טאפ עם הצעת סגירה. שאלת אלון נענתה: חלופות שעה ✓.
2. חלופת סניף (לחם בשר פ"ת): לא קיים — פער ארכיטקטוני (הדף לא מציג סניפים
   אחרים; _ALT_DATES_RULE=שעות/תאריכים בלבד). תשתית מוכנה: resolver מכיר את כל
   5 הסניפים + availability API של אונטופו עונה בלי דפדפן → אפשר "פנוי ב-20:30
   בנמל ת"א" בעלות אפס ריצה. להכרעת אלון.
3. ביטוח resume: הוכח חי — 8 שדות→resume אותו סשן→6 הושחלו→קיר מתכווץ ל-2
   (ת"ז בלבד), עצירה כנה. שקיפות: צ'קבוקס תנאים+הצהרה נחתם ומדווח AGREED —
   לשקול אישור לקוח לפני commit אמיתי. הערה: spike_insurance ממלא ת"ז-דמה
   (_test_answer) — הסבב עקף; לזכור בריצות עתידיות.
פערים קטנים: בדיקת שעה-שעה יקרה (8 שעות=20 צעדים) · אין חוק standby מעוגן ·
"תיאום טלפוני" לא מוצע ללקוח · רעש aclose קוסמטי.

## סבב 5 (19.7 לילה, לייב-טסט של אלון) — merge/round5 מוכן לפוש (502 ירוקים ×2)
חמישה ענפים: fa8c7c0 תוויות סניפים (_with_city בשורש resolve + רשת רצפים כפולים) ·
voice-media ecf58cf+b62ec35+b17d161 (קוליות נכנסות: הורדה+תמלול Gemini+אותו מסלול,
שער לפני הורדה, ≤700KB; סטיקר חגיגה webp אחרי commit, 1/יום; GIF לא קיים ב-Cloud
API — נדחה) · 43861b6 אמת-יכולות (לא מציע לצלצל, "מהמערכת" במשמר, טלפון המקום
בכשל no_online_booking, eval 3/3) · 58f2b79 המלצות בלי המקום-שנכשל (גישור תעתיק
grk) · 11c9440 באג סלון-יווני צומת-סביון (שורש: ontopo venues[:5] לפני דירוג +
בליעת החטאת עיר במותג דו-מילי; דירוג _brand_first לפני חיתוך + עיר-לא-תואמת→many,
אומת חי page/50809918 הרצליה). שני קונפליקטים נפתרו (imports; _with_city×ranking —
שני העולמות) + ציפיית טסט אחת יושרה. באגי הלייב תועדו מצילומי אלון בזמן אמת.

### עוד המלצות (fix/recommend-more 48c47da, 510 ירוקים ×2) — מוכן לפוש
_recs מחזיק את כל המדורגים (rank 10, קיצוץ במציג) + shown; "עוד" מזוהה במפתח
בקשה (category+city+notes) → הגשה מהבאפר בלי grounding; באפר ריק → קריאה אחת עם
exclude; הדרת המקום-שנכשל חלה גם על הבאפר; goal לא טוען "אין עוד". קוליות אומתו
חי ע"י אלון (הקלטה 0:05 → תשובה נכונה).

## הרחבת ביטוח ל-5 מבטחים — feature/insurance-multi (19.7 ערב, ded6c52+e3f9c67)

קבוצת מבטחים סגורה (עקרון ה-resolver): INSURANCE_COMPANIES — פספורטכארד (ברירת
מחדל, תאימות מלאה) + הראל + הפניקס + AIG + מגדל. resolve_insurance_url(company):
נקבו → one על המבטח, לא מוכר → many. שדה company בחילוץ (enum לא-required + גארד),
_ins_draft צובר, החלפת מבטח משחררת סשן ממתין. builder גנרי; חוק היעד נשאר
פספורטכארד-בלבד. הספייק מקבל מבטח כארג'. לקחי recon AIG הוטמעו (e3f9c67).

**ממצאי recon חיים (7 ריצות, 19.7, זהות-דמה):**
| מבטח | עד לאן | ממצאים |
|---|---|---|
| הראל | קיר פרטי נוסעים (8 שדות, דפוס פספורטכארד) | פרוטוקול MISSING מרוכז עבד 1:1 |
| הפניקס | קיר שמות נוסע 2 + PERK ‏10% הנחת אונליין | שאלת "מטרת נסיעה רפואית" ייחודית (MISSING:trip_medical_purpose — הפרוטוקול כיסה) |
| AIG | ריצה 1: קיר ת"ז; ריצה 2 (resume): **CARD_REQUIRED, פרמיה $31.50→$28.35 הנחת אונליין** | הנוסעים נישאים ב-resume (לקח e3f9c67) |
| מגדל | **CARD_REQUIRED** | ריצה קודמת (זיהוי לא ודאי): קיר תאריך-לידה, PERK עד 20% |
| פספורטכארד | (מוכח מסבב הבוקר) רגל resume מלאה עד CARD | — |

פתוח: ריצת E2E דרך הצינור המלא (פייפליין, לא ספייק) לכל מבטח חדש · ניסוח בחירת
מבטח בשיחה (טעימת אלון) · car insurance = החלטת מוצר נפרדת, לא נגענו.

## QA סבב 6 (19.7 ערב — הסשן החדש) — ממצאים ותיקונים

- **עשן "עוד המלצות" (48c47da)**: עבר מלא — באפר נשמר, "עוד" בלי grounding, ריקון→
  exclude, קול נקי. קוסמטי: המודל פלט "וואibe" (ערבוב כתב) בשורה חיה.
- **גאיג'ין (ממצא בטא #4) — נסגר**: רצף-העכבר עבד; זיהה waitlist≠זמין, הציע 20:15
  עם MISSING:seating_area+OPTIONS. עצירה כנה מושלמת.
- **ביטוח resume — החוב נסגר**: 8 שדות→resume אותו סשן→בריאות לפי תשובות הלקוח
  (health="אין" מהספייק — by design, לא באג)→פרמיה $90→CARD_REQUIRED. ת"ז-דמה
  עם ספרת ביקורת עוברות.
- **הוט SVG — פוצח ידנית (fix/coordinate-click, 4 קומיטים)**: (1) flash לא
  ב-allowlist קליק-קואורדינטות של browser-use ⇒ set_coordinate_clicking(True)
  אחרי בניית Agent; (2) recon ידני באשף: כל מושב = <svg class="available status">
  מקונן (884 svg!) — סלקטורי rect/path עיוורים לו; קליק אמיתי על מרכז bbox נמדד
  נקלט מיידית (הוכח בדפדפן, "1 מתוך 2"), אירועי JS סינתטיים נדחים; (3) הפרומפט:
  evaluate למדידה בלבד → קליק נטיבי על מרכז ה-bbox, מדידה מחדש אחרי זום;
  (4) no_cinema_in_city רק מלוח ההקרנות של הסרט (שתי ריצות הוסטו ל-select ניווט
  חלקי בלי פ"ת/כ"ס). llm_screenshot_size=None ⇒ קואורדינטות 1:1. **אימות E2E של
  הסוכן על המפה — עדיין פתוח** (ריצות הערב מתו על רשת).
- **fix/parallel-owner (7e32bd1)**: תג owner-pid בדב; פרוד "prod" בדיוק. נחוץ אך
  לא מספיק למקביליות: ניתוקי ה-CDP בערב 19.7 היו רשת מקומית רועדת (גם ה-API של
  הסוכנים נקטע), לא רק פראטריסייד; connectUrl של Browserbase חד-פעמי — reconnect
  אחרי ניתוק מקבל 410 והריצה מתה. ריצות דפדפן בערב רשת-רועדת = בזבוז.
- **לקח מכניקת סוכני דפדפן**: "הרץ סינכרונית" נכון לסוכני קוד; לסוכן שמריץ ספייק
  ארוך — ריצה שקטה >600ש' מפעילה את ה-watchdog. המכניקה: שיגור ברקע + סיום תור
  (התעוררות אוטומטית בסיום). 4 היתקעויות עד שנוסח.
- **WABA**: שם "גבר" name_status=AVAILABLE_WITHOUT_REVIEW (בלי אימות עסק!),
  code VERIFIED, המספר PENDING. אימות עסק מטא דורש תעודת עוסק — הוחלט: מספר
  בלבד עכשיו, עוסק כשאלון יפתח.

## פולו-אפ אחרי סגירה (feature/followup, 20.7) — "הגעתם?" + "איך היה?"

חזון אלון: אחרי סגירה אמיתית גבר חוזר כמו חבר — צ'ק-אין הגעה 5-15 דק' אחרי שעת
ההזמנה, "איך היה?" ~4 שעות אחרי (נופל בלילה 22:30-10:30 → בבוקר שאחרי,
10:30-12:00 אקראי), והתשובה נשמרת כתעדוף שמזין המלצות. אפס הודעות קבועות —
שתי כוונות חדשות ב-INTENTS (arrival_check / visit_feedback) עם מאגרי fallback
בלבד.

- **תזמון עמיד-לרדפלוי**: הפולו-אפים ב-`prefs._followups` (due+kind+place+date,
  נכתב רק ב-run_commit success — לא ב-DRY_RUN); לולאת רקע ב-lifespan
  (`followup_loop`, tick כל 90ש') מוציאה את מה שבשל. הניקוי ב-DB לפני השליחה —
  קריסה מוותרת על הודעה, לא מכפילה. `memory.set_pref` (read-merge גנרי) +
  `memory.list_followups`. ביטוח מוחרג (אין לאן להגיע).
- **חלון 24ש' של WhatsApp**: הודעה יזומה מותרת רק בתוך החלון מאז הודעת-הלקוח
  האחרונה; גארד `_last_user_ts` (זיכרון חם → prefs._chat) עם שוליים 23ש' —
  מחוץ לחלון דילוג שקט + לוג (template = שלב עתידי).
- **קליטת המשוב**: הפולו-אפ מסמן `_awaiting_feedback`; `_feedback_note` מזריק
  אמת-למערכת רק אז, והסכמה קיבלה feedback_sentiment/note (לא required). converse
  מקפל ל-`prefs.feedback` (מקום → score/note/date) באותו upsert יחיד ומוריד את
  הדגל. run_recommend מדיר score שלילי ומקדם חיובי (התאמת _rec_excluded, sort
  יציב).
- **ביטול**: אין מסלול ביטול בפייפליין — מינימום דטרמיניסטי: הודעה נכנסת עם
  הקשר ביטול (regex) מוחקת את `_followups` ברקע; false-positive רק מוותר על
  פולו-אפ.
- ponytail מתועד בקוד: כללי הזמן = בחירת-בעלים שרירותית-סבירה, לא נוסחה; תאריך
  לא-מדויק ("מחר") → אין פולו-אפ (שמרני). טסטים: tests/test_followup.py.
## קטלוג הופעות פנימי — feature/shows-catalog (20.7)

סוגר את פער הלדג'ר "לאן מאונדקס חלקית ב-Brave — לשקול שלב-1 פנימי" (ואת האימות
החי 19.7: דף אייל גולן נעלם מסט תוצאות Brave). מחקר HTTP בלבד, אפס דפדפן:

- **app/automation/shows_catalog.py** — שני מקורות (אומתו חי 20.7): לאן = דף
  הבית מטמיע __NEXT_DATA__ עם הקטלוג המלא (initialState.search.events, ‏~431
  אירועים פעילים-עתידיים: שם/היכל/עיר/תאריך-אפוק/קטגוריה; URL = ‏/events/
  <שם-במקפים>/<id>, ה-id מנתב — slug שגוי עדיין 200); קופת = גריד דף הבית
  ה-WP server-rendered, ‏~56 מופעים פעילים, אמן→URL בלבד (ה-REST ‏shows-api
  מחזיר 1,403 רשומות אך בלי תאריך שמיש ורובן ישנות). cache שעה בזיכרון; כל
  כשל מקור = לוג+דילוג, לעולם לא חריגה.
- **resolver שלב-1 (resolve_event_url)**: ‏_events_internal לפני Brave, בדפוס
  _hot_internal; מכריע רק one או many-של-פלטפורמה (מועדים אמיתיים ללקוח);
  via=internal/brave. **דיוק-גבוה בכוונה**: כל מילות האמן חייבות לפגוע —
  ה-smoke החי תפס את "שלמה ארצי" נתקע על "אצטדיון שלמה ביטוח" במילת-מותג
  בודדת. אומת חי: אייל גולן/שלמה ארצי → one מקופת; ליאת בנאי → one לאן;
  קובי פרץ → many של שני המועדים האמיתיים; לא-בקטלוג → Brave כרגיל.
- **סקירה ללקוח**: "תמליץ לי הופעה" → recommend עם category=concert (ההנחיה
  עודכנה) → run_recommend מגיש מהקטלוג בלבד (recommend_shows: לאן עם תאריכים,
  בלי ילדים/ספורט, קרוב-קודם, סינון אזור עם גישור התעתיק) דרך recommend_results
  הקיים — עוגני שמות, תאריך טקסטואלי ("25 באוגוסט" — לא נופל על איסור ‎\d.\d),
  אפס grounding ואפס המצאות; קטלוג ריק → recommend_failed הכן. באפר "עוד"
  ומנגנון ה-_recs עובדים כרגיל.
- שני באגים נתפסו ב-smoke חי ותוקנו+ננעלו בטסט: באנר קופת בלי לינק בלע את
  הלינק של ה-article השכן; כפל עיר בהיכל ("תל אביב, תל אביב") → _with_city.
- שער: ruff נקי + **545 ירוקים ×2** (conftest מנטרל את רשת הקטלוג גלובלית).
  פתוח: ריצת E2E חיה של הזמנה דרך URL קטלוגי; קופת בלי תאריכים (סקירה = לאן
  בלבד); "artist" נפרד לא קיים במקורות (title הוא שם המופע/אמן).
